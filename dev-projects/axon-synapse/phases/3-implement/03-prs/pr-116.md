---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-116 — shadow retroactive bulk migration

**Depends-on**: pr-114 (shadow enforcement gates)
**Blocks**: (none — final cleanup)
**Wave**: W5  ·  **Reversibility**: reversible (one-shot migrator + undo subcommand)
**Domain**: system
**dev-mode required**: no
**Risk**: medium
**Status**: ready-for-review

## Goal

Statement:       Ship `workspace/programs/shadow-retroactive-bulk.md` — one-shot migration over every `my-axon/dev-projects/*` (119 PRs) creating shadow records for source-artifact files touched in past PRs. On completion, flip `L:shadow-enforcement-strict=true`.

Acceptance:      dry-run reports n>0 candidate PRs; live run creates ≥1 shadow record per candidate PR; `--undo` restores prior state byte-perfect; L:shadow-enforcement-strict flips to true only after success.

Rejection:       any my-axon/ data is altered outside the shadow tree; `--undo` doesn't fully revert; flag flips on partial failure.

Linked-finding:  F-018 (retroactive shadow coverage)
Linked-demand:   D-15 (shadow on all PRs), D-17 (strict mode rollout)
Linked-ADR:      D-021 (additive only), D-031 (strict-mode flip gate)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/programs/shadow-retroactive-bulk.md` | new | migration program (~150 LOC AXON-LANG) |
| `tests/test_shadow_retroactive_bulk.py` | new | fixture project + dry-run + live + undo |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
rm workspace/programs/shadow-retroactive-bulk.md tests/test_shadow_retroactive_bulk.py; manually unset L:shadow-enforcement-strict if it flipped.
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-116 status → complete on merge

## Notes

Touches many files in `my-axon/`, but only inside the shadow subtree — never source files. Undo path is mandatory for safety.
