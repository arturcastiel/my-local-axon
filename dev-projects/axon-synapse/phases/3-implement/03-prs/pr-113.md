---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-113 — plan_dag auto-emit hook

**Depends-on**: pr-110 (DAG tool)
**Blocks**: (none)
**Wave**: W2  ·  **Reversibility**: reversible (extension of an existing program)
**Domain**: code-dev
**dev-mode required**: no
**Risk**: low
**Status**: pending

## Goal

Statement:       Extend `workspace/programs/code-dev-plan.md`: when the user finalises a plan, automatically run `dag bootstrap plan` and populate the DAG with each PR. Resolves D-2.

Acceptance:      finalising a fixture plan triggers `dag bootstrap plan`; resulting `phases/{phase}/03-prs/DAG.md` has one node per PR + all declared depends-on edges; idempotent if rerun.

Rejection:       plan finalisation no longer works; DAG emit duplicates nodes on rerun; existing DAG.md is overwritten without backup.

Linked-finding:  F-008 (DAG drift — manual maintenance)
Linked-demand:   D-2 (auto-DAG on plan)
Linked-ADR:      D-021 (additive program extension)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/programs/code-dev-plan.md` | mod | +plan-finalize → TOOL(dag, bootstrap, ...) step |
| `tests/test_plan_dag_hook.py` | new | fixture plan + emit + idempotency |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
git checkout workspace/programs/code-dev-plan.md && rm tests/test_plan_dag_hook.py
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-113 status → complete on merge

## Notes

Low risk. The hook is opt-out: if dag bootstrap fails, plan still finalises and the failure surfaces as a WARN log entry.
