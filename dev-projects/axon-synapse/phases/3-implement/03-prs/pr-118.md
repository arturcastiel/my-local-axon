---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-118 — reference workflows ship

**Depends-on**: pr-105 (workflow format), pr-108 (domain scaffold)
**Blocks**: (none)
**Wave**: W5  ·  **Reversibility**: reversible (new files only)
**Domain**: code-dev / library-dev
**dev-mode required**: no
**Risk**: low
**Status**: pending

## Goal

Statement:       Ship 5 reference workflows under the existing domain scaffold: 3 code-dev (canonical, python-, cpp-), 1 library-dev (canonical), 1 cross-domain adaptive-free-text.

Acceptance:      all 5 files validate via workflow-validate (from pr-115); workflow-simulate runs each end-to-end on a fixture project; pr-108 _index.md placeholders replaced with real listings.

Rejection:       any workflow fails validation; simulator hangs; domain scaffold drift.

Linked-finding:  F-020 (no reference workflows)
Linked-demand:   D-9 (workflow authoring UX), D-26 (second-domain proof)
Linked-ADR:      D-029 (canonical reference workflows)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/domains/code-dev/workflows/code-dev.canonical.yml` | new | canonical code-dev workflow |
| `workspace/domains/code-dev/workflows/python-code-dev.yml` | new | python-specific overlay |
| `workspace/domains/code-dev/workflows/cpp-code-dev.yml` | new | cpp-specific overlay |
| `workspace/domains/library-dev/workflows/library-dev.canonical.yml` | new | canonical library-dev workflow |
| `workspace/workflows/adaptive-free-text.yml` | new | cross-domain free-text adaptive workflow |
| `workspace/domains/code-dev/workflows/_index.md` | mod | replace placeholder; list 3 workflows |
| `workspace/domains/library-dev/workflows/_index.md` | mod | replace placeholder; list 1 workflow |
| `tests/test_reference_workflows.py` | new | validate-each + simulate-each |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
rm workspace/domains/*/workflows/*.yml workspace/workflows/adaptive-free-text.yml tests/test_reference_workflows.py && git checkout workspace/domains/*/workflows/_index.md
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-118 status → complete on merge

## Notes

Pure-additive; second-domain proof rides on library-dev.canonical landing here.
