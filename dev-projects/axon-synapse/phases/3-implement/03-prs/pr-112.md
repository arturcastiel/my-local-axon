---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-112 — output-layer suggestions section [dev-mode]

**Depends-on**: pr-109 (synapse-suggest), pr-111 (orchestrator loop)
**Blocks**: (none)
**Wave**: W4  ·  **Reversibility**: reversible (single OS file mutation)
**Domain**: system
**dev-mode required**: yes — kernel file write requires L:dev-mode ≡ true (Rule R9)
**Risk**: medium
**Status**: ready-for-review

## Goal

Statement:       Extend `axon/OUTPUT-LAYER.md` with the footer suggestion block defined in `orchestrator-composition-v1`. Gated at runtime by `L:suggestions-enabled` (default true). dev-mode flipped on for this PR only and back off on completion.

Acceptance:      footer block appears when `L:suggestions-enabled ≡ true`; absent when false; smoke render through the menu program shows three suggestions on a fixture session.

Rejection:       footer block escapes dev-mode flip; toggle key has wrong default; menu render regresses.

Linked-finding:  F-009 (no orchestrator surface), F-019 (no suggestion footer)
Linked-demand:   D-7 (adaptive orchestrator), D-22 (user control over suggestions)
Linked-ADR:      D-007, D-027 (output-layer ownership)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `axon/OUTPUT-LAYER.md` | mod | +suggestion footer block (kernel; dev-mode write) |
| `workspace/programs/menu.md` | mod | consume L:suggestions-enabled toggle (workspace edit) |
| `tests/test_output_layer_suggestions.py` | new | toggle + render assertions |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
git checkout axon/OUTPUT-LAYER.md workspace/programs/menu.md && rm tests/test_output_layer_suggestions.py
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-112 status → complete on merge

## Notes

**The ONLY dev-mode PR in the migration plan.** Per phase _meta: dev-mode required for pr-112 ONLY. Set `L:dev-mode = true` immediately before, flip back to `false` immediately after merge.
