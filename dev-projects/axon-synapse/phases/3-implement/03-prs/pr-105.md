---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-105 — Workflow file v1 spec → workspace docs (+ JSON-schema)

**Depends-on**: pr-101 (glossary), pr-102 (predicate), pr-103 (goal), pr-104 (neuron contract)
**Blocks**: pr-111 (orchestrator), pr-115 (workflow-new author), pr-130/131/132 (reference workflows)
**Wave**: W1  ·  **Reversibility**: reversible
**Domain**: system
**dev-mode required**: no
**Status**: pending

## Goal

Statement:       Promote `phases/2-design/specs/workflow-file-v1.md` to a
                 workspace-level doc at `workspace/WORKFLOW-FILE.md`, and
                 ship a JSON-schema at
                 `workspace/schemas/workflow-file.schema.json` that
                 validates workflow files in both Fixed and Adaptive modes.

Acceptance:      `file.exists('workspace/WORKFLOW-FILE.md') AND
                  file.contains('workspace/WORKFLOW-FILE.md', 'glossary: AXON-GLOSSARY v2') AND
                  file.exists('workspace/schemas/workflow-file.schema.json') AND
                  json.load(...).$schema starts_with 'http://json-schema.org' AND
                  two fixture workflow files at
                    tests/workflow/fixtures/code-dev-pr-merge.yml AND
                    tests/workflow/fixtures/library-dev-ingest.yml
                  validate green AND
                  one negative fixture validates RED.`

Rejection:       JSON-schema malformed OR any fixture mis-validates OR
                 spec content differs from phase-2 source beyond the
                 provenance header.

Linked-finding:  F-006 (workflow-completion programs already exist), F-013
                 (programs are parameterized synapses)
Linked-demand:   D-9 (workflow acceptance), D-14 (Fixed/Adaptive/Hybrid),
                 D-26 (workflow OS)
Linked-ADR:      D-009 (workflow-modes), D-018 (glossary-singular)

## Blast radius (I-05)

Affected paths:
  - `workspace/WORKFLOW-FILE.md`                  (new)
  - `workspace/schemas/workflow-file.schema.json` (new)
  - `tests/workflow/fixtures/*.yml`               (new — 2 positive + 1 negative)
  - `tests/test_workflow_schema.py`               (new — runner)

Max-rows-changed:  ~500 lines (spec ≈ 250, schema ≈ 150, fixtures ≈ 80, tests ≈ 30)
Touches kernel:    no
Touches shared:    no
Touches existing tests:  no

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/WORKFLOW-FILE.md`                  | new | promoted spec + provenance header |
| `workspace/schemas/workflow-file.schema.json` | new | draft-07 JSON-schema |
| `tests/workflow/fixtures/code-dev-pr-merge.yml`     | new | Fixed-mode reference |
| `tests/workflow/fixtures/library-dev-ingest.yml`    | new | Adaptive-mode reference |
| `tests/workflow/fixtures/invalid-missing-goal.yml`  | new | negative fixture |
| `tests/test_workflow_schema.py`               | new | jsonschema validate runner |

## Implementation outline

1. Copy `phases/2-design/specs/workflow-file-v1.md` → `workspace/WORKFLOW-FILE.md`.
2. Prepend provenance header (pr-101 pattern); fix any inline `SYNAPSE-`
   glossary refs to `AXON-GLOSSARY v2`.
3. Translate the spec's schema section into draft-07 JSON-schema.
4. Author two positive fixtures, one negative; runner uses `jsonschema`
   (already a dev dep — verify) to validate.

## Tests

```
python3 -m pytest tests/test_workflow_schema.py -q
test -f workspace/WORKFLOW-FILE.md && \
  grep -q 'glossary: AXON-GLOSSARY v2' workspace/WORKFLOW-FILE.md
```

## Rollback

```
rm workspace/WORKFLOW-FILE.md workspace/schemas/workflow-file.schema.json
rm -rf tests/workflow/
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: no
- DAG mutation:             set pr-105 status → complete on merge
- Shadow obligation:        no (docs + fixtures, no source-artifact-glob match)

## Notes

PR-105 is docs-only — the runtime that consumes workflow files lands in
pr-111 (orchestrator) and pr-115 (workflow-new). Shipping the schema
first lets pr-130..132 (reference workflows) be authored in parallel.
