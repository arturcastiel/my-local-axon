---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-101 — AXON-GLOSSARY promoted to workspace docs

**Depends-on**: (none — DAG source node)
**Blocks**: pr-102, pr-104, pr-106, pr-110 (Group 1)
**Wave**: W1  ·  **Reversibility**: reversible
**Domain**: meta
**dev-mode required**: no
**Status**: merged
**Merged**: 2026-05-17
**Merge-verified**: acceptance criteria ✓ — file exists, contains "Version: v2" and "neuron", front-matter present, content matches `phases/2-design/specs/SYNAPSE-GLOSSARY.md` (only 3-line promotion header delta).

## Goal

Statement:       Promote `phases/2-design/specs/SYNAPSE-GLOSSARY.md` (v2)
                 to a stable workspace-level docs file at
                 `workspace/AXON-GLOSSARY.md`, so every downstream PR
                 can cite it with `glossary: AXON-GLOSSARY v2`.

Acceptance:      `file.exists('workspace/AXON-GLOSSARY.md') AND
                  file.contains('workspace/AXON-GLOSSARY.md', 'Version: v2') AND
                  file.contains('workspace/AXON-GLOSSARY.md', 'neuron')`

Rejection:       file write fails OR content differs from
                 `phases/2-design/specs/SYNAPSE-GLOSSARY.md`.

Linked-finding:  F-008 (kernel must be domain-agnostic — glossary
                 makes vocabulary explicit)
Linked-demand:   D-26 (workflow OS), D-15 (most detailed),
                 D-6 (synapse contract — depends on vocabulary lock)
Linked-ADR:      D-018 (glossary-singular), D-026 (biology-correct rename)

## Blast radius (I-05)

Affected paths:
  - `workspace/AXON-GLOSSARY.md`   (new file)

Max-rows-changed:  ~350 lines (single new file)
Touches kernel:    no
Touches shared:    no
Touches existing tests:  no

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/AXON-GLOSSARY.md` | new | copy of `phases/2-design/specs/SYNAPSE-GLOSSARY.md` |

## Implementation outline

1. Copy `phases/2-design/specs/SYNAPSE-GLOSSARY.md` → `workspace/AXON-GLOSSARY.md`.
2. Add a one-line front-matter to the workspace copy:
   ```
   <!-- Promoted from axon-synapse phase 2 specs. Authoritative version. -->
   <!-- Edits go through the spec first; this is a mirror. -->
   ```
3. Verify file exists + is readable + contains key terms.

That's the entire PR. Pure file copy. No code paths affected.

## Tests

Existing test suite: must remain green — N/A (no test references this file yet).

New tests: none required (file-existence check is the acceptance criterion).

Test command (HUMAN runs):
```
test -f workspace/AXON-GLOSSARY.md && \
  grep -q 'Version: v2' workspace/AXON-GLOSSARY.md && \
  grep -q 'neuron' workspace/AXON-GLOSSARY.md && \
  echo OK
```

## Rollback (mandatory per I-04)

Revert command:
```
rm workspace/AXON-GLOSSARY.md
```

Check command:
```
test ! -f workspace/AXON-GLOSSARY.md && echo "rollback OK"
```

Partial-failure decision tree:
- If file write fails → no rollback needed (file doesn't exist).
- If content differs from spec → `cp` again to refresh.
- If downstream PR depended on this PR → those PRs already reference
  `phases/2-design/specs/` as fallback; no blocker.

## Audit-trail

- Created by:    axon-synapse Phase 3 design
- Reviewer(s):   arturcastiel
- Synapse-contract updated: no (this PR predates contract migration)
- DAG mutation:  set pr-101 status → complete on merge
- Shadow obligation: no (meta-domain doc; no source-artifact-glob match)

## Notes

This PR is intentionally tiny. It's the entry-node of the Phase-3 DAG
and serves as a smoke test that the per-PR rhythm works (spec → review
→ implement → verify → shadow → track → merge). Subsequent PRs add
substantive code.

After merge:
- pr-102 (predicate tool), pr-104 (neuron-contract schema), pr-106
  (domain manifest), pr-110 (DAG tool) become unblocked.
- The "Group 1" parallel work can begin.
