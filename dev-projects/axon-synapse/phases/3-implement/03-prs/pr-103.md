---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-103 — goal tool + goal-schema v1

**Depends-on**: pr-101 (glossary), pr-102 (predicate tool)
**Blocks**: pr-119 (axon-audit extension)
**Wave**: W2  ·  **Reversibility**: reversible (new tool + new template)
**Domain**: system
**dev-mode required**: no
**Risk**: medium
**Status**: pending

## Goal

Statement:       Ship `tools/goal.py` (set/get/confirm/list/met/audit) backed by goal-schema-v1. `goal audit` traverses project / phase / workflow / step / PR / finding / demand levels and surfaces unmet goals with reasons.

Acceptance:      `tools/goal.py` exists; subcommands set, get, confirm, list, met, audit each return exit 0 on smoke fixtures; `workspace/templates/goal-v1.yml` validates against schema; `goal audit` over `my-axon/dev-projects/axon-synapse/` produces a non-empty report.

Rejection:       any subcommand crashes on fixture input; audit-traversal misses a level documented in goal-schema-v1.

Linked-finding:  F-001 (goal layer absent)
Linked-demand:   D-3 (DAG mutation hooks), D-4 (auditability), D-12 (declarative goals)
Linked-ADR:      D-013 (goal-schema split), D-021 (predicate-grammar reuse)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `tools/goal.py` | new | argparse CLI w/ 6 subcommands (~400 LOC) |
| `workspace/tools/goal.md` | new | usage doc (D-034 workspace overrides OS) |
| `workspace/templates/goal-v1.yml` | new | blank goal record template |
| `tools/REGISTRY.json` | mod | register `goal` (status ACTIVE) |
| `tests/test_goal.py` | new | subcommand smoke + audit fixture |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
rm tools/goal.py workspace/tools/goal.md workspace/templates/goal-v1.yml tests/test_goal.py && git checkout tools/REGISTRY.json
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-103 status → complete on merge

## Notes

Schema lives in `workspace/templates/goal-v1.yml` per D-013 (declarative). `goal audit` consumes the predicate tool from pr-102.
