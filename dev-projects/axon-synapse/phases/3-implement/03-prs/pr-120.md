---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-120 — igap + auto-improve wire to synapse-suggest

**Depends-on**: pr-109 (synapse-suggest)
**Blocks**: (none)
**Wave**: W6  ·  **Reversibility**: reversible (additive — new signal source + ranker tap)
**Domain**: system
**dev-mode required**: no
**Risk**: low
**Status**: pending

## Goal

Statement:       Extend `tools/igap.py` so that recorded inference gaps become a signal source for the synapse-suggest ranker. Extend `workspace/programs/auto-improve.md` so that auto-improve re-ranks on new igap entries.

Acceptance:      adding a new igap entry shifts at least one suggestion's confidence within ±0.05 in a fixture session; ranker exposes igap as a named signal source in `--explain` output.

Rejection:       ranker ignores igap entries; auto-improve doesn't re-rank; --explain output regresses.

Linked-finding:  F-022 (igap not wired to ranker)
Linked-demand:   D-14 (signal-rich ranker)
Linked-ADR:      D-019 (composition over codegen)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `tools/igap.py` | mod | +ranker-signal exporter (~40 LOC) |
| `workspace/programs/auto-improve.md` | mod | +re-rank trigger on new igap |
| `tests/test_igap_signal.py` | new | fixture confidence-shift |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
git checkout tools/igap.py workspace/programs/auto-improve.md && rm tests/test_igap_signal.py
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-120 status → complete on merge

## Notes

The last polish PR in the migration plan. Closes D-14.
