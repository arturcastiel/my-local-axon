---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-117 — alias canonicalization + finalize stub + self-review collision

**Depends-on**: pr-108 (metadata migration)
**Blocks**: (none — closes critical path)
**Wave**: W5  ·  **Reversibility**: reversible (declarative alias edits + new finalize program)
**Domain**: code-dev
**dev-mode required**: no
**Risk**: medium
**Status**: pending

## Goal

Statement:       Resolve the three legacy collisions in code-dev: (a) preserve code-dev-audit, code-dev-pr, code-dev-shadow permanently as ALIASes; (b) implement code-dev-finalize per axon-cleanup PR-119 (was a STUB); (c) pick one of code-dev-self-review / code-dev-review-self as canonical, other → ALIAS.

Acceptance:      `synapse-validate --all-corpus` rate ≥ 95 % (post-canonicalization); all ALIASes carry a `canonical:` line; finalize program responds to `axon.py code-dev-finalize`; no F-012 / F-007 entries remain unresolved in 04-log.md.

Rejection:       any program loses an invocation path; ALIAS without canonical; finalize regresses an existing test.

Linked-finding:  F-012 (alias ambiguity), F-007 (finalize STUB)
Linked-demand:   D-8 (no orphan programs), D-23 (canonical mapping)
Linked-ADR:      D-021 (additive), D-026 (neuron rename canonicalization)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/programs/code-dev-audit.md` | mod | + canonical: code-dev-knowledge-shadow; status: ALIAS |
| `workspace/programs/code-dev-pr.md` | mod | + canonical: code-dev-pr-create |
| `workspace/programs/code-dev-shadow.md` | mod | + canonical: code-dev-knowledge-shadow |
| `workspace/programs/code-dev-finalize.md` | new | implementation per axon-cleanup PR-119 |
| `workspace/programs/code-dev-self-review.md` | mod | becomes canonical (or ALIAS — choose at impl) |
| `workspace/programs/code-dev-review-self.md` | mod | becomes ALIAS of code-dev-self-review (or reverse) |
| `tests/test_alias_canonicalization.py` | new | asserts every ALIAS has canonical: + canonical exists |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
git checkout HEAD^ -- workspace/programs/code-dev-*.md && rm workspace/programs/code-dev-finalize.md tests/test_alias_canonicalization.py
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-117 status → complete on merge

## Notes

**Critical-path terminus (hop 5/5).** After this PR the synapse migration's core chain is complete; remaining PRs are parallel polish (pr-118 workflows, pr-119 audit, pr-120 igap wire).
