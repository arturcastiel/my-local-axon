---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-119 — axon-audit extension

**Depends-on**: pr-103 (goal tool), pr-107 (synapse-validate), pr-114 (shadow gates)
**Blocks**: (none)
**Wave**: W6  ·  **Reversibility**: reversible (single-program extension)
**Domain**: system
**dev-mode required**: no
**Risk**: low
**Status**: ready-for-review

## Goal

Statement:       Extend `workspace/programs/axon-audit.md` with three new audit rows: synapse-contract coverage (calls synapse-validate --all-corpus), shadow coverage (calls shadow stats), demand-audit (calls goal audit --level demand).

Acceptance:      axon-audit output gains 3 rows; each row reports pass/warn/fail with an evidence link; running audit takes < 5 s on this repo.

Rejection:       any row crashes the auditor; per-row timeout exceeded; existing audit rows regress.

Linked-finding:  F-021 (audit gaps)
Linked-demand:   D-11 (continuous audit)
Linked-ADR:      D-019 (audit composition)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/programs/axon-audit.md` | mod | +3 audit-row blocks |
| `tests/test_axon_audit_synapse.py` | new | smoke each new row |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
git checkout workspace/programs/axon-audit.md && rm tests/test_axon_audit_synapse.py
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-119 status → complete on merge

## Notes

Each new row is a `TOOL(...)` call into an existing tool — no new logic, only composition.
