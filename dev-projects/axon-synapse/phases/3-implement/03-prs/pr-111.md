---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-111 — orchestrator loop (program)

**Depends-on**: pr-109 (synapse-suggest), pr-110 (DAG tool)
**Blocks**: (none — closes the main orchestration path)
**Wave**: W3  ·  **Reversibility**: reversible (single new program file)
**Domain**: system
**dev-mode required**: no
**Risk**: high
**Status**: ready-for-review

## Goal

Statement:       Ship `workspace/programs/orchestrator.md` — the mainline loop from `orchestrator-composition-v1.md`. After a neuron fires it queries `synapse-suggest`, walks the DAG, and renders the next-action set. Both fixed-workflow and adaptive-free-text modes covered.

Acceptance:      5 fixture sessions under `tests/synapse/sessions/` replay through the loop and produce the expected next-action set ± confidence; both modes (fixed/adaptive) covered.

Rejection:       any fixture session diverges from its expected trace; loop hangs or recurses without progress on any fixture.

Linked-finding:  F-009 (no mainline orchestrator), F-013 (parameterized synapses)
Linked-demand:   D-7 (adaptive orchestrator), D-18 (loop termination guarantees)
Linked-ADR:      D-007 (compose-not-codegen), D-019 (fixed+adaptive duality)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/programs/orchestrator.md` | new | mainline loop, ~300 lines AXON-LANG |
| `tests/synapse/sessions/*.session.json` | new | 5 fixture session traces |
| `tests/test_orchestrator_loop.py` | new | replay harness |

## Implementation outline

See § Files changed. Each new/modified file lands per the file-level
note; the implementation order is top-to-bottom in the table.

## Tests

`python3 -m pytest tests/ -q` must stay green. New tests listed under
§ Files changed are added by this PR and must pass.

## Rollback

```
rm workspace/programs/orchestrator.md tests/test_orchestrator_loop.py && rm -rf tests/synapse/sessions/
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes (downstream contracts may regen)
- DAG mutation:             set pr-111 status → complete on merge

## Notes

High-risk because it introduces a new mainline. Defensive: fixture sessions exercise both modes and termination corners. Output-layer suggestion footer is **not** rendered here — that's pr-112.
