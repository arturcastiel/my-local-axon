# Project profile — flowsim-vectorize

linter:        (set per project: MATLAB check_code / mlint or equivalent)
changelog:     CHANGELOG.md
reviewers:     []   # GitHub handles
cross-repo:    []   # sibling repo paths
test-cmd:      (set per project — e.g. run a small mesh benchmark)
build-cmd:     (set per project — HUMAN runs, agent never executes)
