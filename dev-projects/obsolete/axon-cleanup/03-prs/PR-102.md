# PR-102: tools/test.py always emits valid JSON

**Wave:** 0 — CI unblock
**Status:** SHIPPED
**Depends on:** PR-101

## Goal

On a synthetic tmp_path with no `workspace/programs/`, `tools/test.py` exits non-zero with empty stdout. Callers do `json.loads(stdout)` and crash. Tool must always emit valid JSON, even for error/empty cases.

## Touch

- `tools/test.py` — wrap `main()` so every exit path prints a JSON object (e.g. `{"valid": false, "issues": [...], "file": null}`). Exit code stays meaningful but stdout is always parseable.

## Tests (mandatory per axon-tests PR-020)

- New `tests/test_tools_test_json_shape.py` (3 cases): empty workspace, missing file, valid file. Each asserts `json.loads(stdout)` succeeds.
- Existing `tests/test_budget_lint.py` failures clear automatically.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-COMPILER.md § tools/test.py` (or wherever the tool is documented) and the corresponding `## Guarded by` table.

## Notes / risks

Behavioural contract change. Verify no caller does `if proc.stdout:` truthy-check that would flip semantics.
