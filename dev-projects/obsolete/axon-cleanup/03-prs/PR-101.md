# PR-101: Drop --maxfail=1 from tests-full CI job

**Wave:** 0 — CI unblock
**Status:** SHIPPED
**Depends on:** none

## Goal

CI must run the whole pytest suite so all failures are visible in one pass and the coverage gate executes. Today the first failure aborts and coverage never runs.

## Touch

- `.github/workflows/ci.yml` — remove `--maxfail=1` from the `tests-full` invocation. Replace with `--maxfail=0` (or omit). Keep `--durations=20`.

## Tests (mandatory per axon-tests PR-020)

- Extend `tests/test_coverage_config.py` (axon-tests) with a case asserting `--maxfail=1` is absent from the workflow YAML.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-TESTING.md § Coverage` to note the gate runs end-of-suite, not first-fail.

## Notes / risks

Trivial; smallest PR in the wave. Lands first — every downstream wave depends on a CI that runs to completion.
