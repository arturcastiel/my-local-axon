# PR-132: Drop unused top-level packages

**Wave:** 2 — Deps cleanup
**Status:** SHIPPED
**Depends on:** PR-131

## Goal

After PR-131 establishes the curated list, prune everything that has no direct import. Spot-checked candidates with zero imports: rich, typer, pydantic*, click, shellingham, markdown-it-py, Jinja2, MarkupSafe, Pygments, uvicorn, uvloop, httptools, httpx, httpcore, h11, watchfiles, websockets, websocket-client, anyio. Plus the chromadb cascade (~70 packages auto-removed transitively).

## Touch

- `pyproject.toml` only — narrow the `dependencies` list to ~10 packages.
- Verify each dropped package is actually unused by `grep -r "^import {pkg}\|^from {pkg}"`.

## Tests (mandatory per axon-tests PR-020)

- `tests/test_requirements_intent.py` (PR-133) enforces this going forward.
- Existing pytest must still pass after pip install with the new list.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-GOVERNANCE.md § Dependencies` with a one-table summary of what stays and why. Guarded-by row added.

## Notes / risks

If a package is unexpectedly required at runtime (e.g. pulled by a tool we missed), CI will catch it. Keep the previous `requirements.txt` content saved in `02-deviations.md` so we can resurrect specific packages if needed.
