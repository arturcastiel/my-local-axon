# PR-105: Fix test_session::test_recover_active_session

**Wave:** 0 — CI unblock
**Status:** SHIPPED
**Depends on:** PR-101

## Goal

`recover` returns a tuple/dict where `is_active` is True when test expects False (or vice versa). Either contract drift in `tools/session.py` or stale test expectation.

## Touch

- Investigate `tools/session.py:recover()`. Patch the wrong side.

## Tests (mandatory per axon-tests PR-020)

- Update or extend the existing test as needed. If the tool changed legitimately, add 1–2 cases pinning the new shape.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-SESSIONS.md § session lifecycle` if behaviour changed and add Guarded-by row.

## Notes / risks

Read the git history of `tools/session.py` to identify intended behaviour.
