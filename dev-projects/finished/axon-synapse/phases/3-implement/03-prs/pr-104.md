---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-104 — Neuron-contract schema → workspace docs

**Depends-on**: pr-101 (glossary)
**Blocks**: pr-105 (workflow), pr-107 (synapse-infer), pr-109 (synapse-suggest), pr-114 (shadow gates)
**Wave**: W1  ·  **Reversibility**: reversible
**Domain**: system
**dev-mode required**: no
**Status**: merged
**Merged**: 2026-05-18
**Merge-verified**: acceptance criteria ✓ — `workspace/NEURON-CONTRACT.md` exists (297 lines, 11037 bytes) with `glossary: AXON-GLOSSARY v2` at line 13; `tools/REGISTRY.json` parses; `schema_version: "v1.1"`; `contract_version: "neuron-contract v1.1"`; tool count unchanged (75).

## Goal

Statement:       Promote `phases/2-design/specs/synapse-contract-v1.md`
                 (renamed v1.1 per D-026) to a stable workspace-level
                 schema doc at `workspace/NEURON-CONTRACT.md`, and bump
                 `tools/REGISTRY.json` schema to accept the new optional
                 fields (`synapse:` block per neuron entry) without
                 breaking existing parsers.

Acceptance:      `file.exists('workspace/NEURON-CONTRACT.md') AND
                  file.contains('workspace/NEURON-CONTRACT.md', 'glossary: AXON-GLOSSARY v2') AND
                  file.contains('tools/REGISTRY.json', '"schema_version"') AND
                  json.load('tools/REGISTRY.json') succeeds AND
                  all 75 existing tool entries still parse-validate`

Rejection:       REGISTRY.json parse breaks OR any existing tool entry
                 fails its current schema check.

Linked-finding:  F-005 (no synapse-contract today — BLOCKER), F-013
                 (programs are parameterized synapses)
Linked-demand:   D-6 (synapse contract), D-20 (auto-discoverable tools),
                 D-27 (register new synapses)
Linked-ADR:      D-005 (hybrid inferred+declared), D-020 (infer-first),
                 D-026 (neuron rename), D-032 (source-artifact-glob)

## Blast radius (I-05)

Affected paths:
  - `workspace/NEURON-CONTRACT.md`   (new file)
  - `tools/REGISTRY.json`            (schema-version field bumped; existing entries unchanged)

Max-rows-changed:  ~330 lines (new file) + 2 lines (REGISTRY metadata bump)
Touches kernel:    no
Touches shared:    no
Touches existing tests:  no (REGISTRY parsers are tolerant of new top-level fields)

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/NEURON-CONTRACT.md`     | new | promoted spec; renamed from "Synapse Contract" per D-026 |
| `tools/REGISTRY.json`              | mod | bump `schema_version` to `"v1.1"`; preserve all `tools[*]` entries verbatim |

## Implementation outline

1. Copy `phases/2-design/specs/synapse-contract-v1.md` →
   `workspace/NEURON-CONTRACT.md`.
2. Prepend the same 2-line provenance header used in pr-101.
3. Read `tools/REGISTRY.json` to confirm current `schema_version` field
   (currently absent OR present; both safe).
4. Update `tools/REGISTRY.json` minimal-diff:
   - Add or bump `"schema_version": "v1.1"` at the top.
   - Add `"contract_version": "neuron-contract v1.1"` field (informational).
   - Do NOT modify any individual `tools[*]` entry.
5. Run verify: REGISTRY.json still loads as JSON; tool count unchanged
   (75 entries).
6. Bonus (low risk): re-render `workspace/tools/REGISTRY.md` from
   REGISTRY.json. Skipped this PR — separate housekeeping.

## Tests

Existing test suite: any test that loads REGISTRY.json must still pass.

Verify command (HUMAN runs; safe — JSON load + file existence):
```
test -f workspace/NEURON-CONTRACT.md && \
  grep -q 'glossary: AXON-GLOSSARY v2' workspace/NEURON-CONTRACT.md && \
  python3 -c "import json; r=json.load(open('tools/REGISTRY.json')); \
    assert r.get('schema_version')=='v1.1', 'schema_version not bumped'; \
    assert len(r['tools'])==75, f'tool count drift: {len(r[\"tools\"])}'; \
    print('OK', len(r['tools']), 'tools, schema', r['schema_version'])"
```

## Rollback (mandatory per I-04)

Revert command:
```
rm workspace/NEURON-CONTRACT.md
git checkout tools/REGISTRY.json   # if committed
# OR: edit REGISTRY.json to remove the added schema_version + contract_version lines
```

Check command:
```
test ! -f workspace/NEURON-CONTRACT.md && \
  python3 -c "import json; r=json.load(open('tools/REGISTRY.json')); \
    assert len(r['tools'])==75" && \
  echo "rollback OK"
```

Partial-failure decision tree:
- If REGISTRY.json fails to parse after edit → restore from git;
  redo with minimal diff.
- If schema_version conflicts with existing key → choose `v1.1`
  (canonical post-D-027) over any prior value.
- If a downstream PR has consumed the new schema → revert the
  downstream PR first; this PR cannot be reverted alone once
  pr-107/pr-109 ship.

## Audit-trail

- Created by:    axon-synapse Phase 3 design (auto-piloted)
- Reviewer(s):   arturcastiel
- Synapse-contract updated: this PR IS the contract; not self-referential
- DAG mutation:  set pr-104 status → complete on merge
- Shadow obligation: no (meta/system docs; no source-artifact-glob match)

## Notes

This PR is the BLOCKER referenced in F-005. After it lands, the synapse
contract schema is workspace-stable and four downstream PRs (pr-105,
pr-107, pr-109, pr-114) can begin in parallel.

Two future considerations (NOT in this PR):
1. `synapse-infer` (pr-107) will read the schema declared here to know
   what fields to emit. The schema doc must be precise.
2. The `synapse:` block on individual tool entries (per F-013 / D-005
   hybrid model) is OPTIONAL in this PR. Bulk population happens in
   pr-108 (metadata migrate).

Why no test fixture: REGISTRY.json's structural integrity is asserted by
the verify command (JSON parse + tool count). Deeper schema validation
arrives in pr-107 (`synapse-validate` tool).
