---
glossary:      AXON-GLOSSARY v2
audience:      tier-A
version:       1
---

# PR-106 — Domain manifest v1 + reference manifests (code-dev, library-dev)

**Depends-on**: pr-101 (glossary)
**Blocks**: pr-107 (synapse-infer reads manifests), pr-108 (metadata migrate)
**Wave**: W1  ·  **Reversibility**: reversible
**Domain**: system
**dev-mode required**: no
**Status**: merged
**Merged**: 2026-05-18
**Merge-verified**: acceptance criteria ✓ — `workspace/DOMAIN-MANIFEST.md` exists (296 lines, glossary v2 at line 6); `workspace/domains/{code-dev,library-dev}/manifest.md` both declare all required fields; `python3 axon.py domain_validate --all` exits 0 with `{ok: true, valid: 2/2}`; reject path verified (exit 1 + missing-field errors); REGISTRY count 75 → 76 with `domain_validate` registered ACTIVE.

## Goal

Statement:       Promote `phases/2-design/specs/domain-manifest-v1.md` to
                 `workspace/DOMAIN-MANIFEST.md`, and ship the two reference
                 manifests at `workspace/domains/code-dev/manifest.md` and
                 `workspace/domains/library-dev/manifest.md`, with a tiny
                 validator at `tools/domain_validate.py` that checks each
                 manifest against the spec's required-fields list.

Acceptance:      `file.exists('workspace/DOMAIN-MANIFEST.md') AND
                  file.contains(..., 'glossary: AXON-GLOSSARY v2') AND
                  file.exists('workspace/domains/code-dev/manifest.md') AND
                  file.exists('workspace/domains/library-dev/manifest.md') AND
                  python3 axon.py domain_validate --all → exit 0 AND
                  both manifests declare: name, version, workflows,
                  verb-map, default-goals.`

Rejection:       Any manifest missing a required field OR validator crash
                 OR domain folder layout breaks an existing program path.

Linked-finding:  F-008 (kernel domain-agnostic), F-011 (library-dev already
                 proves multi-domain DNA)
Linked-demand:   D-15 (most-detailed), D-26 (workflow OS), D-27 (register
                 new synapses)
Linked-ADR:      D-014 (code-dev preserved), D-025 (no rename), D-031
                 (manifest schema)

## Blast radius (I-05)

Affected paths:
  - `workspace/DOMAIN-MANIFEST.md`                 (new)
  - `workspace/domains/code-dev/manifest.md`       (new)
  - `workspace/domains/library-dev/manifest.md`    (new)
  - `tools/domain_validate.py`                     (new)
  - `tools/REGISTRY.json`                          (mod — add `domain_validate`)
  - `tests/test_domain_manifest.py`                (new)

Max-rows-changed:  ~450 lines
Touches kernel:    no
Touches shared:    no
Touches existing tests:  no

## Files changed

| Path | Kind | Note |
|------|------|------|
| `workspace/DOMAIN-MANIFEST.md`              | new | promoted spec |
| `workspace/domains/code-dev/manifest.md`    | new | reference manifest |
| `workspace/domains/library-dev/manifest.md` | new | reference manifest |
| `tools/domain_validate.py`                  | new | required-field checker |
| `tools/REGISTRY.json`                       | mod | add `domain_validate` entry |
| `tests/test_domain_manifest.py`             | new | runs validator + spot-checks |

## Implementation outline

1. Promote spec → `workspace/DOMAIN-MANIFEST.md` with provenance header.
2. Author code-dev manifest: name=code-dev, version=v1, workflows=[study,
   plan, pr-author, pr-review, finalize, audit], verb-map per spec,
   default-goals: project-level acceptance template.
3. Author library-dev manifest: name=library-dev, version=v1, workflows=
   [ingest, explain, intersect, report, search, cite], verb-map per spec.
4. `domain_validate.py`: walks `workspace/domains/*/manifest.md`, parses
   YAML/markdown front-matter + body sections, asserts required fields,
   emits JSON `{ok, errors[]}`.
5. Register tool in REGISTRY; smoke = `domain_validate --all` exit 0.

## Tests

```
python3 axon.py domain_validate --all
python3 -m pytest tests/test_domain_manifest.py -q
```

## Rollback

```
rm -rf workspace/domains/ workspace/DOMAIN-MANIFEST.md \
       tools/domain_validate.py tests/test_domain_manifest.py
git checkout tools/REGISTRY.json
```

## Audit-trail

- Reviewer(s):              arturcastiel
- Synapse-contract updated: yes — `domain_validate` neuron entry added
- DAG mutation:             set pr-106 status → complete on merge
- Shadow obligation:        yes — `tools/domain_validate.py` is source
                            artifact; inferred contract via PR-107 / PR-114

## Notes

Manifests live at `workspace/domains/{name}/manifest.md` so PR-108 can
later bulk-add `domain:` to every existing synapse contract via the
manifest's `verb-map`. No file moves in this PR — only additions.
