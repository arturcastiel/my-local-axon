# PR-015 — for-use sync + cross-checkout parity test
status: in-progress · wave 4 · vector V5 (re-grounded) · depends PR-001
## Finding: for-use checkout is 289 behind & never had tools/hr_team.py. Re-grounded: not "reconcile
## divergence" but "sync + prevent future divergence by TEST". The actual 289-commit git pull on the
## SEPARATE for-use working tree is an OPS/human decision (can conflict) — flagged, not auto-run.
## Change: a parity test (AXON_FORUSE_CHECKOUT-driven, graceful skip until synced) asserting both
## checkouts' hr_team.py are byte-identical — the standing guard against re-divergence.
## Tests: parity test skips honestly without the env; enforces byte-identity once for-use is synced.
## OWNER OPS: sync for-use via `git -C <for-use> pull --ff-only origin main` (resolve conflicts if any),
## then run with AXON_FORUSE_CHECKOUT=<for-use> to activate the parity guard.
## Constraints: no kernel edits · AXON trailer.
