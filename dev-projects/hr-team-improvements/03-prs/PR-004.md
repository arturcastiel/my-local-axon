# PR-004 — Collapse the dual path: main() cannot surface a verdict + seal the import seam

status: in-progress
wave: 1 · Keystone
vector: V9a (hardened)
depends: none
council: 3-seat design council (kernel-architect, app-security, eval-engineer) → ship-with-changes (all 3)

## Goal
Make tools/hr_team.py structurally incapable of producing a §4.3 advisory verdict — via the CLI AND via import —
so the .md neurons are the sole verdict-surfacing runtime (ADR-001).

## Change (council-hardened)
1. main() default → REFUSE: print {"error":"cli_is_fixture","message":"real councils run via the neuron path"} to
   STDERR, exit 2. Stdout empty. No §4.3 verdict ever on stdout.
2. POSITIVELY-GATE verdict-hood: real verdicts carry required discriminator "kind":"advisory_verdict", stamped ONLY
   by an authorized seal (a sentinel/token the fixture path never holds). _REQUIRED_VERDICT_KEYS includes "kind".
3. SEAL THE IMPORT SEAM: _build_verdict (and run_seats' stub branch) refuse to mint a sealed verdict unless the
   authorized token is presented → `from hr_team import _build_verdict` cannot reconstruct a real verdict.
4. --emit-seam-shape (requires BOTH ALLOW_STUB AND the flag) → wrapped non-verdict
   {"fixture":true,"not_a_verdict":true,"seam_shape":{...}} with numeric confidence neutralized; fails verdict-validation
   even when unwrapped. ALLOW_STUB alone still REFUSES (no env backdoor).
5. AUDIT-BUNDLE write moved behind the fixture gate (no verdict-shaped files written from a refusing CLI).
6. REGISTRY: reconcile hr_team.py status with ADR-001 (helper/fixture, not an ACTIVE runtime tool).

## Acceptance / Test (Core Rule 13)
- Re-home verdict/manifest/persistence/M1-M3 assertions to IMPORT-LEVEL (call functions in-process with the seal token).
- NEW negative tests: default stdout=="" + exit 2 + never verdict-parseable; stderr is typed {"error":"cli_is_fixture"};
  ALLOW_STUB-alone still refuses; --emit-seam-shape output (and unwrapped seam_shape) fails _REQUIRED_VERDICT_KEYS;
  import of _build_verdict without token cannot produce a "kind":"advisory_verdict" object.
- Full crucible gate GREEN.

## Constraints
- No kernel edits. AXON-only commit trailer. Non-kernel repo.
