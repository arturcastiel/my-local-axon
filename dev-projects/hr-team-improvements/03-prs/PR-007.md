# PR-007 — Context-load helper + wire the dead --context
status: in-progress · wave 2 · vector V6 · depends PR-005
## Change: tools/hr_team.py load_context() (path-or-text, sanitized, sha256-bound, truncation-safe)
+ a deterministic-helper CLI SURFACE (subcommand dispatch: `hr-team load-context`) so neurons can
TOOL()-call helpers while the bare council path still refuses (ADR-001). Router loads context once
(convener is TOOL-free) → W:hr-team-context-loaded; convener delimits it into each seat user message.
## Tests: load_context literal/file/empty/nonfile-path/truncate/sanitize + subcommand + wiring.
## Constraints: no kernel edits · AXON trailer.
