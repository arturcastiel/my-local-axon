# PR-008 — Deterministic F1..F6 mode resolver
status: in-progress · wave 2 · vector V7 · depends PR-005
## Change: tools/hr_team.py resolve_mode() — expands a named preset OR an a+b+c 6-tuple (any order,
## partial) into the canonical F1..F6 tuple + the on-disk family-fragment paths; validates values,
## fills defaults. Subcommand `hr-team resolve-mode`. Router resolves → W:hr-team-mode-resolved;
## convener READs+JOINs the fragments (the 31 family fragments were unreachable before — dead flat lookup).
## Tests: preset, order-independent tuple+defaults, unknown→invalid, fragments-exist-on-disk, subcommand, wiring.
## Constraints: no kernel edits · AXON trailer.
