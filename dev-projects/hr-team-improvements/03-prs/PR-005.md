# PR-005 — Single invocation contract: router marshals all flags → W: keys
status: in-progress · wave 1 · vector V9b · depends PR-004
## Finding: the router already marshals most flags into W:hr-team-* keys; PR-004 made the program
the single entry (CLI refuses). Remaining: marshal the dead --context into W:hr-team-context (load
helper + consumption = PR-007) and lock the contract with a test.
## Change: hr-team.md marshals W:hr-team-context; contract test asserts the router reads every
documented invocation key (task/domain/roster/mode/context). 
## Constraints: no kernel edits · AXON trailer.
