# PR-016 — End-to-end real-council test via the injectable fake
status: in-progress · wave 4 · vector V3/V8 e2e · depends PR-010,PR-012,PR-013
## Change: tests/test_hr_team_e2e.py — deterministic full-pipeline proof via make_fake_backend (no live
## sub-agents): schema-v2 messages -> run_seats(fake) -> _build_verdict (WSV winner + real dissent
## classification + contested status + metrics) -> seal -> fan-out receipt -> verify_surfaceable(require_receipt).
## Known-input assertions: winner A, challenger SUBSTANTIVE dissent -> contested, variant threaded, provenance verified.
## + the un-provenanced council is fail-closed (a fake council can never reach a human as a real verdict).
## Constraints: no kernel edits · AXON trailer.
