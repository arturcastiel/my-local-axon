# PR-012 — Fan-out receipt provenance (keystone) + .md↔Python conformance
status: in-progress · wave 3 · vector V10b · depends PR-011 · council-promoted to keystone
## Change: tools/hr_team.py — real verify_fanout_receipt(): test_only rejection (production allowlist),
## nonce anti-replay, ts freshness window, PER-SEAT COMPLETENESS (receipt seats == verdict seats, each
## sub_call_id present in the out-of-band ledger — one real seat can't launder fabricated ones).
## make_fanout_receipt() / write_fanout_receipt() / _ledger_sub_call_ids() mirror the r_tool_receipts ledger
## (workspace/log/receipts/<date>.jsonl). verify_surfaceable(require_receipt=True) threads nonce/freshness.
## + the .md↔Python schema conformance test (false-green guard: neuron DERIVE(run_seats) schema == Python == fake).
## HONEST CEILING (council): forgery-RESISTANT/detectable, NOT cryptographically unforgeable — absent a
## dispatcher-held signing key the in-process agent cannot read. Production require_receipt=True activation
## awaits the harness dispatcher as the SOLE signed ledger writer (documented in the code).
## Tests: valid, test_only-rejected, nonce, stale, per-seat-mismatch, absent-from-ledger, surfaceable-e2e, conformance.
## Constraints: no kernel edits · AXON trailer.
