# PR-009 — Roster keyword/domain map + fail-closed gate
status: in-progress · wave 2 · vector V4 · depends PR-005
## Change: tools/hr_team.py match_roster() — deterministic keyword/domain pre-pass over the 151-row
## catalog (stopword-filtered token overlap, domain filter, ranked). Unknown domain → fail_closed (hard);
## weak/empty → fail_closed advisory. Subcommand `hr-team match-roster`. Router runs it (selector is
## TOOL-free) → W:hr-team-roster-candidates; selector is deterministic-first: hard-refuses unknown domain,
## warns on weak match, DERIVE refines WITHIN the matched pool (OD-C: no silent LLM-takeover).
## Tests: catalog parse, keyword hits, unknown-domain fail-closed, domain scoping, empty-weak, subcommand, wiring.
## Constraints: no kernel edits · AXON trailer.
