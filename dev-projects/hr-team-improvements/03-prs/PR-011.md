# PR-011 — run_seats seam v2 + injectable fake (Wave 3, council-recut)
status: in-progress · wave 3 · vector V10a · depends PR-005
council: Wave-3 design council (harness/app-sec/eval) → ship-with-changes; re-cut adopted.
## Architecture (council-confirmed): real fan-out = the convener NEURON's DERIVE-per-seat (agent spawns
## sub-agents). NO python api/mcp backend (would resurrect the dual path). Python = seam contract + test fake + refusal.
## Change: run_seats(messages, *, _backend=None) — param-injected, CONTENT-KEYED fake (not env, not index-stub);
## schema v2 (per-seat variant/effort threaded, tolerant of model_variant); production default unchanged (fail-closed).
## make_fake_backend(scenario) — content-keyed, echoes variant/effort, BAD_JSON sentinel for the retry path.
## Killed the misleading BACKEND api/mcp comment.
## Tests: injected-fake order+contract, schema-v2 threaded, production-default-still-raises, content-keyed+BAD_JSON, no-api-mcp.
## Re-cut for 012/013: PR-012 = receipt provenance KEYSTONE (out-of-band ledger, nonce, per-seat-complete; honest
## ceiling = forgery-resistant not unforgeable) + the .md↔Python schema conformance test (false-green guard).
## PR-013 = substantive-dissent re-round (retry already exists). Constraints: no kernel edits · AXON trailer.
