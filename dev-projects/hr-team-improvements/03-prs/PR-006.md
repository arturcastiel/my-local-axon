# PR-006 — Sole run_seats seam: reconcile the 2nd call-site
status: in-progress · wave 1 · vector V9c · depends PR-004
## Finding: the deliberator retry (L65) re-invokes the seam via DERIVE(run_seats) — the SAME seam
the convener owns, not a divergent path. (A TOOL() call would be wrong: run_seats is agent-cognition
via DERIVE, and hr-team-convener is a program not a tool.) Reconcile = document the sole-seam contract
+ test it. Exactly one seam name (run_seats); all council cognition flows through it.
## Change: deliberator SOLE SEAM CONTRACT comment; contract test asserts DERIVE(run_seats) + the contract.
## Constraints: no kernel edits · AXON trailer.
