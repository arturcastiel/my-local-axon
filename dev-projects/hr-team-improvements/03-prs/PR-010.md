# PR-010 — Deliberation math helpers + measurable 'smarter'
status: in-progress · wave 2 · vector V8 · depends PR-004
## Change: tools/hr_team.py — extract WSV / aggregate_confidence / order_sensitivity (BPC) /
## deliberation_metrics as the TESTED reference implementation (golden vectors). _build_verdict now
## USES them (kills the templated inline math + the fabricated 0.25xx aggregate). Measurable 'smarter'
## metrics: winner_margin (decisiveness), confidence_spread + dissent_rate (disagreement), order_sensitive
## (BPC robustness — meaningful on ties, since WSV summation is order-independent). Subcommand
## `hr-team deliberation-metrics`. Deliberator inline math pinned to these helpers.
## Tests: golden WSV/confidence/order-sensitivity(tie vs clear)/metrics + _build_verdict-uses-them + subcommand.
## Constraints: no kernel edits · AXON trailer.
