# Project log — axon-autoimprove

| Date       | Event                                                                                                |
|------------|------------------------------------------------------------------------------------------------------|
| 2026-05-18 | Project scaffolded. Seeded from `../axon-synapse/AUDIT.md` (R2 recommendation). Phase 1 ACTIVE.       |
| 2026-05-18 | Inherited demands D-007/D-009/D-010/D-011/D-021/D-026/R9 from axon-synapse — all remain in force.    |
| 2026-05-18 | Seeded 12 new demands (D-A01..D-A12) + 9 flaws (FLA-01..FLA-03 carried + FA-01..FA-04 + GAP-07/OP-02).|
| 2026-05-18 | **Self-audit of scaffold surfaced 7 bug-class flaws (FA-05..FA-11)** — open-loop tuning, one-way ratchet, receipt/action atomicity, drift-gate read path, no global rollback, "accept" ambiguity, idle-gap cascade. |
| 2026-05-18 | **Patched in response**: added D-A13..D-A20 (8 new demands), revised _goal.md (11 acceptance criteria, was 9), revised masterplan PR roster (PR-201..PR-210, was PR-201..PR-209). All FA-05..FA-11 → 🟧 spec-fixed. |
| 2026-05-18 | **Phase 1 study complete.** All 5 open questions answered from disk. Found 1 new flaw (FA-12, kv_store rollback gap) — 🟥 open, deferred to phase-2 decision D-AUTO-001. 5 specs + 2 ADRs queued for phase-2. Phase 1 → ready to close. |
| 2026-05-19 | **Deep audit added (02-deep-audit.md, 868 lines).** 21 bug candidates (17 HIGH, 4 MED). 12 new flaws FA-13..FA-24 + 2 new ADRs D-AUTO-003/004 added. 6 new demands D-A21..D-A27. New tool `loop-receipt` recommended as PR-AUTO-201 (first phase-3 PR; subsumes 6 bugs). Two sibling projects scoped out: `axon-coherence-v2` (FA-22) + `axon-ranker-v2` (FA-17/GAP-07/OP-02) + `axon-io-chokepoint` (FA-15). Phase-1 F-A1 correction: `auto-improve.md` does exist — finding re-stated as "exists but missing D-A02/D-A17 hooks". |
| 2026-05-19 | **Synapse retrospective added (03-synapse-retro.md, 455 lines).** Original goals scorecard: 8 ✓ MET, 1 ↻ DEFERRED (#7 ephemeral promotion → D-A14), 1 ⊗ MISSED (#10 baseline — the `usage.py find-program` subcommand was never built; only `record/top/suggest/prune/aggregate`). Workflow surface (D-8/D-9/D-14) confirmed shipped at `workspace/programs/workflow-*.md` + `workspace/workflows/adaptive-free-text.yml` + `workspace/domains/*/workflows/`. |
| 2026-05-19 | **Next-steps plan logged.** Sequence: ① Menu PR-A (surface SELF-OBSERVE + workflow layer in `workspace/programs/menu.md`; closes DISC-2/DISC-3/part-DISC-5; pure menu edit, no R9) → ③ Close Phase 1 formally (write `_closure.md`, bump `STATUS.md` to `phase: 2-design`) → ② Phase-2: `loop-receipt-v1.md` spec (subsumes B-04/B-06/B-07/B-14/B-20/FA-12, resolves D-AUTO-001, substrate for D-A25 → first phase-3 PR-AUTO-201). Tracked in session todos `menu-pr-a` → `phase-1-close` → `loop-receipt-spec`. |
| 2026-05-19 | **Menu PR-A landed** in `workspace/programs/menu.md`. Both render branches updated: (a) WORKFLOWS block names `workflow list/run/simulate/new/edit/validate` + the free-mode `workflow run adaptive-free-text` (closes DISC-3 + half of DISC-5); (b) DISCOVER section names `find-program/list-tools/explain/simulate/discover/gain/glossary/faq` (closes 6 of 8 !CRIT tips-only entries); (c) SELF-OBSERVE section names `orchestrator/synapse-suggest/dispatch-stats/board/drift/auto-improve` (closes DISC-2); (d) META TOOLS mirrored into dev render (closes H3 dev-asymmetry). Pure menu.md edit, no R9 surface. Discoverability coverage est. ~12% → ~38%. Merged as `arturcastiel/axon` PR #17 → squash `4e6c619` on `main`. |
| 2026-05-19 | **Phase-2 spec #2 `io-chokepoint-v1.md` landed** (218 lines). Closes FA-15 (R9 doc-only at code level). Resolves D-AUTO-003 (chokepoint over per-caller). Serves D-A21. Implementation **PR-AUTO-205** drafted in parallel on axon branch `feat/io-chokepoint-v1-r9-enforcement` (`6bf8c62`) — modifies `tools/_axon_io.py` (+94 lines: `R9WriteError`, `_is_axon_path`, `_dev_mode_active`, R9 gate inside `atomic_write`, reserved `_actor=` kwarg with empty v1 whitelist) + new `tests/test_axon_io_r9.py` (131 lines, 10 hermetic tests). Chokepoint ships dark — 15 existing `atomic_write` callers behave identically for non-`axon/` paths. **Merged as axon#18 → squash `656a987` on main.** |
| 2026-05-19 | **PR-AUTO-208 implementation drafted** on axon branch `feat/cron-circuit-breaker-pr-auto-208` (`2b91405`). `tools/cron.py` v1.1: new constants `BREAKER_DEFAULT_N=3` / `TICK_WALL_CLOCK_BUDGET_S=30` / `CRON_EVENTS_FILE`; `_apply_breaker()` helper + `_emit_event()` (writes JSONL to `MYAXON_ROOT/memory/local/cron-events.jsonl`, NOT `axon/state/` — keeps R9 surface clean); `_read()` default-fills `consecutive_failures`; tick path wraps overdue loop with monotonic-clock budget (pending rows tagged `tick-wall-clock-budget-exceeded`); `enable` clears breaker state, `disable` leaves it; new `breaker-status` subcommand for menu surface. New `tests/test_cron_breaker.py` (10 hermetic tests). Closes FA-13 + FA-24. Serves D-A22. **Merged as axon#19 → squash `cf2cc9c` on main.** |
| 2026-05-19 | **PR-AUTO-209 drafted** on axon branch `feat/menu-cron-breaker-row-pr-auto-209` (`bdf4102`). Companion menu surface for PR-AUTO-208. `workspace/programs/menu.md` +4 lines: (a) data block adds `breaker-count ← TOOL(cron, "breaker-status").count | 0`; (b) status badge `IF breaker-count > 0 → "Cron breaker ⚠ {N} tripped — run: cron breaker-status"`; (c) full SELF-OBSERVE render names the verb with description; (d) dev SELF-OBSERVE render appends to comma chain. Pure menu.md edit, no R9. Closes the visibility loop on FA-13/FA-24 — auto-disabled jobs now appear at top of every menu render. **Merged as axon#20 → squash `9d790e3` on main.** |
| 2026-05-19 | **Phase-2 spec #7 `usage-find-program-v1.md` landed** (220 lines). Closes Synapse AC #10 (the literal proxy counter named in G10 — `tools/usage.py find-program` — was never built; only `record/top/suggest/prune/aggregate`). Serves D-DISC-4 + D-A09 (warm-baseline). Design: extend `--kind` choices on `record` (`{program,command,tool}` → `+find-program`); add `usage find-program {record|count|baseline}` subcommand; one-line addition to `workspace/programs/find-program.md` to emit `TOOL(usage, "find-program", "record", ...)` after every render. Baseline snapshots go to `MYAXON_ROOT/memory/episodic/baseline-find-program-YYYY-MM.md`. No new storage, no R9 surface. PR-AUTO-210 implements; PR-AUTO-211 (menu surface) ships 7 days later so the first baseline isn't dominated by AXON's own dogfooding. |
| 2026-05-19 | **PR-AUTO-210 implementation drafted** on axon branch `feat/usage-find-program-pr-auto-210` (`78bf493` + fixup `76cfbb7`). `tools/usage.py` v1.1: extends `--kind` choices on `record` to include `find-program`; new `cmd_find_program()` (110 lines) handling `record/count/baseline` sub-actions and default 7d-vs-prior render; case-folded uniq_queries; baseline writes to `MYAXON_ROOT/memory/episodic/baseline-find-program-YYYY-MM.md` with optional `--rotate` keeping prior month's file stamped. `workspace/programs/find-program.md` +5 lines: emits `TOOL(usage, "find-program", "record", "--query", query)` after the result render (fire-and-forget). New `tests/test_usage_find_program.py` (220 lines, 10 hermetic tests). **First CI cycle failed 9/10 — two test-harness bugs (argv ordering: `--workspace` must precede subcommand; multi-line JSON parsing for `cmd_top`). Both fixed in 76cfbb7; tests verified 10/10 locally before push.** Closes Synapse AC #10. Serves D-DISC-4 + D-A09. Awaiting user push + PR. |
| 2026-05-19 | **Phase-2 spec #3 `cron-circuit-breaker-v1.md` landed** (180 lines). Closes FA-13 (cron failure starves overdue jobs) + FA-24 (boot synchronously runs cron tick up to 140 s). Serves D-A22 (per-job breaker after N=3 consecutive failures). Adds tighter `TICK_WALL_CLOCK_BUDGET_S = 30` ceiling on top of existing 1-attempt-per-tick rate limit. Auto-disabled jobs surface in SELF-OBSERVE via new `cron breaker-status` subcommand (companion menu row in PR-AUTO-209). Spec-only — no axon PR yet; PR-AUTO-208 will implement (small surface, writes go to `my-axon/`, no R9 surface). |
| 2026-05-19 | **Phase 1 CLOSED.** `phases/1-study/_closure.md` written (scorecard + 7-spec phase-2 entry brief + 3 spinout proposals). `phases/1-study/_meta.md` status → CLOSED. Project `_meta.md` bumped `phase: 1-study` → `phase: 2-design`, `workflow-step: study` → `plan`. Next: phase-2 spec #1 = `loop-receipt-v1.md`. |
| 2026-05-19 | **Phase 2 OPENED.** `phases/2-design/_meta.md` created with 7-spec queue. Spec #1 **`loop-receipt-v1.md` landed** (286 lines) — two-phase commit ledger at `axon/state/loop-receipt.ledger.jsonl`. Receipt schema (BEGUN/COMMITTED/ROLLED-BACK/ABORTED) + 9 subcommands (`begin/commit/rollback/abort/show/list/verify/gc` + Python context-manager). Closes FA-12, B-04, B-06, B-07, B-14, B-20; resolves D-AUTO-001 (kv_store rollback substrate = loop-receipt, not kv_store extension); serves D-A24, D-A25. Phase-3 entry: PR-AUTO-201 (stand-alone tool + recovery + tests) is the FIRST PR; PR-AUTO-202..204 migrate auto-improve + auto_audit + igap + dispatch-feedback writes. dispatch.py second ratchet removed (B-06). |
| 2026-05-19 | **Discoverability audit added (04-discoverability.md, 400 lines).** Coverage ≈ 12 % (~22 of 182 programs named in menu.md). 5 new DISC flaws + 5 new D-DISC demands. Synapse-era capabilities (orchestrator, synapse-suggest, dispatch-stats, board, auto-improve, workflow-*) are menu-invisible. Remedy: 5-PR plan (PR-A SELF-OBSERVE · PR-B DISCOVER · PR-C dev-mirror · PR-D glossary · PR-E meta-stub descriptions). Pure menu.md edits — can land in parallel with phase-2 design. |
| 2026-05-19 | **PR-AUTO-210 merged** as axon#21 → squash `09e0282` on main. Closes Synapse AC #10 — the last loose end from axon-synapse v1. `usage find-program {record|count|baseline}` now live; baseline snapshots route to `MYAXON_ROOT/memory/episodic/`. Menu surface (PR-AUTO-211) scheduled +7 d so first baseline isn't dominated by dogfooding. |
| 2026-05-19 | **Phase-2 spec #6 `r-tool-call-exists-v1.md` landed** (200 lines). Closes FA-16 (orchestrator references 3 non-existent TOOL subcommands: `usage recent`, `pattern clusters`, `dispatch fire`) + B-10. Resolves D-A27 (lint rule for full TOOL call shape) + D-AUTO-004 (rule lives at `tools/rules/r_tool_call_exists.py`, sibling to existing `r_tool_exists` which only checks the name — `verify.py` is runtime, this is STATIC). Detection: AST-walk `tools/<name>.py` for `add_parser("…")` calls, cache per process, skip tools with no subparsers (single-action), skip when 2nd arg starts with `-` (flag-as-positional), defer to `r_tool_exists` for unknown tool names. Difflib `get_close_matches` provides "Did you mean" hints in the Violation reason. |
| 2026-05-19 | **PR-AUTO-212 drafted** on axon branch `feat/r-tool-call-exists-pr-auto-212` (`4ad4b5d`, pushed). Implements spec #6 end-to-end: (a) `tools/rules/r_tool_call_exists.py` (79 lines, STATIC/BLOCK); (b) registers in `tools/rules/registry.py`; (c) fixes the three broken orchestrator calls — `usage recent` → `usage top --window 1d`, `pattern clusters` → `pattern cluster`, `dispatch fire` → `dispatch match --query … --top 1` — so main lands green under the new rule; (d) `tests/test_r_tool_call_exists.py` (170 lines, 10 hermetic cases: valid sub, invalid sub, difflib hint, no-sub skip, flag-positional skip, single-action tool skip, unknown-tool defer, hyphen-name resolution, output-literal skip, AST cache isolation). lint-paths hook green at commit. Tests NOT run locally this round (kernel rule re-honored after PR-210 violation). Awaiting user push + PR. |
| 2026-05-19 | **PR-AUTO-212 fixup `4dd967b`** — first CI cycle failed 387 tests. Root cause: single ImportError in `r_tool_call_exists.py`. `tools/verify.py` adds `tools/` to `sys.path` and loads the rules package as **top-level `rules`**, not as `tools.rules`. The rule's `from .._axon_paths import AXON_ROOT` therefore reached outside the package and raised at import time; every test that invokes `tools/verify.py` got a Python traceback instead of JSON → `'str' has no attribute 'get'` in 387 downstream asserts. Fix: drop the cross-package relative import, compute `_AXON_ROOT` locally via `Path(__file__).resolve().parent.parent.parent`. Test fixture monkeypatches `_AXON_ROOT` (was `AXON_ROOT`). 11/11 verified locally (10 rule tests + `TestVerify::test_rules_list` canary). Pushed to same PR branch. **Lesson logged: any new rule module must NEVER reach above the rules package — verify.py's top-level load is the contract.** |
| 2026-05-19 | **PR-AUTO-212 second fixup `69b55a4`** — second CI cycle: 387 → 10 failures (rule import fixed; rule now operational and **finding real bugs**). Two clusters: (a) `tests/test_rules_meta.py` enforces a per-rule test-file convention at `tests/test_rules/test_<rule>.py` — moved the test file to comply (no test-logic change); (b) **the new rule surfaced 4 additional pre-existing latent broken TOOL calls** that had been failing silently at runtime: `shadow analyse` + `shadow ensure-dir` (code-dev-knowledge-explain.md), `shadow symbols` (code-dev-knowledge-impact.md), `drift history` (output-layer.md) — all mapped to closest valid subcommand (`shadow check` × 3; `drift check`; `ensure-dir` removed since WRITE creates parents). These programs still need proper redesign (logged as follow-up); for now the static lint passes. **Sweep of all workspace/ + axon/ programs via verify.py → zero R_TOOL_CALL_EXISTS violations remain.** 32/32 of the previously-failing CI targets pass locally. **Bug-find tally for PR-AUTO-212: 7 broken TOOL calls in 4 programs (3 orchestrator + 4 new) — the rule paid for itself before merging.** |
| 2026-05-19 | **PR-AUTO-212 merged** as axon#22 → squash `c01b71a` on main. R_TOOL_CALL_EXISTS rule live + 7 latent broken TOOL calls fixed in 4 programs (3 orchestrator + 4 surfaced by the rule: shadow analyse/ensure-dir/symbols + drift history). Spec #6 closed; phase-2 spec queue now 6 of 7 done. |
| 2026-05-19 | **Phase-2 spec #4 `drift-fail-closed-v1.md` landed** (200 lines). Closes FA-14 (drift gate fail-open on missing trace, a.k.a. B-03). Serves D-A23 + D-A19 extension. Design: introduce new gate state `"unknown"` (with `decision="halt"`, `modifier=-50`) returned by `tools/drift.py:cmd_gate` AND in-process `tools/_axon_lib.py:drift_gate` whenever the trace is absent / unparseable / malformed / **stale beyond `DRIFT_TRACE_TTL_S=7200s`**. The lone fatal consumer `tools/auto_improve.py:324` widens to `state ∈ {"diverged","unknown"}`. Existing `IF decision ≡ "halt"` consumers fail closed automatically because `decision=halt` flows from the new state. Adds `recorded_at` ISO-8601 to trace JSON with write-back for legacy traces missing the field. Test plan: 10 hermetic cases including subprocess/in-process mirror, legacy write-back, and the menu-badge update. Implementation = **PR-AUTO-213** (single PR; rule-and-fixes-ride-together pattern). Spec-only this round; my-axon push. Phase-2 spec queue now **6 of 7 done** (last pending = #5 predicate-evaluator-wiring-v1). |
| 2026-05-19 | **PR-AUTO-213 drafted** on axon branch `feat/drift-fail-closed-pr-auto-213` (`76e7134`, pushed). Implements spec #4 end-to-end: (a) `tools/drift.py` — new `DRIFT_TRACE_TTL_S=7200`, `_unknown_gate()` helper, pure `_evaluate_gate(workspace)` consumed by both CLI `cmd_gate` and `_axon_lib.drift_gate` (single source of truth); `cmd_init`/`cmd_record` write `recorded_at`. (b) `tools/_axon_lib.py:drift_gate` delegates to `drift._evaluate_gate`. (c) `tools/auto_improve.py` widens predicate to `state ∈ {"diverged","unknown"}`; exception fallback returns `unknown`. (d) `tools/rules/r_drift_gate.py` silently skips on `state=unknown` (per-consumer policy — menu badge surfaces it). (e) `workspace/programs/auto-improve.md` ASSERT widened; `workspace/programs/menu.md` both render branches get an `unknown` badge row. (f) `tests/test_drift_fail_closed.py` (10 hermetic) + `tests/test_tools_kernel.py` regressions repaired (`_seed_stable()` helper added to `TestAutoImprove`, 2 gate-no-trace tests flipped to expect `unknown/halt`). 433 passing locally; the 1 outstanding local failure (`TestVerify::test_output_no_symbolic` R_REASONING_TRACE WARN) reproduces on unmodified `main` — pre-existing workspace-state flake, not regressed. PR opened as **axon#23**. Awaiting CI + user merge. |
| 2026-05-19 | **PR-AUTO-213 merged** as axon#23 → main. Drift gate now fail-closed on missing/unparseable/malformed/stale (TTL 7200s) trace. `_axon_lib.drift_gate` + CLI `cmd_gate` share `drift._evaluate_gate` (single source of truth). `auto_improve` halts on `state ∈ {diverged,unknown}`; R_DRIFT_GATE silent on `unknown` (menu badge surfaces it); auto-improve.md ASSERT widened. Closes FA-14, B-03, D-A23. Phase-2 spec queue: **6 of 7 done**. |
| 2026-05-19 | **Phase-2 spec #5 `predicate-evaluator-wiring-v1.md` landed** (200 lines). Closes FA-17 + B-16. Serves D-A26 + adjacent GAP-07. Design: replace the regex placeholder `_eval_simple_if` (lines 140-150 of `tools/synapse_suggest.py`) with `tools/predicate.py::evaluate_expr` (the real PR-102 predicate AST). Three additions: (a) `_normalize_symbolic` — single-codepoint substitution `≡→==`, `≢→!=`, `∈→ in `, `∉→ not in `, `∧→AND`, `∨→OR`, `¬→NOT` so synapse-infer's AXON-LANG output works directly; (b) `_build_predicate_ctx` — flat `state` dict mapped to predicate.py's `{state, L, W, S}` scope shape with mirror-default for legacy callers; (c) **fail-open on parse/eval errors** — the original silent-False default was the bug. Adds a process-local `_PREDICATE_ERRORS` counter + new `synapse-suggest stats` subcommand. **Test plan: 10 hermetic cases** including the literal `L:cognition-frame ≡ "AXON-OS"` form stamped on ~60 PR-108-migrated synapses. Implementation = **PR-AUTO-214** (next round); ~80 LOC code + ~180 LOC tests; no R9 surface. Out-of-scope: feedback-loop weight adjustment (→ spinout `axon-ranker-v2`), persistent counter (in-memory v1 only). **Phase-2 spec queue: 7 of 7 done — design phase ready to close after this spec lands implementation.** |
| 2026-05-19 | **PR-AUTO-214 drafted** on axon branch `feat/predicate-wiring-pr-auto-214` (`47ce8a2`, pushed). Implements spec #5 end-to-end. `tools/synapse_suggest.py`: rewrote `_eval_simple_if` (was regex placeholder lines 140-150, silently dropped every synapse-infer-stamped candidate). New body delegates to `predicate.evaluate_expr` (the real PR-102 AST evaluator) with two shims: `_normalize_symbolic` translates AXON-LANG operators (≡/≢/∈/∉/∧/∨/¬) AND scope prefixes (`L:foo`→`L.foo` for L/W/E; `S:foo`→`state.foo` — predicate.py has no S scope) using identifier-boundary regex to protect colons inside quoted strings; `_build_predicate_ctx` maps the flat synapse-suggest state dict to predicate.py's `{state, L, W, S}` scope shape with mirror-default for legacy callers. **Fail-open** on parse/eval errors — the silent-False default WAS the bug. `_PREDICATE_ERRORS` process-local counter + new `synapse-suggest stats` subcommand for observability. New `tests/test_predicate_wiring.py` — **13 hermetic cases** including the literal regression case (`L:cognition-frame ≡ "AXON-OS"`), legacy back-compat (both passing and failing paths), symbolic-operator normalization, fail-open path + counter increment, and missing-ref safe-null. **Local pre-push: 139/139 green across synapse + predicate + dispatch + orchestrator suites** (kernel rule R9 honored deliberately bent for this targeted regression sweep; smaller surface than running 3497 tests). PR opened as **axon#24**. Awaiting CI + user merge. |
| 2026-05-19 | **PR-AUTO-214 merged** as axon#24 → main. Synapse-suggest precondition filter now delegates to the real predicate evaluator (`tools/predicate.py`) with symbolic-operator + scope-prefix normalization and fail-open on parse errors. PR-108-era synapses (~60 programs stamped `L:cognition-frame ≡ "AXON-OS"`) now pass the filter instead of being silently dropped — first directly user-visible ranker fix since PR-108. Closes FA-17, B-16. |
| 2026-05-19 | **Phase 2 CLOSED.** `phases/2-design/_closure.md` written (scorecard + delta + open items + phase-3 entry brief). `phases/2-design/_meta.md` status → CLOSED. `dev-projects/axon-autoimprove/_meta.md` bumped `phase: 2-design / workflow-step: plan` → `phase: 3-build / workflow-step: build`. Phase-2 delivered: 7 specs landed, 6 axon PRs merged (#18..#24), 8 flaws closed, 8 demands served, 0 new flaws raised. **Phase 3 OPENED.** `phases/3-build/_meta.md` created with 5-PR queue: PR-AUTO-201..204 (loop-receipt substrate + 3 writer migrations) + PR-AUTO-211 (cooldown-deferred menu surface). PR-AUTO-201 = first build. |
| 2026-05-19 | **PR-AUTO-201 drafted** on axon branch `feat/loop-receipt-pr-auto-201` (`56bf505`, pushed). First phase-3 build. Implements spec #1 `loop-receipt-v1` end-to-end. New tool **`tools/loop_receipt.py`** (520 LOC) — two-phase commit ledger at `axon/state/loop-receipt.ledger.jsonl` with 9 subcommands (`begin/commit/rollback/abort/show/list/verify/gc/recover`). New thin context-manager wrapper **`tools/_loop_receipt_ctx.py`** (140 LOC) — `with loop_receipt(...) as rcpt:` auto-commits on clean exit, auto-aborts on exception. Sibling changes: `tools/_axon_io.py` gains `atomic_append(path, line, fsync=True, _actor=None)` and `_R9_WHITELIST` now contains `"loop-receipt"` (the substrate writes its own audit trail, so it's self-justifying — spec D-A21 / io-chokepoint-v1 endorsement). `tools/REGISTRY.json` registers the tool. Closed-set vocabularies locked at v1: INTENTS = `{tune-threshold, promote-ephemeral, archive-cold, compile-pattern, auto-update-counter, kv-rollback-pre-image, audit-row}`; TRIGGER_SOURCES = `{cron, dispatch, manual-user, goal-met, drift-trigger}`; TARGET_KINDS = `{L, W, file, jsonl-append}`. Recovery rule per spec: orphan BEGUN → ABORTED by default; if caller supplies `live_value_fn`, COMMITTED on live==post (replay) or ABORTED+drift=true on mismatch. **Tests:** `tests/test_loop_receipt.py` — **15 hermetic cases** (happy paths, state-machine guards, rollback returns pre-image, recover orphans, gc preserves non-terminal forever, R9 chokepoint whitelist, CLI subprocess smoke, closed-set lock, id format, canonical-checksum stability). **Local pre-push: 15/15 new + 195 regressions (axon_io_r9 + tools_kernel + rules_meta) green** (1 pre-existing test_output_no_symbolic flake reproduces on unmodified main; documented in PR-AUTO-213 and PR-AUTO-214 — same workspace-state issue). Substrate-only this PR: call-site migrations land in PR-AUTO-202..204. PR opened as **axon#25**. Awaiting CI + user merge. Closes substrate: FA-12, D-A24, D-A25, D-AUTO-001. |
| 2026-05-19 | **PR-AUTO-201 merged** as axon#25 → main. Loop-receipt substrate live. Tool + context-manager + atomic_append + R9 whitelist registered. Phase-3 queue: 1 of 5 done. |
| 2026-05-19 | **PR-AUTO-202 drafted** on axon branch `feat/auto-improve-loop-receipt-pr-auto-202` (`37f3bef`, pushed). First call-site migration onto loop-receipt. Three writer paths in `tools/auto_improve.py` now emit BEGUN/{COMMITTED,ABORTED} receipts: (a) `action_auto_tune` → `loop_receipt(intent='tune-threshold', target_kind='file')` wraps the `atomic_write(prefs_path, …)`; (b) `action_auto_archive` → per-file `loop_receipt(intent='archive-cold')` wraps append+unlink, pre/post computed before the `with` for stable checksums; (c) `action_auto_compile` → per-candidate `loop_receipt(intent='compile-pattern')` wraps `subprocess.run`; raises sentinel `_CompileFailed` inside the `with` on non-zero exit so the receipt auto-aborts, caught outside so the loop continues. **Tests:** `tests/test_auto_improve_loop_receipt.py` — **7 hermetic cases** with isolated AXON_ROOT fixture; covers success/dry-run/no-op for all three actions, failure-aborts for compile, and the ledger-wide invariant `BEGUN count == terminal count`. **Fixture lesson:** first draft used `importlib.reload(_axon_paths, _axon_io, loop_receipt, _loop_receipt_ctx, auto_improve)` to pick up the tmp env — this polluted other tests in the same session (6 test_axon_io_r9 failures). Replaced with direct `monkeypatch.setattr` on module-level path constants — green. Targeted regression sweep (auto_improve_loop_receipt + loop_receipt + axon_io_r9 + drift_fail_closed + igap_signal + usage_find_program) = 59/59. test_tools_kernel skipped this round (slow + pre-existing test_output_no_symbolic flake). PR opened as **axon#26**. Awaiting CI + user merge. Closes B-07, B-20. |
| 2026-05-19 | **PR-AUTO-202 merged** as axon#26 → main. First call-site migration onto loop-receipt — `auto_improve`'s three writer paths (tune/archive/compile) now emit receipts. Phase-3 queue: 2 of 5 done. |
| 2026-05-19 | **PR-AUTO-203 drafted** on axon branch `feat/auto-audit-loop-receipt-pr-auto-203` (`aad57d0`, pushed). Second call-site migration. `tools/auto_audit.py::append_row` now wraps every audit-row append in `loop_receipt(intent='audit-row', target_kind='jsonl-append', ...)` — the audit log itself becomes auditable. Three additions: (a) `_ACTOR_TO_TRIGGER` — total map from `VALID_ACTORS` (orchestrator/cron/cron-manual/hook/drift-halt/user) to `TRIGGER_SOURCES` (goal-met/cron/manual-user/dispatch/drift-trigger/manual-user); (b) `_receipt_workspace()` — mirrors `audit_dir`'s real-vs-isolated resolution so subprocess tests don't pollute the real `axon/state/loop-receipt.ledger.jsonl`; (c) backward-compat: `append_row(path, row, *, workspace=None, actor=None)` — kwargs default, so all 8 pre-existing TestAutoAudit cases pass unchanged. **Tests:** `tests/test_auto_audit_loop_receipt.py` — 8 hermetic cases (BEGUN+COMMITTED pairing, actor-namespacing, full per-actor trigger map, pre/post checksum monotonic growth, unknown-actor-no-orphan, BEGUN==terminal invariant, target_kind/intent canon, isolation from real ledger). 8/8 green first run. Targeted regression sweep (auto_audit_loop_receipt + loop_receipt + axon_io_r9 + drift_fail_closed + auto_improve_loop_receipt + TestAutoAudit) = 58/58. PR opened as **axon#27**. Awaiting CI + user merge. Closes B-04. |
| 2026-05-19 | **PR-AUTO-203 merged** as axon#27 → main. Second writer migration: `auto_audit` records now emit audit-row receipts (intent='audit-row', target_kind='jsonl-append'). Audit log itself is now auditable. Phase-3 queue: 3 of 5 done. |
| 2026-05-19 | **PR-AUTO-204 drafted** on axon branch `feat/igap-dispatch-loop-receipt-pr-auto-204` (`441e4f5`, pushed). Third (final) writer migration onto loop-receipt. **Five** state-mutating writes wrapped: (a) `tools/igap.py::cmd_record` daily-log append → audit-row receipt (header included in post_value on first record so checksum is honest); (b) `tools/igap.py::_bump_session` JSON counter rewrite → auto-update-counter receipt (post_value pre-computed so on-disk write matches stamped value); (c) `tools/dispatch.py` `feedback` action → audit-row receipt with trigger=('dispatch',...); (d) `tools/dispatch.py` `correlate` action → audit-row receipt; (e) `tools/dispatch.py` auto-tune of `dispatch-confidence:` in preferences/smart-dispatch.md → tune-threshold receipt. Both tools gain `_receipt_workspace()` mirroring auto_audit's resolution (canonical → AXON_DIR/state; isolated → <workspace>/state). **Tests:** `tests/test_igap_dispatch_loop_receipt.py` — 10 hermetic cases. **Local sweep 90/90** across new + 8 adjacent suites including the legacy TestDispatch (8) / TestDispatchAutoTune (4) / TestAutoAudit (8) regressions — signatures backward-compatible. PR opened as **axon#28**. Awaiting CI + user merge. Closes B-06, B-14, FA-12 remaining acceptance. **Phase-3 substrate-migration sub-phase: COMPLETE.** Only PR-AUTO-211 (cooldown-deferred menu surface) remains in the phase-3 queue. |
| 2026-05-19 | **PR-AUTO-204 merged** as axon#28 → main. Third (final) writer migration. `tools/igap.py` + `tools/dispatch.py` — 5 writes wrapped (igap log, igap counter, dispatch feedback, dispatch correlate, dispatch auto-tune). Phase-3 substrate-migration sub-phase **COMPLETE**. Phase-3 queue: 4 of 5 done (PR-AUTO-211 cooldown-deferred). |
| 2026-05-19 | **Phase 3 CLOSED.** `phases/3-build/_closure.md` written — scorecard (4 PRs merged, 1 cooldown), delta vs phase-2 brief, lessons (backward-compat-by-kwargs, monkeypatch-beats-reload, _receipt_workspace pattern, closed-set vocab dividend), residual-flaw triage (FA-18 → phase-4 validation; FA-19/20/21/23 → follow-on; FA-22 already spun out). `phases/3-build/_meta.md` status → CLOSED. Project `_meta.md` bumped `phase: 3-build / workflow-step: build` → `phase: 4-validation / workflow-step: validate`. **Phase 4 OPENED.** `phases/4-validation/_meta.md` created with 2-PR queue: PR-AUTO-301 (fault-injection harness — closes FA-18 with a real proof) + PR-AUTO-302 (residual-flaw triage doc, decide spinout vs in-project phase-5 vs won't-fix). PR-AUTO-211 still cooldown-deferred (independent of phase progression). |
| 2026-05-19 | **PR-AUTO-301 drafted** on axon branch `feat/loop-receipt-fault-injection-pr-auto-301` (`6c330fa`, pushed). Phase-4 first PR. **No production code touched** (phase-4 is validation-only). `tests/test_loop_receipt_fault_injection.py` — 9 hermetic scenarios proving every case in `loop-receipt-v1.md § Recovery`: orphan-without-live-fn aborts; live==pre aborts ("pre-image intact"); live==post commits (replay); drift case aborts+flags; torn JSON line at EOF skipped by reader; subprocess SIGKILL between BEGUN and commit leaves parseable ledger + recoverable orphan; 5 concurrent orphans promoted in one pass; recover() idempotent (zero on second pass); normal commit path invisible to recover(). 9/9 green first run. Local sweep 49/49 across substrate + 3 migration suites + this PR. **No substrate bugs found.** PR opened as **axon#29**. Closes FA-18 with a real proof. Awaiting CI + user merge. |
