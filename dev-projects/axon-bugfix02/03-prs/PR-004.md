# PR-004 — Rebuild resume detection over persisted working memory
Status: implemented · Wave: B (criticals) · Complexity: M · Deps: none

## What (workspace/programs/resume.md)
- Detection = persisted `W:active-phase` (W: keys live as files under
  workspace/memory/working/ and survive session death) using the SAME non-terminal
  predicate as the kernel boot + interrupt gate; plus `W:_paused-program`/`W:_paused-phase`
  (interrupt-gate pause keys). The old episodic filter matched event names NO producer
  writes (real rows: checkpoint/session-saved only) → detection was fully dead (C4).
- Episodic log demoted to enrichment: last real `checkpoint` rows via {time, event, notes}
  (the real columns; the old .session/.detail reads targeted nonexistent fields).
- Turn-log parse fixed to real bare-timestamp headers (`## YYYY-MM-DD …`) in both the
  today and yesterday branches (old `## T-\d+` matched zero blocks).
- Resume action now resumes the detected program (EXEC target from the phase pointer;
  paused programs restore their phase first); desc header updated. Council overturn D3:
  no event-vocabulary standardization, no kernel edit, no writer changes.

## Verification
- Content pins (dead event names absent from ops; W:active-phase present; .detail gone;
  T-N pattern gone). Conformance + memory-key lints green.
