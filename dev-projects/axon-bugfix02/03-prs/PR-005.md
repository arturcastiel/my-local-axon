# PR-005 — Gain: honest rebuild over data that exists
Status: implemented · Wave: B (criticals) · Complexity: M · Deps: none

## What (workspace/programs/gain.md + one conformance exemption)
- SESSIONS panel rebuilt over real sources: E:session-log row counts by real event
  (checkpoint / session-saved over {time,event,notes}) + daily turn-log files
  (workspace/log/turns/*.md, one `## <timestamp>` block per turn) → turns total,
  turns/day, active days. The old panel aggregated s.turn_count/s.programs_run/
  s.drift_events/s.errors — fields the rows NEVER carry (C2).
- CONTEXT panel reads the real `context status` fields (accumulated_tokens/percent/
  pressure); the old .estimated_tokens_used/.tee_saves reads targeted fields the tool
  never emits.
- TOP PROGRAMS: honest-starvation statement per owner decision D2 (no recorder exists on
  the agent-side execution path) — replaces a plausible-zero table; ADR lands in Wave E.
- RTK panel guarded by the stub's REAL marker (status="not_installed"); the old truthy
  TOOL? check let the panel read nonexistent fields. rtk added to the conformance lint's
  EXEMPT_TOOLS (which+forward passthrough stub — flags belong to the external CLI, same
  class as shell); its baseline entry deleted (ratchet: 2 → 1 entries).
- Turn-log writer degeneracy (constant OUT text, no program attribution) documented as
  an owner-side kernel-spec item — readers stay robust to it.

## Verification
- Content pins (dead fields absent, accumulated_tokens present, not_installed guard).
- Conformance check green with the rtk exemption; baseline shrunk to 1.
