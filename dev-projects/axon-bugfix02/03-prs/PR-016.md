# PR-016 — Metrics ADR + honest-starvation banners (D2)
Status: implemented · Wave: E · Complexity: M · Deps: none

## What
- 03-decisions/adr-001-metric-pipeline-descope.md (accepted): no agent-side usage
  recorder gets wired in this project; the kernel-protocol alternative is documented as
  owner-only future work; turn-log derivation rejected (writer degeneracy).
- tools/dispatch_stats.py: emits an `inputs` presence block ({usage_log, feedback_log})
  on summary AND savings; when both are missing a `note` states "metric pipeline
  starved … the zeros below measure the RECORDER, not your activity". The dead
  saved_tokens double-computation (first value overwritten two lines later) removed.
- gain's TOP-PROGRAMS statement (landed with the criticals wave) points at this ADR.

## Verification
- 2 tests: starvation note + inputs=missing on an empty workspace; no note and
  inputs=present when the files exist.
