# PR-012 — undo: align the rollback contract + always-fresh manifest
Status: implemented · Wave: D · Complexity: M · Deps: PR-001

## What
- undo.md reads memory.py rollback's REAL emission ({rolled_back, restored_value,
  remaining_history}); the old .ok/.value reads matched neither success nor error
  shape, so every SUCCESSFUL rollback reported FAILED while state had changed —
  safety-adjacent dishonesty, now impossible (rollback pair is manifested since
  the contract-guard wave).
- tools/run.py: the manifest is ALWAYS refreshed on a real run — the old
  `and l_keys_written` guard preserved the previous run's manifest across no-write
  runs, so undo would roll back an OLDER, unrelated run. Empty key-lists now make
  undo report "no L: changes" for the run that actually just happened.
- Cleared-manifest shape keeps every field Step 1 reads (double-undo reported
  cleanly instead of reading absent fields).

## Verification
- Pins (real fields present, dead fields absent, cleared shape); full suite.
