# PR-006 — Board: rewire aggregation to the real PR store (D1)
Status: implemented · Wave: B (criticals) · Complexity: L · Deps: none

## What (tools/pr_aggregate.py, tools/board.py)
All three stacked breakages fixed (C1; owner locked FIX over descope):
1. `pr_aggregate list` now EXISTS: parses the real PR store — each project's
   `02-prs.md` `## PR-NNN — Title` blocks + `- **Status:**` bullets (Status→state;
   id/slug aliases keep render_table and board bucket keys working) — and emits ONE
   JSON document {ok, count, prs} (board's expected shape, not JSONL). Legacy `pr-N:`
   _meta.md parsing retained only for --project-meta/--set-field compatibility.
2. board._aggregate fails LOUD: subprocess rc≠0 or non-JSON output raises RuntimeError
   (the old bare `except: return []` turned every failure into a silent empty board,
   exit 0).
3. Loud empty-state render ("no PRs found — is 02-prs.md populated?") instead of a
   header-only table; real status vocabulary mapped to columns (merged→done,
   not-started→backlog, ready→ready-for-review, unknown→backlog).

Live: 168 PRs aggregate across 16 projects today; bugfix02's own 19 render in backlog.

## Verification
- 14 board/aggregate tests green incl. new: parse_prs_md fixtures, single-doc emission,
  loud-empty render, subprocess-failure raise, real-vocabulary bucket mapping.
