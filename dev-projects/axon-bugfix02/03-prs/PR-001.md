# PR-001 — Grow output_manifest.json over the reporting-layer tools
Status: implemented · Wave: A (contract-guard) · Complexity: M · Deps: none

## What
- `tools/output_manifest.json`: 5 → 17 tools (32 manifested (tool,sub) pairs) — axon-state,
  context, drift, memory (get/set/clear/rollback), cron, auto-audit, goal, igap, dispatch-index,
  prompt-log, queue, docgen, usage, shell (exec/inspect) added; every key probed live before entry.
- `tools/program_tool_conformance.py`: new "reporting" scope family (14 dashboard/session
  programs) + baseline ratchet (`tools/conformance_baseline.json`, identity =
  (file, kind, tool, sub, detail), line-number-free; baselined findings excluded from `ok`,
  reported separately). `--scope reporting` CLI choice.
- Baseline seeded with the exactly-2 pre-existing violations, each owned by a later PR:
  find-program's dead `semantic-search` call (PR-014), gain's `rtk --period` (PR-005).
- Tests: 17 new tripwires in test_output_manifest.py (read-only pairs probed live;
  branch-dependent shell pairs probed per branch, asserted against the union; mutating
  memory/docgen pairs source-pinned); ratchet + partition + baseline-shape tests in
  test_program_tool_conformance.py.

## Verification
- `program_tool_conformance.py check` (scope-all, the crucible gate cmd): ok=true,
  0 live violations, 2 baselined. 39/39 targeted tests. Full suite: see commit.
