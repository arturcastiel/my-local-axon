# PR-014 — find-program: excise the dead semantic-search block; scan both layers
Status: implemented · Wave: D · Complexity: S · Deps: none

## What (workspace/programs/find-program.md)
- Cross-cutting pattern 3 closed: the deprecated-to-comments semantic-search setters
  left a LIVE consumer reading W:has-semantic/W:sem that nothing ever set — excised
  entirely (no-dense-rag constraint keeps semantic search out by design). This was the
  last conformance-baseline entry → the accessor-lint baseline is now EMPTY (ratchet
  fully burned; pins flipped to enforce it stays empty).
- Scan covers BOTH program layers (workspace/ + {W:ws-os}/programs/) — the 29 OS
  programs were unfindable while the description promised "installed programs".

## Verification
- Pins; conformance check green with empty baseline (regression = baseline growth).
