# PR-018 — LOW / doc-honesty sweep + arg-seeded read guards
Status: implemented · Wave: F · Complexity: M · Deps: none

## What
- ALL 38 remaining memory-key baseline entries resolved: arg-convention unguarded reads
  across 22 files (plan-*/dev-new/register-preference/code-dev-partition/run-tests/…)
  guarded with the established `| QUERY(user)` idiom (unseeded direct invocation now
  prompts instead of propagating ∅); config-class reads (L:health-score in quickstart,
  L:inventory, L:workspace-context, L:output-layer-show-context-pressure) guarded with
  explicit defaults. memory-key baseline: 38 → 0.
- The 2 shell-result dead captures fixed at source: pr-ready's ahead-behind capture
  removed with its long-gone consumer (Gate A left in U-6); review-self's changed-files
  closed with its intended feature (declared-vs-touched files gap check).
  shell-result baseline: 2 → 0.
- LOW fixes: constraints.py docstring names the real CONSTRAINTS.json; dispatch-stats
  docstring declares its precision action; auto-actions banner says side-effect
  (it STOREs L:, role mutator). `todo done --id` doc landed with the menu wave.
- Explicitly deferred (audit LOWs left flagged): synapse inputs/outputs counts (no
  counting convention defined anywhere — grading them would be guessing); vestigial
  W:myaxon-turns path key (declared path map entry, harmless); the audit's
  "setup-skipped has no reader" was re-verified FALSE (the kernel boot block reads it).

## Verification
- memory-key + shell-result lints green with EMPTY baselines.
