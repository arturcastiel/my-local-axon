# PR-011 — status + stats: real drift source, real queue shape, orphan retirement
Status: implemented · Wave: D · Complexity: L · Deps: none

## What
- status.md: queue panel reads TOOL(queue, list) {status,tasks,count} (W:ws-queue +
  .active-tasks were fiction); dispatch count from the real source-built index (was:
  compiled-mirror count rendering "Dispatch 8" vs the 166-entry live index); recent
  events read .notes (rows are {time,event,notes}).
- stats.md: queue shape fixed; drift from TOOL(drift, check) — episodic/drift-log.md
  does not exist and its only writer was the ORPHANED duplicate workspace/tools/drift.py
  (now DELETED; registry points at tools/drift.py) — the health score is no longer
  structurally blind to drift; cron "overdue" (a field rows never carry) → breaker-
  tripped count (the mechanically real signal); false "Health score saved" print →
  honest single-writer statement; Packages panel (dir never exists) removed; dead
  `memory list --key` call removed.

## Verification
- Pins: real shapes asserted, dead paths absent, orphan file gone. Lints green.
