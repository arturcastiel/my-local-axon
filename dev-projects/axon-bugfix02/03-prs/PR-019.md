# PR-019 — Mutating-path test conversion + ratchet flip to BLOCK
Status: implemented · Wave: F · Complexity: L · Deps: PR-002, PR-009, PR-012, PR-017

## What
- tests/test_bugfix02_mutating_paths.py: drives every mutating path the read-only study
  could only assess from source — todo add/list/done roundtrip; memory set→rollback
  end-to-end against the post-fix manifested contract (rolled_back/restored_value/
  remaining_history, state actually restored); constraints add→list in an isolated
  store; usage record→top (proving the recorder WORKS — only the wiring is absent,
  which pins the ADR's premise); usage→dispatch-stats inputs=present integration.
  (Loop-contract define/iterate/commit landed with the metric wave's tests.)
- RATCHET FLIP: memory-key-lint and shell-result-lint promoted WARN → BLOCK in crucible
  (their promotes_on conditions — empty baselines — were met the same day they were
  seeded). Baseline pins flipped to empty-forever: a future entry means someone
  grandfathered a new dead read instead of fixing it. keystone (the meta-gate) green.

## Verification
- 5 conversion tests + 20 lint tests green; keystone green; full suite: see commit.
