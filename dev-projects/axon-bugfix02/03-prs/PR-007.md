# PR-007 — workspace-backup: structured result checks + human-handoff on BLOCK (D3)
Status: implemented · Wave: C (safety) · Complexity: L · Deps: none

## What (workspace/programs/workspace-backup.md — full result-handling rewrite)
- RESULT-HANDLING RULE at top: success judged ONLY from {verdict, ok, exit_code};
  substring-sniffing ("error"/"fatal") eliminated everywhere — a gate-BLOCK JSON contains
  neither, which is how "✓ restored" printed after the gate refused the restore.
- Restore (has .git): remote/fetch checked stepwise; `reset --hard` expects BLOCK → renders
  the D3 HUMAN-HANDOFF block (exact command + grant-delegation alternative), stores
  awaiting-human-restore phase — never a false ✓.
- Restore (no .git) — NEW finding at plan time: clone/rsync/rm decomposed and each checked;
  failed clone FAILs loud with the temp path preserved.
- PUSH — NEW finding at plan time: the `add && diff --quiet || commit && push` precedence
  bug (push silently skipped on no-change days → committed-but-unpushed never retried)
  fixed by split, checked steps; push ALWAYS runs.
- `skip` routed BEFORE the unconfigured-∅ fallback (was shadowed in exactly the state the
  menu reminder fires in); ws-path self-referential fallback → explicit FAIL when unset;
  desc/doc naming drift (workspace→my-axon) fixed.

## Verification
- 5 content pins (no substring sniffs; BLOCK branch + handoff; push-always; skip order;
  fallback). shell-result lint green over the rewrite. Full suite: see commit.
