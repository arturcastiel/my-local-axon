# PR-010 — Menu + snapshot contract repair (parity-tested)
Status: implemented · Wave: D · Complexity: L · Deps: PR-001

## What (workspace/programs/menu.md + compiled/menu.cmp.md in lockstep)
- Queue/Cron lines read the tools (W:ws-queue and L:cron-jobs had no writer anywhere —
  the queue warning could never fire; the Cron line was dead while 13 real jobs ran).
- Reminders: true count from snap.todos_open (was: capped preview COUNT misreporting
  4-of-8 every boot); renders string (snapshot) or object (tool fallback) shapes;
  `todo done --id <id>` (the CLI's real flag).
- workflows golden-path check via .total (snapshot carries no ok); audit-7d normalized
  to a SCALAR on both paths; footer reads c.why (candidates never carried reason).
- `kv-store set L:…` (a hard-refused namespace — guaranteed failure the menu instructed
  users to run) → `memory set --scope L …`.
- Dead SELF-IMPROVEMENT panel lines removed (W:boot-cron-tick / W:boot-last-snapshot
  have no writers); duplicate library/audit probes unified.
- Compiled mirror updated in lockstep (lossless mandate) — 13 replacements + residuals.

## Verification
- 3 lockstep pins run over BOTH files; conformance + memory-key lints green
  (menu entries burned out of the memory-key baseline: 46→38).
