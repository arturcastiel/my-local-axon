# PR-013 — list-tools: registry honesty
Status: implemented · Wave: D · Complexity: S · Deps: none

## What (workspace/programs/list-tools.md — rewritten LOAD/render)
Reads tools/REGISTRY.json (the source of truth) and renders real ACTIVE/OPTIONAL/
PLANNED statuses with per-entry registry purposes. The old render read
t.status/.name/.purpose off a boot-seeded list of NAME STRINGS and branched on a
"SKIPPED" status the registry has never contained — OPTIONAL tools misrendered or
vanished. The "merged OS + workspace registries" claim (no workspace tools registry
exists) and the stale 11-entry hand-written purposes dict are gone.

## Verification
- Pins (SKIPPED absent, OPTIONAL/PLANNED present, merge claim gone).
