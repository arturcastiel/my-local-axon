# PR-017 — loop-contract: receipts land in the canonical ledger, terminally
Status: implemented · Wave: E · Complexity: M · Deps: none

## What (tools/loop_contract.py)
- Canonical→None routing guard (same pattern as the dispatch/auto_audit siblings): the
  old unconditional --workspace sent every receipt to workspace/state/ while
  list/recover read AXON_DIR/state/ — 0 loop-contract rows ever landed in the ledger
  its own "receipts LAND" fix was meant to feed.
- begin → COMMIT immediately (audit rows are atomic): rows no longer sit non-terminal
  where the next boot's recover() marks them ABORTED.
- Goal cross-registration: root-caused DEEPER than the audit — the explicit
  `--id loop-{id}` violated goal.py's goal-YYYY-MM-DD-N schema, so registration had
  NEVER once succeeded. Now: auto-generated id + `loop:{id}` tag, result CHECKED
  (surfaced warn + goal_registered in the define envelope), and CANONICAL-workspace-only
  (goal.py has a single store; the fixed call immediately exposed test-suite pollution
  — 14 junk goals purged from workspace/memory/goals.yml, hermeticity pinned by test).

## Verification
- 4 tests: routing guard unit, isolated-ledger landing with begun==committed,
  isolated-run goal skip, call-shape pin. Live e2e: define+iterate → 2 committed rows,
  goal_registered=true on canonical.
