# PR-001 — Deletion-verb gate coverage
Status: implemented · Wave: A (t3-floor) · Complexity: L · Deps: none

## What (tools/shell.py, autonomous_mode.py, hooks/enforce_pretooluse.py, output_manifest.json)
- `_classify_deletions` gives the gate a deletion SEMANTIC (had none): direct verbs
  (rm/shred/unlink/rmdir), `find -delete` / `-exec rm`, `rsync --delete`,
  `tar --remove-files`, `git clean -f*`; `_unwrap_wrapper` unwraps xargs/env/timeout/
  busybox/toybox/nice/nohup one level so the real verb surfaces.
- `_INTERP_WRITE_CALL` widened with the interpreter DELETE calls that slipped entirely
  (os.remove/os.unlink/os.rmdir/shutil.rmtree/Path.unlink/fs.unlink). (The computed-path
  interpreter residual is PR-002's addaudithook barrier by design.)
- `_enforce_deletions`: protected-subtree DENY (axon/, workspace/programs, workspace/
  memory, workspace/tools, workspace/harness) covering target-under-root AND ancestor-
  sweeps (`find workspace -delete` covers workspace/programs); bulk threshold (>25 targets
  or recursive-under-unattended) routed through the grant as a new destructive op
  `bulk-delete`. Both fail-closed.
- Hook `_BASH_ENFORCED_CODES` is data-driven and registers protected-delete + bulk-delete
  so they bite on the host Bash path; the fail-open→fail-closed hook flip is STAGED
  (council: not smuggled). `deletions` key added to shell.inspect manifest.

## Verification
- 10 tests (unwrap, classifier families, interp-delete regex, protected/ancestor/bulk
  enforcement, hook registration, destructive-op membership). Live-probed the council's
  full attack list: find -delete / shred / unlink / env-wrapped rm / rm -rf workspace/*
  all BLOCK; /tmp deletes pass. Full suite: see commit.
