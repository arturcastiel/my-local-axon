# PR-005 — Program-integrity manifest incl. AUTONOMY.md + run graphs
Status: implemented · Wave: A (t3-floor) · Complexity: M · Deps: none

## What (tools/program_integrity.py NEW, tools/program_hash_manifest.json)
- sha256 manifest over the files that GRANT or DIRECT power: workspace/programs + axon/
  programs, AND — per the red-team, moved ahead of the spine — every AUTONOMY.md under
  my-axon/dev-projects + repo roots, and every doctrine run graph (03-prs/DAG.json,
  *.routine.yml). REGISTRY carried only last_modified; no content manifest existed.
- Separate REVIEWED file (never the autogen REGISTRY, so drift is never auto-blessed);
  `bless` re-records (human/review action), `check` flags tampered (blessed hash != live)
  + missing; new files are `unblessed` (grandfathered), not tampered.
- Crucible WARN + promotes_on (0 unblessed for a wave → BLOCK) — the proven staging
  recipe. Registered (178 tools); mirror + doc counts lockstep.
- 247 files blessed at implementation; check green.

## Verification
- 5 tests: coverage (programs + power globs), live-manifest-clean, tamper detected,
  unblessed≠tampered, registered-WARN-with-promotes_on. keystone green.
