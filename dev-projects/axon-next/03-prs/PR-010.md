# PR-010 — Workflow schema: outputs legalized, typed node kinds
Status: implemented · Wave: C · 

schema synapse gains kind (action/gate/checkpoint/human-handoff) + outputs (G8: the runner enforced what the schema forbade); doctrine_lint profile (gate has if:, checkpoint has outputs, handoff no auto-branch). 6 tests.
