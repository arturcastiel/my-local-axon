# PR-008 — Fail-closed activation + grant⟷doctrine binding
Status: implemented · Wave: B · Deps: PR-006, PR-003, PR-005
grant.doctrine field {project_dir, doc_sha, bound_ts}; bind_doctrine() records it.
authorized() BEFORE repo/scope: an UNATTENDED grant authorizes NOTHING unless bound to a
present+valid AUTONOMY.md whose hash still matches — closes the red-team's raw-grant
bypass. Interactive grants unaffected (human present). Tamper the doc → re-locks. CLI bind.
The op-time refusal dominates a merge-time rule; runner-halt is PR-014's start check. 6 tests.
