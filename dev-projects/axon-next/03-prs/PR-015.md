# PR-015 — S7b: current-node op-class gate + scope binding
Status: implemented · Wave: D · 

shell.gate_check gains _enforce_doctrine_node: while a doctrine run is active, the HOST refuses a write/delete whose current node's op_class is not write (gate/human-handoff can't mutate). The council's unanimous sharpest tooth — run and OBEYED, not self-reported. Hook registers doctrine-node. 7 tests.
