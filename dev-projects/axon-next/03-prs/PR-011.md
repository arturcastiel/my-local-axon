# PR-011 — Goal-ctx bridge + deterministic resolve-next
Status: implemented · Wave: C · 

build_gate_ctx gains a goal-state source (G9); resolve_next evaluates on-complete rules in-order via predicate.py and returns first-true — the TOOL picks the branch, not the walker (skeptic V2.2: no self-graded gate). 6 tests.
