# PR-004 — Receipts v1.1: one enum bump, hash-chain, destructive-act wrapping
Status: implemented · Wave: A (t3-floor) · Complexity: M · Deps: none

## What (tools/loop_receipt.py, tools/shell.py)
- SCHEMA v1.1: ALL doctrine-era vocabulary in ONE version event (ordering seat): intents
  doctrine-node/delegated-destructive/dag-repair(v2-reserved); triggers unattended-run/
  doctrine-run; target kind tree.
- HASH-CHAIN: each row carries prev-sha = hash of the prior row; `verify-chain` walks the
  ledger and a silent insert/edit/delete breaks it (red-team: "a self-written ledger is
  the agent's diary"). MIGRATION-TOLERANT: the 3882 pre-chain canonical rows (no prev-sha)
  don't break verification — the chain enforces from the first chained row.
- shell.cmd_exec wraps any allowed destructive-git/deletion (under an unattended run) in a
  two-phase receipt: begin before exec, commit/abort on exit — a green ledger becomes
  evidence, not just the agent's word.

## Verification
- 5 tests: v1.1 vocabulary present, valid chain, tamper detected (first_break), legacy
  rows tolerated, shell wrapping pinned; existing loop_receipt closed-set pin updated to
  v1.1. Live: valid chain ok, tamper → ok:false, 3882 legacy canonical rows tolerated.
