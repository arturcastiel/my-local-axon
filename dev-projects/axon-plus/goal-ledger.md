# Goal ledger — Axon Plus · organized 2026-06-11 · interrogation in progress
# Order = interrogation + likely execution order. Hardened text replaces each entry as
# its interrogation completes.

A  tokens ★TOP        Deep token-consumption reduction. MANDATE: lossless — nothing may
                      degrade. Instrument spend FIRST, then reduce. [INTERROGATING]
B  discoverability+   code-dev/workflow modes & tools: suggested, explained, accessible
                      at the moment of need (extends the pr-6 pattern to the whole surface).
C  loop-prompting     Easy authoring of convergent self-improving loops (target →
                      iterate → measure → converge). Substrate exists (UNTIL/EVAL/RETRY,
                      goal tool, loop-receipts, multiple-code-dev); missing = the easy
                      user-facing designer + convergence contract. Incorporate into
                      code-dev/workflows.
D  modes-expansion    Review+expand code-dev & workflow modes: study-definition modes,
                      technical-audit mode, technical-goal-setting help, better
                      translation, explore the whole design space. (New modes may BE
                      loops → builds on C.)
E  workflow-tools     Workflow engine strong; its tools need tweaking/improvement.
F  quality-loop       MERGED G2+G6: continuous find→fix→audit bug loop. Natural pilot
                      for C's convergence contract.
G  model-robustness   Older/smaller models compress & bypass rules — instructions must
                      be tier-robust (explicit, shortcut-proof). Interacts with A
                      (instruction length vs compliance).
H  documentation      Docs improvement pass.

Dependencies: A informs B,D,G · C feeds D,F · B's pattern exists (pr-6 pilot).
