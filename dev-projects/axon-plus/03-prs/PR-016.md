# PR-016 — B: situation-trigger engine (≤1 hint/turn, deduped, why+command)

**Project:** axon-plus · **Phase:** 3-quality-discover · **Complexity:** M
**Status:** merged locally (2026-06-15) · **Branch:** axon-plus/pr-16-situate
**Depends on:** none · **Design:** 03-prs/PR-016-DESIGN.md (workflow w3g1myq00)

## Delivered (engine half; PR-017 renders it)
tools/situate.py: 4 priority-ordered detectors (phase-entry > read-streak > repeat-steps
> graph-queries) over EXISTING on-disk signals — a reader, no new instrumentation. ≤1
hint enforced two ways: per-call Hint|None type + per-turn idempotence (last_turn).
Turn-based dedup ledger (window 8) + anti-double-up against the footer's own candidates.
why+command are MANDATORY by construction (a Hint missing either raises). Wired into
orchestrator.md (ANTICIPATE block + tick.hint in RECORD), with an anti-orphan lock test.
The footer render (tick.hint → output) is PR-017 (incl. the OUTPUT-LAYER.md kernel edit).
7 tests. Gate catch: resweep --state-json drift guard collided → flag renamed --signals.
