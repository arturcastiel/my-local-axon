# PR-005 — F: census discrepancies — investigation closed

**Project:** axon-plus · **Phase:** 0-instrument-floor · **Complexity:** S
**Status:** delivered (investigation — zero repo diff, 2026-06-11)
**Created:** 2026-06-11 · **Depends on:** none

## Findings (both "discrepancies" were not tool defects)
1. **residue-lint 0-vs-27**: the tool is healthy — `report` returns exactly 27
   literal-meta-write sites. The census probe parsed a wrong JSON key. PROBE ERROR,
   corrected. The 27-site migration remains tracked by existing todo (1b03a09c).
2. **prompt-log "no corpus"**: tool healthy (record works, writes to
   workspace/memory/episodic/prompt-log.jsonl). The real defect is DEAD WIRING:
   KERNEL-SLIM declares prompt logging "fires on every user input" but nothing
   mechanical fires it — corpus had 0 entries ever. Contract-without-mechanism =
   exactly Goal G's enforcement-reality class. ROUTED to W5/G hook work (PR-024
   family: hooks installed-by-default), not half-built here.

## Design consequence (folded into Goal F)
The quality loop MUST adversarially verify findings before queueing them — this wave's
own census produced 1 false positive (probe error) out of 2 findings. Verification step
added to F's contract in 01-study.md.
