# PR-014 — F: quality-loop (scan battery → triage → prepared diffs, report-only)

**Project:** axon-plus · **Phase:** 3-quality-discover · **Complexity:** M
**Status:** merged locally (627e342, 2026-06-12 — push queued) · **Branch:** axon-plus/pr-14-quality-loop
**Created:** 2026-06-12 · **Depends on:** PR-010 (loop-contract)

## Change
- `tools/quality_loop.py` (NEW): `scan` runs the battery (dead-code, igap, residue-lint,
  lint-code, lint-paths, program-tool-conformance, liveness, doc-anchors) → unified
  findings JSON (id, source, severity, file, hint) with dedupe keys; `report` writes the
  cycle report (my-axon project artifact) with per-finding adversarial-verify slots
  (the W0 lesson: 1 of 2 census findings was a probe error — findings get verified
  before queueing).
- `workspace/programs/quality-loop.md`: runs scan → verifies findings (evidence probes)
  → SHAREABILITY ROUTING (shared-surface first, local logged-only) → prepares fixes as
  REPORTED DIFFS (ramp cycles 1–3: nothing merges) → iterates the loop-contract
  (id=quality, metric=open_findings, min→0, budget=3/run — the owner-signed ramp design
  IS the human budget) → convergence report.
- Weekly cron row (`quality-loop run`) joining the Monday battery.

## Gates
R13 tests · scanners · crucible green.
