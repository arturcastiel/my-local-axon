# PR-008 — A: brief envelopes (boot default flipped)

**Project:** axon-plus · **Phase:** 1-tokens-round1 · **Complexity:** S
**Status:** merged locally (d71cba4, 2026-06-12 — push queued) · **Branch:** axon-plus/pr-8-brief-envelopes
**Created:** 2026-06-12 · **Depends on:** PR-001

## Problem
boot's envelope = 2,960 tokens/session; cron verbose objects (1,473) + the 146 tool
names (646) carry no per-session decision value — counts and ids suffice.

## Change
boot.py default = BRIEF (tools count only · cron overdue_ids + tick counts);
`--full` restores the verbose contract. Consumer audit: no tool parses the heavy
fields; only tests pinned them (updated to --full + a new brief-contract test).
Measured: 2,960 → 593 tokens (−2,367/session). prefs (691) deliberately kept — its
fields are genuinely consumed at boot.

## Gates
R13 tests · lint-paths · freshness · crucible green.
