# Audit triage — axon-plus self-audit (workflow w3g1myq00, 2026-06-15)

Multi-agent adversarial audit over the 15 merged PRs. 27 findings raised → 19 confirmed
real after per-finding adversarial verification (8 refuted). This is the quality-loop
thesis (scan→verify→fix) run via orchestration instead of cron.

## Remediation plan

### R1 — enforcement & autonomy hardening (autonomous: Python + workspace)
| sev | defect | file |
|-----|--------|------|
| CRITICAL | `define --force` bypasses the human budget wall + terminal-state refusal | loop_contract.py |
| HIGH | R_TOOL_RECEIPTS: `verify` in _EXEMPT = unconditional mimicry bypass | r_tool_receipts.py |
| HIGH | R_TOOL_RECEIPTS: regex misses the `axon.py <tool>` CLI form (mass false-neg) | r_tool_receipts.py |
| HIGH | R_MENU_RENDERED: skip branch dead (menu.md never sets active_program) → use active_phase | r_menu_rendered.py |
| HIGH | loop-receipt reuse dead-on-arrival (closed enums reject every call, silently) | loop_contract.py |
| MED | loop_contract NaN value poisons best/best_iter | loop_contract.py |

### R2 — lossless integrity (autonomous: Python + tests) — the "lose nothing" mandate
| sev | defect | file |
|-----|--------|------|
| HIGH | menu-snapshot wrapper-dict kills per-field fallback (prompt_log, workflows) | axon_state.py |
| HIGH | boot --brief drops tools.names → W:tool-registry empty (Python-only fix: keep names) | boot.py |

### R3 — engine robustness (autonomous: Python + workspace)
| sev | defect | file |
|-----|--------|------|
| HIGH | quality_loop report KeyError on non-bool verified verdict | quality_loop.py |
| MED | constraints run_checks KeyError on row missing id | constraints.py |
| LOW | quality_loop scan() crash on array-shaped scanner output | quality_loop.py |
| LOW | quality_loop dedupe key omits kind/detail | quality_loop.py |
| MED | token_bench fallback estimate undercounts ~38% (note, not block) | token_bench.py |

### OWNER-FLAGGED — kernel files (axon/) + design questions, NOT auto-fixed
- **BOOT.md** re-source W:tool-registry from REGISTRY.json — recovers the last ~646 tokens
  of pr-8 (kernel doc edit; the Python-only R2 fix restores correctness without it).
- **COMMANDS.md** dispatch pre-flight reconstructs a nonexistent `.cmp.md` path — should
  use dispatch.py's emitted source `run` field (kernel doc edit; latent — the agent
  already follows dispatch.py's run field, so not live-triggered today).
- **ramp.json integrity** (MED): autonomy state is agent-writable + self-asserted — no
  signature boundary between the loop and the gate authorizing it. Design question
  (HMAC/append-only receipts) — surface for owner decision, not a one-line fix.
- **compiled staleness** (MED): enforced only by a checkout-fragile CI test, not at
  runtime load. Design question (load-time hash check).
- R_TOOL_RECEIPTS / R_MENU_RENDERED residual MEDs (claim-vs-mention, substring section
  check) — partially addressed by R1; remaining edge cases noted as honest text-verifier limits.
