# PR-019 — ladder-advance-under-flags fix + outputs-missing escalation
Status: merged
Commit: cd3ca04
Phase: pr
Lane: AXON (autonomous, non-kernel)  ·  Critical-path  ·  Unblocks: PR-008b

## Problem (root cause — todo b3b5aea3)
code-dev-study mode-dispatch returns before the manifest advance (done() at line ~92 fires before the
~528 advance), AND an emits/WRITE drift: `# emits: 01-study.md` vs the mode-aware `study/<mode>.md`
write. Under mode flags the phase-model done() output-check misses → the ladder silently never advances.

## Approach (HR design council 2026-06-23, 3 grounded seats — architect / back-compat skeptic / completer)
The council corrected the spec's assumption: flag modes write `study/{subsystems,deep}/<sanitized>.md`
(dynamic, NOT `study/<mode>.md`). The skeptic seat established that the `# emits:` header / REQUIRED_OUTPUTS
is a STATIC FLOOR pinned by 3 test files (test_emits_drift, test_phase_model_outputs ×2) AND the
`R_TERMINAL_OUTPUTS` BLOCK rule — changing it breaks all of them. CHOSEN SYNTHESIS (sidesteps every
breakage): do NOT touch the emits floor; instead —
- **A (early-return):** add the canonical advance idiom INTO the flag-mode dispatch block in
  `code-dev-study.md` (before `DONE(...dispatched)`), per-branch (no hoisting → no double-advance).
- **B (gate parity):** `code-dev-study-area.md` writes a canonical `01-study.md` POINTER (guarded by
  `NOT FILE-EXISTS`, never clobbers a real overview study) → the existing completeness gate passes under
  flags with zero floor changes.
- **C (loud failure):** escalate `outputs-missing → LOG(ERROR)` (branch on `reason_code`) in all 5 ladder
  programs; benign `deps-not-done` stays `LOG(WARN)`.

## Files
- `workspace/programs/code-dev-study.md` (dispatch advance + overview-path escalation)
- `workspace/programs/code-dev-study-area.md` (canonical 01-study.md pointer)
- `workspace/programs/code-dev-{plan,pr-create,journal-log,safety-audit}.md` (escalation)
- `tests/test_phase_ladder_advances.py` (lockstep test update + 2 new PR-019 assertions)

## Acceptance
- Flag-mode `study` advances `_phases.json` (area writes 01-study.md → gate passes → dispatch advances).
- outputs-missing escalates LOG(ERROR) in all 5 programs; the pinned emits floor is untouched.
- Tests: ladder + emits-drift + outputs + R_TERMINAL_OUTPUTS green (40); program-md lint green (784).
- Full crucible green.

## Notes
"Do it right, don't rush" — multi-file, back-compat-sensitive foundation. Spec-FIRST per node-first
discipline; design decided by HR council (not owner) per the autonomous directive. PR-018 shipped the
reason_code foundation this escalation depends on.
