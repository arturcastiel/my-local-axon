# PR-021 — Final docs sweep + README badges
Project:     axon-tests
Created:     2026-05-16
Complexity:  M
Depends on:  PR-020
Status:      not-started
AXON score:  8/10

## Summary
Replace the 36-line `AXON-DOCS-TESTING.md` stub with a full reference
that lists every test tier, every workflow with its Guarded-by, every
rule with its test file. Add README badges for CI + coverage. Update
the `AXON-DOCS.md` index.

## Goal Context
This is the project's final PR — it ratifies everything the prior 20
PRs built. After this, AXON ships with a self-describing test surface.

## Entry Conditions
- All prior PRs (PR-001..PR-020) merged.
- All Guarded-by rows present and verified by PR-018 `--strict`.

## Changes Required

### workspace/AXON-DOCS-TESTING.md
- **What:** Full rewrite. Sections:
  · 1. Test taxonomy (smoke, unit, rules, workflows, programs,
    integration).
  · 2. Running (local + CI + pre-push).
  · 3. Coverage gates (per PR-002).
  · 4. Adding a test — checklist matching PR template.
  · 5. Catalogues:
        - rules table (rule_id ↔ test file) — sourced from
          `helpers/rules-crosswalk.md` final state,
        - workflows table (W-NN ↔ test file),
        - programs tier-A list.
  · 6. Workflow harness usage (PR-005 reference).
  · 7. Co-output rule (PR-004 + PR-018).

### README.md
- **What:** Badges:
  · CI status (tests-full job),
  · Coverage (artifact link or codecov if enabled — TBD),
  · Doc-gate (docgen-strict).
  Plus a one-line link to AXON-DOCS-TESTING.

### AXON-DOCS.md (index)
- **What:** Make AXON-DOCS-TESTING.md the headline page in the
  index; reorder if needed.

### workspace/AXON-DOCS-WORKFLOWS.md, AXON-DOCS-GOVERNANCE.md
- **What:** Final sanity sweep — confirm every Guarded-by row
  resolves to a real test id; fix any drift.

## Architecture Impact
Documentation-only. No code changes.

## Acceptance Criteria
- ✓ `docgen_verify.py --strict` passes after this PR (was already
  passing post-PR-018; this PR must not regress it).
- ✓ README shows three green badges on `main`.
- ✓ AXON-DOCS-TESTING.md > 200 lines, all sections populated, no
  TODOs.
- ✓ Every Guarded-by row maps to an existing test_id.

## Risks & Gotchas
- ⚠ Badge URLs depend on repo slug — verify post-rename if any.
- ⚠ Codecov: only enable if the user opts in; default to artifact
  upload from PR-002.
- ⚠ Documentation drift: future PRs may add tests and forget to
  update tables — that's exactly what PR-018 catches.

## Tests Added
0 (docs only).

## Doc Anchor
- `workspace/AXON-DOCS-TESTING.md` (the page itself).

## Rollout / Rollback
- Rollout: merge last, after PR-020.
- Rollback: revert; the stub state still passes upstream gates.

## Out of Scope
- New tests.
- Refactoring AXON-DOCS structure beyond reordering the index.
