# axon-obsidian — log

- COMPLETE 2026-07-08: 5 PRs. Obsidian is now properly hooked — an invokable code-dev step
  that projects a project's always-fresh graphify graph into a REAL Obsidian vault (per-file
  notes + cross-note wikilinks so the graph view works + .obsidian config + INDEX map-of-
  content). Export-only, always-fresh (rebuilds only when the graph changed; the graphify
  freshness guarantee propagates). Suite 5296/0/15. Owner decisions honored: per-project
  graph content, invokable+always-fresh, export-only. Composes directly on code-dev-graphify.
