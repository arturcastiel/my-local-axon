# Decisions (ADRs) — study

- D-001: Architecture = kernel/adapter (dag.py+goal.py+reconciler as kernel; phase/synapse as vocabulary adapters compiling down to nodes). Source: hr-team council 7/7 + Step-0 re-audit.
- D-002: Sequence = FOUNDATION-FIRST (owner-chosen 2026-06-24). Invisible refactor + schema + reconciler before user-facing R1/R3.
- D-003: git<->DAG reconciler ships READ-ONLY, fail-closed, no --fix in v1.
- D-004: R3 ships warn+auto-suggest+entry-render; no done() hard-gate in v1 (phase_model.done not wired to phase_gate — source-confirmed).
- D-005: Schema widening is a tested+migrated PR, NOT additive (add_node/set_status closed enums + SCHEMA_VERSION_MISMATCH — source-confirmed).

## Decision-council resolutions (hr-team high, 5×2, advisory_only → owner-ratifiable) — 2026-06-24
- D-006 (council D1, unanimous, c=0.87): --fix/mutation code is CUT ENTIRELY from v1 (not flag-guarded). No write/repair logic in PR-004..007. Mutation re-enters as PR-008 whose CORE exit criteria = _axon_rollback.snapshot() + idempotency + CONFLICT-HUMAN-EDIT branch tests proven in CI.
- D-007 (council D2, 4-1, c=0.78): CONFLICT-HUMAN-EDIT precedence ADOPTED WITH HALT-AS-DEFAULT amendment: (1) DAG wins on non-git-derivable hand-authored fields (disposition/goal-id/kind:gate); (2) git wins ONLY when no node exists for a merged commit AND no hand-authored field implicated; (3) flag-and-HALT is the explicit DEFAULT for every other/residual case. Per-node SCHEMA_VERSION_MISMATCH cleared upstream by D3 normalization; anything missed → HALT. Dissent: product-program-mgr keeps rule as-is but agrees unrecognized-field must HALT at PR-004.
- D-008 (council D3, unanimous, c=0.88): RUN the one-time normalization/baseline pass before the reconciler goes live — snapshotted, idempotent, fail-closed (leave-unknowns-FLAGGED, never guessed). Ships as PR-003 with fixture tests.
- D-009 (council D4, 4-1, c=0.75): R3 gate-promotion = PRE-COMMIT the METRIC, NO auto-flip in v1. Metric = accept-rate = accepted-unedited / surfaced, tracked SEPARATELY from edit-rate; rolling window min(200 surfaced, 30d), N>=50 floor; encoded as a parameterized test fixture (instrumentation, not docs). warn->block requires EXPLICIT owner/council ratification after >=1 window. RESIDUAL OWNER CHOICE: name a candidate threshold now (qa: accept-rate>=0.60 / two 30d windows / N>=30) vs observation-only no-number (4 seats). Default adopted: observation-only.
- D-010 (council D5, unanimous, c=0.85): R4 origin REQUIRES traceable source-id (tool/agent-id machine; user/session-id human) as a HARD precondition for any future R4 gating. Field defined in PR-002, asserted in PR-007 tests. Interim: fail-OPEN on untagged/machine; only human-tagged nodes gate-relevant once gating exists.
- D-011 (council D6, unanimous, c=0.92): IMMEDIATE NEXT ACTION = spec PR-001 now in parallel with ratification. DONE — 03-prs/PR-001.md written.
- D-012 (verification correction): PR-001 Item C (workflow_dag.py registry/test reconcile) DROPPED — re-audit shows workflow_dag.py is live, REGISTERED (REGISTRY.json:253), and TESTED (tests/test_workflow_dag.py + 3 more). Its dag.py-reimplementation dedup is DEFERRED to a later consolidation, not a PR-001 fix.

- D-013 (owner directive 2026-06-24): AXON does NOT autonomously modify the harness (tools/, workspace/programs, axon/ kernel). AXON's deliverable for this project is the PR SPECS (study→plan→pr); IMPLEMENTATION is the human's (code-dev 'log' phase + the "building is a human task" Core Rule). A started edit to tools/plan_dag.py was reverted; harness source confirmed unchanged. Future PRs are specced, not implemented, unless the owner explicitly directs otherwise.

## Harmonization-council resolutions (hr-team high 5x2, w4vzc426j, advisory → ratifiable) — 2026-06-24
- D-014 (harmonization, conv 0.82-0.91): The project is REFRAMED felt-value-first. ROOT goal = G2 (felt guidance pain at study phase). DELIVERY is DECOUPLED from the technical DAG and ordered by G2-payoff-per-human-hour. Two lanes: AXON spec-lane runs AHEAD+DEEP (free); human implement-lane is the bottleneck. New head of human queue = PR-S0 (study-phase entry-render, goal+how-to), foundation-INDEPENDENT (reads kernel data, lives in the phase-ladder adapter → no schema/reconciler/kernel edit), flag-gated, warn-first. Precondition: a Cycle-0 one-page kernel/adapter boundary spec (AXON design deliverable) ratified so the render lands in the adapter, not the kernel. Foundation (PR-001..004) follows BEHIND PR-S0, in locked dependency order, behind an owner re-ratification appetite gate. Goal hierarchy: G2 root → R3-render primary → R1/R2/R5 supporting → G4 enabling (hard prerequisite, not felt) → R4 deferred (last).
- D-015 (unanimous): G3 (generalize owner scripts: ship.sh/apply-pr/rule-review/stranger-test/render-bridge) is CUT from this project — owner convenience, not G2. Spin into a separate axon-ops-tooling project after the kernel/adapter boundary stabilizes (post-PR-003). Recorded here so it is not lost.
- D-016 (preserved DISSENT, owner's call): PR-S0 scope — include a STATIC auto-suggest text template (dx-designer c=0.88: cheap, completes the relief) OR goal+how-to render only with auto-suggest in slice 2 (challenger/goals-strategist/delivery-pm). Both foundation-independent. UNRESOLVED — owner decides.

- D-017 (owner clarification 2026-06-24, CORRECTS D-013): "the harness" = the HOST HARNESS (Claude Code: settings.json, hooks, output-styles, ~/.claude/, persona install) — "dont change host harness at all." It does NOT mean AXON's own code. AXON MAY implement changes to its own tools/*.py and workspace/programs/*.md (dev-mode). Still human-only/off-limits: host harness (never), axon/ kernel (Core Rule 9 floor), running builds/tests (Core Rule — AXON writes code+tests, human executes). D-013's "AXON only specs, human implements" is SUPERSEDED for AXON code: AXON implements AXON code; human runs tests. The harmonization's two-lane model relaxes accordingly (AXON is no longer blocked from implementing AXON code).

- D-018 (PR-002 implementation refinement, 2026-06-25): schema widening landed ADDITIVELY — add_node gained optional commit/origin/disposition/goal-id/source-id (included only when set; origin validated machine|human), and verify() already tolerates extra keys, so NO SCHEMA_VERSION bump and NO workspace migrate were needed. This sidesteps the council's feared red-window (D-005's "migrated PR" caution was about ENUM changes; we added FIELDS, not kinds/statuses). CONSEQUENCE for PR-003: the baseline/normalization pass no longer needs to clear SCHEMA_VERSION_MISMATCH (there is none); its remaining scope is normalizing informal kinds/statuses (kind:gate, owner-open, etc.) in existing hand-edited DAGs IF/when the enums are widened — a separate, deferrable concern. _axon_rollback.snapshot() is now wired into dag._atomic_write (every DAG write reversible) — the PR-008 mutation-safety precondition is met early.

- D-019 (2026-06-25): PR-003 (one-time baseline pass) is ABSORBED into PR-004. Rationale: D-018 removed the version bump, so there is no SCHEMA_VERSION_MISMATCH to clear — PR-003's primary purpose is gone. Its residual "record a baseline so the reconciler reports only NEW drift" is intrinsic to the reconciler and folds into PR-004. PR-003 node marked skipped (absorbed), not implemented standalone.

## Finish-tail council (hr-team high 6x2, wg63jfl2d, advisory → ratifiable) — 2026-06-29
- D-020 (STAGED-GO, unanimous): Finish the tail STAGED, never co-merged. Substrate empirically verified present in HEAD (dag.py:64 snapshot; dag.py:118-126 provenance fields; reconciler real+wired) → the "substrate-unverified" blocker is SATISFIED (decide, not re-gate).
  · Q1 PR-007 trust-waiver: GRANT conditional — spec+tests+additive code buildable NOW; MERGE gated on owner ratifying the origin rule + Tier-A advisory bar.
  · Q2 PR-008/D-006: SAFE-SUBSET — fold additive CODE_FIRST_DRIFT→add-node into PR-007; PR-008 = OVERWRITE/field repair only, stays CUT behind its own named go/no-go; DAG_MERGED_NO_COMMIT + PHANTOM_MERGE stay flag-and-HALT (never auto-repaired).
  · Q3 SIX hard safety gates: dry-run-default + --apply; idempotency proof (apply-twice==apply-once, SHA-keyed); snapshot-fires assertion (wiring already present); full CONFLICT 3-branch coverage incl negative HALT; human-confirm before OVERWRITE (per-node audit log lets additive use batch-confirm); surface-split origin: fail-OPEN additive/git-derived, fail-CLOSED the moment a hand-authored field could be touched.
  · Q4 STAGED 8-step plan; PR-007 then a NAMED PR-008 go/no-go; never co-merge.
  · Q5 advisory-stability TWO-TIER: Tier-A (PR-007) = wired + committed advisory artifact + INJECTED-DRIFT fixture proving the detector fires (clean dogfood alone is NOT sufficient); Tier-B (PR-008) = a drift-exercised finding human-classified.
  · Q6 R3 metric backend (D-009) + workflow-mirror (T3) EXPLICITLY OUT — separate projects.
  · Q7 missing: substrate audit DONE; genuinely-new BUILD = R4 detection trigger + dag_write_demand path + dry-run renderer + 6-gate test suite + injected-drift fixture + advisory artifact; PR-008 overwrite engine only after its gate.
  · OWNER WAIVERS REQUIRED (open_for_owner): (1) ratify surface-split origin rule [HARD precond]; (2) ratify additive add-node may auto-write (v1 was read-only); (3) accept substrate audit passed; (4) write+ratify the PR-008 named go/no-go threshold BEFORE PR-007 merges; (5) PR-007 waiver scope prep-only vs build-now-merge-gated; (6) per-node vs batch human-confirm.
  · DISSENT preserved: challenger (tightest Q1 prep-only; Q2 keep-CUT); release-devops-sre r1 (Q2 option b — flagged inconsistent); harness-architect r1 (Q5 organic non-CLEAN required → moved to Tier-B); qa (subset belongs in PR-008 not PR-007). 2 seats dropped R2 (API error); r1 STAGED-GO stands.

- D-021 (owner ratification 2026-06-30, via "continue"): all 6 finish-tail waivers ACCEPTED as council-recommended.
  (1) Origin rule: fail-OPEN on additive/git-derived paths; fail-CLOSED the moment a hand-authored field could be touched. (2) Additive CODE_FIRST_DRIFT→add-node MAY auto-write in PR-007, dry-run-DEFAULT + --apply + per-node audit. (3) Substrate audit ACCEPTED as passed. (4) PR-008 NAMED go/no-go threshold = a drift-exercised finding human-classified (Tier-B) AND PR-007 stable one real cycle; ratified-before-PR-008-only. (5) PR-007 scope = build additive code now, merge-gated. (6) Batch human-confirm OK for additive writes IF per-node audit entries are produced.
  → EXECUTING PR-007 (additive R4 write-half) staged: spec → tests-first (incl. injected-drift fixture = Tier-A) → reconciler --apply additive mode → full crucible → merge. PR-008 stays CUT behind its named gate. R3 metric + T3 workflow-mirror OUT (separate projects).
