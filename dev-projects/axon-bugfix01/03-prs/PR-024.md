# PR-024 — library-dev report type, ingest handoff, articles/ scan
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
H17: report read W:_args — a key the tokenizer never writes — so every requested type silently
produced a literature-review. H18: search downloaded approved articles but never wrote the path
onto the candidate; ingest's MAP(…, file) mapped nulls and approved articles vanished from the
shadow loop. M15: ingest never scanned the articles/ drop folder library-dev-new itself creates.

## Changes
- library-dev-report.md: type ← W:library-dev-report-type | W:_arg2, validated against the 4
  documented types (unknown → loud FAIL, never a silent default).
- library-dev-search.md: c.file recorded per download; FILE-EXISTS-verified; failed downloads
  excluded from the hand-off with a WARN + user-visible count.
- library-dev-ingest.md: {lib-path}articles/ joins the source-resolution chain (hand-off >
  single file > meta folder > created articles/ > ask).
- tests/test_library_dev_trio.py: 5 pins incl. the resolution-order pin.

## Acceptance: full suite green (04-log).
