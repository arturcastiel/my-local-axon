# PR-008 — Repair adaptive-free-text.yml (orchestrator node type)
Project:     axon-bugfix01
Created:     2026-07-03
Complexity:  S
Depends on:  PR-003, PR-004, PR-007
Status:      implemented (merged jointly with PR-007 — see deviation note)
AXON score:  8/10
User score:  8/10 (standing, owner-delegated 2026-07-03)

## Summary
C4: `workflow run adaptive-free-text` failed unconditionally at its first step — s1 dispatched
`synapse-suggest` (a TOOL; EXEC needs a program), s2 was hardcoded to code-dev-flow regardless of
ranking (defeating dynamic dispatch), and s3 pointed at the permanently-dead code-dev-finalize (H12).

## Design (the spec's "tool-invocation node type" option)
- workflow-run.md implements `role: orchestrator` as a runtime node type: rank the PROGRAMS corpus
  (synapse-suggest --corpus programs, post-PR-003 calibrated scores) against the goal + recent
  input, EXEC the top dispatchable hit, loud fallback (LOG + QUERY, 'stop' honored) when nothing
  clears — never a crashed EXEC of a tool name.
- adaptive-free-text.yml: s2 removed (the tick fires the ranked program itself); s1 self-loops
  until acceptance/rejection (steps>25 bound terminates); s3 → session-summary (real, live program).
- check_stale: NARROW runtime-backed exemption — role:orchestrator tool-named synapses only;
  any other tool-named synapse remains a tool-not-program finding (the C4 bug shape).

## Deviation note (logged per discipline)
Merged in the same squash as PR-007: the crucible gate's workflow-check-stale control (correctly)
BLOCKs any repo state where the strengthened lint's finding persists — PR-007 alone was not
mergeable under green-only while the C4 bug existed. DAG order (007 → 008) preserved within one merge.

## Acceptance Criteria
- [x] check-stale + check-templating: 0 findings repo-wide (crucible control green)
- [x] role:orchestrator node type implemented in workflow-run with loud no-hit fallback
- [x] Fixture test: tool-named synapse without the role → flagged; with the role → exempt
- [x] Full repo pytest suite green (counts in 04-log)
