# PR-012 — Route check-structure to the actual structure checker
Project:     axon-bugfix01 · Created: 2026-07-03 · Complexity: S · Depends on: PR-011
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
C5: `code-dev check-structure` stored W:code-dev-safety-audit-sub="structure" — a key its
target (code-dev-safety-audit) never reads — and EXEC'd the heavyweight Phase-5 FINAL audit
(wrong program, including an unexpected 05-audit.md write prompt), while the real structure
checker (code-dev-safety-audit-structure, with --fix support) was called by nothing at all.

## Changes
- code-dev.md + compiled form: check-structure → EXEC(code-dev-safety-audit-structure);
  the orphan never-read key removed (grep-verified gone from the program corpus).
- Pin test in test_two_token_router.py (route + no-orphan-key + target-exists).

## Acceptance: route pinned; orphan key gone; full suite green (04-log).
