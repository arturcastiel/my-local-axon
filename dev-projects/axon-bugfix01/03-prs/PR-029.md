# PR-029 — Inline-EXEC-args lint + pr-ready preflight fix
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
H1 + cross-cutting pattern 2: EXEC(prog, key=value) silently never propagates (callees read W:
keys). pr-ready's preflight delegate always ran noisy full mode and Gate C misread the absent
result as failure; the same shape reappeared in hr-team's re-round DURING this project.

## Changes
- code-dev-pr-ready.md Gate C: W-key protocol (STORE mode/pr → EXEC bare → RETRIEVE
  W:code-dev-preflight-result — the callee's real check-only output key).
- tools/exec_args_lint.py: forbids the kwarg shape across all program surfaces; comment
  citations exempt. Corpus swept clean first → registered as a BLOCK crucible control from day
  one (unlike the liveness WARN-first rollout — this class has no legitimate instances).
- Registry entry; onboarding doc counts synced (182 tools).
- tests/test_exec_args_lint.py: clean-corpus invariant, H1-shape catch, comment exemption,
  W-key-protocol pin at the H1 site, BLOCK-registration pin, CLI semantics.

## Acceptance: full suite green (04-log).
