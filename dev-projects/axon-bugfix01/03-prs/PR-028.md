# PR-028 — Liveness/reachability lint (pattern-7 guard)
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: M · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
The audit's dominant defect shape — implemented + unit-tested + ZERO production callers —
produced S1 (unwired authorized()), S2 (dead fire step), C2 (unregistered bundle writer),
C12 (writer-less goal index), H7 (unseeded cron). tools/liveness_lint.py makes the production
call graph the evidence: (1) every ACTIVE registry tool needs ≥1 production reference (program
TOOL() sites incl. axon kernel md, workflow YAML, cross-tool imports/subprocess, cron, hooks,
crucible controls — menu DISPLAY lines deliberately do not count); (2) SENTINEL SEAMS — the
audit's four exact dead seams — must stay wired (explicit caller patterns, python + md forms).

## Rollout
WARN-first crucible control (registered), reasoned whitelist (8 entries: 3 enforced-via-gate-
test lints, 1 external-invocation surface, 4 quarantine-candidates newly FOUND by the lint and
added to QUARANTINE.md for owner sign-off: diff, fail-render, rerank, retrieval-trace). Waivers
are always reported in the output (no silent caps).

## First-run value: found 4 previously-unknown dead tools beyond the audit's list.
## Acceptance: sentinels wired; live check green; display-vs-call-site discrimination pinned;
full suite green (04-log).
