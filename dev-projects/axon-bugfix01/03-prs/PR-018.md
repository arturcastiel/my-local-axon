# PR-018 — Goal system: envelope bug, wire the writer, scoping ADR
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: M · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
C12 (systemic, 5 call sites) + M14: phase-entry guidance treated goal list's {ok,count,goals}
envelope as the goals array — the "no goal set" fallback could never trigger and FIRST() operated
on the dict; the guidance had never surfaced a real goal in any project, ever. goal set had zero
production callers (goals.yml didn't exist on disk); records had no project scope.

## Changes
- tools/goal.py: optional `project` field on set; `--project` filter on list (project records +
  global records); list rows carry project + acceptance.
- 5 call sites: unwrap `.goals`, scope `--project {project}` (verified `project` in scope in all
  five), unreachable-fallback shape removed.
- goal-define.md: goal-shaped hardened items now write through TOOL(goal, set) with the active
  project's scope — the index has a real production writer; the markdown ledger stays as narrative.
- ADR-003 (accepted): scoping semantics + rejected alternatives.
- tests/test_goal_scoping.py: scoped set/list, envelope contract pin, per-call-site source pins,
  writer-wiring pin.

## Acceptance: guidance can now surface real goals per project; fallback reachable; full suite
green (04-log).
