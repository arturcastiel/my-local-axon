# PR-020 — Wire hr-team filters and weights
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: M · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
C3: --family/--roles were fully dead (match_roster had no such parameters); W:hr-team-filter —
the ONLY M2-FILTERED detection signal the selector reads — was never stored, so M2 was
unreachable and --domain runs were mislabeled M1-FULL at the program layer. H2: the deliberator
computed initial-weights (incl. the auditor 2x boost) and never used them — uniform 1/N always.

## Changes
- hr_team.py match_roster: roles = explicit slug allow-list (fail-closed on unknown; overlap is
  a rank not a gate for explicit picks); family = token narrowing (fail-closed on empty match);
  CLI flags added. deliberation-metrics gains --weights ([floats] or [{seat,weight}] mapped by seat).
- hr-team.md: STOREs W:hr-team-filter when any narrowing flag is set; passes --family/--roles.
- hr-team-deliberator.md: passes initial-weights through (uniform default when absent).
- Live checks: 2:1 weight elevation flips a computed winner; roles/family paths verified both ways.
- tests/test_hr_filters_weights.py: 9 tests (narrowing matrix, filter-store pin, winner-flip,
  seat-keyed CLI weights, deliberator pass-through pin).

## Acceptance: full suite green (04-log).
