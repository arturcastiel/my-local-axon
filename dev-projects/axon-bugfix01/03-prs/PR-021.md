# PR-021 — hr-team deliberation flow repairs + LOW sweep
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: PR-020
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
H3: the re-round-on-substantive-dissent path was structurally unreachable (convener ran every
budgeted round; rounds-run < rounds-max always false), the inline reround=true kwarg was never
read (H1 class), and GOTO(STEP 1) matched no heading. M2: bpc_passes referenced winner variables
defined nowhere. M3: verdict-status was ASSIGNED inside a condition — corrupted exactly in the
contested case. Plus the 5 hr-team LOWs.

## Changes
- Convener: consensus EARLY-EXIT inside the round loop (budget preserved for re-rounds;
  rounds_run reports actual); REROUND ENTRY consumes W:hr-team-reround-request (one extra sealed
  round over the existing transcript; request cleared — can never loop).
- Deliberator: one-shot guard (W:hr-team-reround-done) preserves the hard ceiling; kwarg removed;
  GOTO(SECTION:VALIDATE) with a real heading; bpc_passes = metrics.winner_forward/reversed
  (deterministic BPC, exposed through the tested helper); M3 condition fixed.
- Router LOWs: empty-task answer captured+stored (no double-prompt); provenance-audit routed;
  --context/--persona in usage; HELP tier range matches the selector (full 10×5); the undefined
  session-budget duplicate check removed from the convener.
- tests/test_hr_deliberation_flow.py: 13 pins.

## Acceptance: full suite green (04-log).
