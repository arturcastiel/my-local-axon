# PR-019 — Make hr-team audit bundles real
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: M · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
C2: write_audit_bundle (transcript/decision/manifest/events/checksums) was fully implemented and
test-covered but NOT registered in _HELPER_COMMANDS — no .md program could ever invoke it; every
`--persistence full-audit` council produced zero persisted audit trail (live bundles on disk were
test-fixture residue with literal "STUB" seat text). M1: selector tiers spoke none/session/full vs
the writer's no-logs/decisions-only/full-audit — a full-tier council would silently degrade.

## Changes
- hr_team.py: _cmd_write_audit_bundle CLI wrapper (JSON args, malformed-verdict refusal, derived
  minimal manifest) + registered in _HELPER_COMMANDS; _PERSISTENCE_ALIASES normalized at the
  single write point (tier vocabulary accepted).
- hr-team.md: persists the bundle after the surfaceability/advisory guards; WARN if a
  should-persist mode wrote nothing. Selector tier table canonicalized.
- tests/test_hr_audit_bundle_wiring.py: registration pin, alias matrix (5/2/0 files), CLI
  round-trip with checksum verification, malformed-verdict refusal, program + selector source pins.

## Live verification: --mode full → 5-file checksummed bundle under hr-team/councils/<id>/.
## Acceptance: full suite green (04-log).
