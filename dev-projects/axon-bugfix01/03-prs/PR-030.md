# PR-030 — Doc/registry honesty sweep (KERNEL EDIT, pre-authorized)
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: PR-016
Status:      implemented · AXON 8/10 · User 8/10 (standing)
Authorization: owner order 2026-07-03 covers the kernel lines (logged).

## Changes
- KERNEL (v1.1.9 → v1.1.10, F50 relocked): "Enforcement reality" updated — destructive-git
  refusal, the unattended kernel floor, and the AEGIS test-execution gate are now stated as
  MECHANICAL (this project built them), with ordinary-op git governance honestly protocol-level
  and the liveness lint named as the seam guard. M13: local/-scope description corrected
  (dev-mode/first-run-complete are canonical L: keys by design).
- M6: axon/tools/REGISTRY.md regenerated from the live registry (was >100 tools stale) +
  test_registry_mirror_completeness (completeness, no ghosts, live count) — the "guarded by"
  tests its header claimed now exist.
- M4: orphaned + divergent workflow-new-questions.yml deleted.
- M10: the three empty scaffolding stubs quarantined for owner decision (they announce
  themselves honestly when run; excluded from dispatch since the status-aware rebuild).
- LOW batch: 7 stale FAIL() self-attributions corrected (mechanical sweep matched the audit's
  list exactly); code-dev-init HELP claim; glossary placeholder precondition; workflow-new
  undefined enumeration → programs registry; workflow-explain f-string normalized; discover's
  computed-but-never-read dep-scan surfaced; skip-guard menu now receives real downstream
  phase ids (manifest-derived).
- tests/test_doc_honesty_sweep.py: kernel-claims pins, local/-scope pin, the FAIL-attribution
  invariant (whole-corpus), orphan-gone pin, LOW-batch pins.

## Acceptance: full suite green (04-log).
