# PR-026 — dispatch-index status field + filename identity
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: PR-003
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
H20: index entries carried no status field (fast-path couldn't distinguish STUBs) and were keyed
by the internal `# PROGRAM:` header name — a match on a mismatched name tried to EXEC a path that
doesn't exist. H21's residue (docstring + degraded-record labeling) already landed in PR-003;
synapse-infer's zero-caller status is PR-028 liveness-lint territory.

## Changes
- dispatch_index.py: identity = filename stem (the EXEC-able name; differing header names kept as
  header_name); status parsed (default ACTIVE); STUB/DEPRECATED/ARCHIVED excluded from the index
  outright with an explicit excluded list in the rebuild report (no silent caps).
- DISCOVERY: the first rebuild excluded 11 — 7 were IMPLEMENTED programs whose synapse headers
  rotted to STUB in the bulk migration (one says "Status: ACTIVE" in its own body; all are routed
  since PR-011). Headers corrected: meta-board/dispatch-stats/igap/usage, rules-audit,
  events-emit, phase-new. The 3 genuine PR-119 stubs + quarantined finalize stay excluded.
- Live index rebuilt: 166 entries, 4 excluded, 0 header mismatches.
- tests/test_dispatch_index_status.py: identity/status pins, exclusion+report pins, live-index
  invariant, header-rot regression pin.

## Acceptance: full suite green (04-log).
