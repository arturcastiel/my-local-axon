# PR-009 — Rewrite library-dev.canonical.yml against library-dev's real programs
Project:     axon-bugfix01
Created:     2026-07-03
Complexity:  M
Depends on:  PR-005, PR-006, PR-007
Status:      implemented
AXON score:  8/10
User score:  8/10 (standing, owner-delegated 2026-07-03)

## Summary
C8: version 1 was an almost-verbatim copy of code-dev.canonical.yml — all 8 synapses named
code-dev-* programs; `workflow run library-dev` either failed at s1 (code-dev-study requires
W:code-dev-project) or silently ran an entire code-PR pipeline under the library-dev label,
contradicting the domain manifest ("flat container — no phases, no PRs").

## Changes (as implemented)
- library-dev.canonical.yml v2: 6-synapse linear pipeline over library-dev's OWN programs —
  new → ingest → explain → intersect → report → status. search/cite stay interactive utilities.
  Gates deliberately honest: no invented predicates (the C1 disease) — generic goal acceptance +
  steps-bound rejection backstop; every synapse verified to exist as a real program file.
- _index.md description corrected (was describing code-dev's flow in code-dev vocabulary).
- tests/test_library_dev_workflow.py: domain-purity pin (every synapse must be library-dev-* AND
  a real program file — the C8 regression trap), schema+reachability pin, vocabulary pin.

## Acceptance Criteria
- [x] Every synapse names an existing library-dev-* program
- [x] Schema-valid; no dangling on-complete targets; both repo lints 0 findings
- [x] All gate expressions evaluate error-free (registered vocabulary only)
- [x] Full repo pytest suite green (counts in 04-log)
