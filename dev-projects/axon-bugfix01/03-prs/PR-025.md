# PR-025 — Cron reconciliation
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
H6 (live-failing at fix time, 2 consecutive): CRON-003 ran bare `self-care` — the tool requires a
{check,report} subcommand. H7: lint-code-weekly coded in DEFAULTS, never seeded live. H13 (reverse
drift): axon-freshness-weekly live-default but absent from DEFAULTS — a rebuild would drop it.
H14: DEFAULTS' memory-compact entry referenced a tool name that doesn't exist.

## Changes
- Live cron.json: CRON-003 → `self-care report` (verified runs, rc=0); breaker counter reset with
  an honest pending-retry note. lint-code-weekly seeded via seed-defaults (slot moved to 09:45 to
  make room for freshness at its live 09:35 time).
- DEFAULTS: memory-compact → the real program path; axon-freshness-weekly added.
- tests/test_cron_reconciliation.py: the BIDIRECTIONAL drift guard (every DEFAULT seeded live;
  every live default job present in DEFAULTS) — H7/H13 become a test failure as a class — plus
  the subcommand pin and a live self-care execution check.

## Acceptance: full suite green (04-log).
