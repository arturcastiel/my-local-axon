# PR-022 — Fix simulate .md↔CLI contract
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
C13: the safety command menu.md repeatedly tells users to run before anything irreversible failed
with a CLI error on EVERY input — simulate.md passed a bare positional path + a verbosity flag
(neither exists; the tool takes --program <name>), and its report read output fields
(ops_total/agent_ops/tool_calls/queries_needed/l_changes/file_writes) that were never in the
tool's return schema.

## Changes
- simulate.md: real call (--program {target}); report rebuilt from the REAL schema — total_ops,
  op_counts breakdown, tools_called (stubbed), QUERY count, and the safety core: the
  irreversible_ops list rendered loudly (clean-run line otherwise); verbose = render
  shadow_memory_final (a program-side decision, no tool flag); the report json is persisted by
  the program (the tool stays write-free) to workspace/memory/working/sim/.
- program_tool_conformance SCOPE_GLOBS += simulate.md (C13 IS the md↔CLI drift class —
  same silent-scope-exclusion story as orchestrator.md).
- tests/test_simulate_contract.py: live run-by-name, real-contract pins, phantom-field pins,
  scope pin. Lesson repeated: explanatory comments must not contain literal dead flags.

## Acceptance: simulate runs end-to-end by name; 0 conformance violations; full suite green (04-log).
