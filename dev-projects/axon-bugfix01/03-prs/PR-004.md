# PR-004 — Make the orchestrator fire step real; conformance covers orchestrator.md
Project:     axon-bugfix01
Created:     2026-07-03
Complexity:  S
Depends on:  PR-003
Status:      not-started
AXON score:  8/10
User score:  8/10 (standing, owner-delegated 2026-07-03)

## Summary
S2 (c): the fire block calls `TOOL(dispatch, match, "--query {top.name} --top 1")` — dispatch.py
match has no `--top` flag (live-reproduced today: argparse error), the result JSON has no `ok` key
(the assert can never pass), and no `EXEC()` exists anywhere in the block — "firing" is telemetry +
a doomed assert. The lint that catches .md↔CLI drift excludes orchestrator.md from its scope.

## Re-verification against HEAD (2026-07-03)
- orchestrator.md:184-190 — confirmed verbatim.
- `dispatch.py match --help` → no --top; real output keys: {action, program, confidence, threshold,
  id, run, top_matches, latency_ms}. CONFIRMED live.
- program_tool_conformance.py SCOPES (lines ~50-66) — workflow-*/goal-*/chat-*/plan-*/library-dev-*
  globs; orchestrator.md absent. CONFIRMED.

## Entry Conditions
- PR-003 merged (top.name is now a real program name with calibrated confidence).

## Changes Required
### workspace/programs/orchestrator.md
- **What:** Rewrite the ACT fire branch: resolve `workspace/programs/{top.name}.md` — exists →
  `EXEC()` it directly (top.name IS a program post-PR-003). Else fall back to
  `TOOL(dispatch, match, "--query {top.name}")` (real flags), and fire `EXEC({match.program path})`
  only when `match.program ≠ ∅ AND match.confidence ≥ match.threshold`; otherwise LOG(WARN) +
  downgrade to the ask branch (never a silent no-op, never a doomed assert). Replace the
  `result.ok` assert with checks on fields that exist. Annotate both synapse-suggest rank calls
  with `--corpus programs` (explicit, PR-003 default made visible).
- **Why:** A "fire" decision must actually invoke the chosen program or honestly ask.

### tools/program_tool_conformance.py
- **What:** Add ("orchestrator", "workspace/programs/orchestrator.md") to SCOPES so this exact
  drift class is mechanically caught next time.
- **Why:** The audit's regression trap: the lint was clean only because it never looked here.

### tests
- **What:** Conformance-scope test asserting orchestrator.md is scanned and currently clean
  (post-fix); a fixture test that the rewritten fire block's TOOL(dispatch, match) call site passes
  the conformance checker (no unknown flags).

## Architecture Impact
Fire becomes: rank (programs corpus, calibrated) → threshold via inference-mode → EXEC the real
program file. The adaptive layer becomes capable of executing its own top suggestion for the first
time; C4's workflow fix (PR-008) builds directly on this.

## Acceptance Criteria
- [ ] program-tool-conformance report scans orchestrator.md with 0 violations post-fix
- [ ] Fire branch contains a real EXEC path + honest fallback; no `--top`, no `.ok` assert
- [ ] Full repo pytest suite green (full counts reported)

## Risks & Gotchas
- ⚠ Keep bridge-mode semantics intact (workflow-run owns dispatch; fire must stay skipped in bridge-mode).
- ⚠ dispatch match latency (~4s observed) — acceptable on the rare fallback path only; the primary path is a file-existence check.

## Files Analysed (shadow index)
- workspace/programs/orchestrator.md · tools/dispatch.py (CLI) · tools/program_tool_conformance.py

## Implementation Notes
_To be filled during Phase 4._
