# PR-027 — Misc verified breakage sweep
Project:     axon-bugfix01 · Created: 2026-07-07 · Complexity: S · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary + changes (six independent fixes, each pinned)
- H22 axon-compare: read the REAL 1b schema (the compilation key was deleted from axon_audit
  2026-06-10; the compare broke on undefined values) — rebased to structural/usage/usefulness
  scores; a live test asserts 1b carries every field the compare reads.
- H23 crucible: register/status performed the side-effect/fetch but rendered NOTHING — output
  branches added for both.
- H24 handoff: {today}/{now} used throughout with no assignment anywhere — defined from
  TOOL(clock); the log-excerpt read is a fall-soft chain (my-axon log → workspace log → honest
  placeholder).
- H25 migrate-workspace: step 4's answer was never checked — a failed push fell through to a
  receipt unconditionally stamped complete. error → FAIL; skip-push → an honest receipt status.
- H26 harness-builder: bare {i} in the criteria loop (the only such case in the corpus) emitted
  literal criterion_{i} text in every generated harness — ENUMERATE-bound.
- M5 orchestrator fixed-branch: read a steps[] map no real workflow YAML has — null-safe
  resolution over the real synapses/on-complete schema.

## Acceptance: full suite green (04-log).
