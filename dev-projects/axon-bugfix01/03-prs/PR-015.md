# PR-015 — Queue integrity: pop, deps, clear semantics
Project:     axon-bugfix01 · Created: 2026-07-03 · Complexity: M · Depends on: none
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
C10 (behavioral core), all empirically confirmed: pop permanently lost a task (removed from
active, never re-inserted; complete searched active only); --deps stored but never read (a task
depending on an incomplete task popped immediately); clear wiped active and kept completed
history — the opposite of documented session-end semantics.

## Changes
- queue_tool.py: in_progress list (non-lossy pop; legacy queue.json normalized on read);
  dep-aware pop eligibility (+blocked_on_deps count); complete searches active+in_progress;
  new `requeue` recovery action; clear --scope {completed(default)|active|all}.
- workspace/scheduler/QUEUE.md: honest pointer/contract doc (tables were never tool-written).
- tests/test_queue_integrity.py: 8 tests incl. the exact original-bug shapes and legacy-shape load.

## Acceptance: pop→complete round-trips; unmet-dep never pops; priority+FIFO preserved;
clear default = session-end semantics; full suite green (04-log).
