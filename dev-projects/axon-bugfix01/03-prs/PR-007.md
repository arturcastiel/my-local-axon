# PR-007 — Wire check-stale + check-templating into workflow validate
Project:     axon-bugfix01
Created:     2026-07-03
Complexity:  S
Depends on:  none
Status:      implemented
AXON score:  8/10
User score:  8/10 (standing, owner-delegated 2026-07-03)

## Summary
H5: the two lints purpose-built to catch the C1/C4/C8 workflow-death class existed, unit-tested
and CLI-exposed, but workflow-validate.md called neither. Also: check-stale WHITELISTED tool-named
synapses as legitimate — which is exactly why it could never have caught C4 (adaptive-free-text s1
names the TOOL synapse-suggest; workflow-run dispatches PROGRAMS via EXEC).

## Changes (as implemented)
- workflow_run.py check_stale: the tool-name whitelist branch now emits a DISTINCT finding kind
  `tool-not-program` (the tool exists; the wiring is undispatchable). missing-neuron unchanged.
- Both lint CLIs gain `--target` (basename/path-suffix filter) so validate stays per-file.
- workflow-validate.md: runs both lints after the graph checks; severity mapping —
  missing-neuron/tool-not-program/templating → ERROR; stub-neuron → WARN (grandfathered).
- tests: validate-wiring pins, --target scoping, C4-shape fixture; the old
  "tool-backed synapse must not be flagged" test rewritten to assert the new distinct kind
  (its original point — not-a-MISSING-neuron — preserved).

## Live verification
`check-stale --target adaptive-free-text.yml` → 1 finding: s1 tool-not-program synapse-suggest —
the lint now sees C4's exact bug (fixed next in PR-008).

## Acceptance Criteria
- [x] workflow validate reports lint findings with the severity mapping
- [x] tool-named synapse → tool-not-program (distinct from missing-neuron)
- [x] Full repo pytest suite green (counts in 04-log)
