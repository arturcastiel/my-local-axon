# PR-013 — Router debt cleanup
Project:     axon-bugfix01 · Created: 2026-07-06 · Complexity: M · Depends on: PR-011
Status:      implemented · AXON 8/10 · User 8/10 (standing)

## Summary
Five verified debt items behind the router: H8 (lifecycle init = copy-paste of load), H9 (whatif
EXEC'd the raw cmd word — `whatif review` silently ran a real-but-wrong program), H10 (two ALIAS
stubs live-routed past their own "removed next release" deadline), H11 (migrate unrouted +
non-grammar positional args + malformed TOOL-in-EXEC), H12 (finalize permanently dead — its
precondition key is never set anywhere).

## Changes
- H10: both stubs DELETED (their headers pre-announced removal); router entries `preflight`/
  `reviewer` retargeted to the real programs; umbrella EXECs (safety/knowledge hubs) retargeted.
- H8: lifecycle init → code-dev-new (the v4 scaffolder).
- H9: whatif resolves through a canonical target-map (23 entries, all verified on disk) +
  FILE-EXISTS FAIL — never a silent mis-execution.
- H11: migrate routed (`code-dev migrate`); rewritten to W-key/arg-bridge inputs and a real
  TOOL(migrate_meta) call (registry-exact name).
- H12: finalize marked DEPRECATED + QUARANTINE.md entry (owner-gated deletion, M9 precedent).
- Compiled code-dev regenerated (TOC fixpoint). 5 pin tests added.

## Acceptance: stubs gone + routes live; whatif map complete-and-existing; migrate grammatical;
finalize quarantined; full suite green (04-log).
