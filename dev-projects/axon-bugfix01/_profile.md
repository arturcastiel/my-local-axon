# Project profile — axon-bugfix01

linter:        (set per project: clang-format / black / etc.)
changelog:     CHANGELOG.md
reviewers:     []   # GitHub handles, e.g. ['alice', 'bob']
cross-repo:    []   # sibling repo paths
test-cmd:      (set per project)
build-cmd:     (set per project — HUMAN runs, agent never executes)
