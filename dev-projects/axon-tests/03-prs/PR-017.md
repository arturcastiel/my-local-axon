# PR-017 — Tool-level coverage gap fills
Project:     axon-tests
Created:     2026-05-16
Complexity:  M
Depends on:  PR-002
Status:      not-started
AXON score:  7/10

## Summary
Use the PR-002 coverage report to find tools below the 80 % line gate
and add targeted cases. At minimum: cron tick, prompt-log
non-blocking BG, turn-log append, output-layer footer render.

## Goal Context
F-3 closure on the tools side. The coverage gate from PR-002 names
the hot spots; this PR is the response.

## Entry Conditions
- PR-002 merged (coverage report generated in CI).
- The post-PR-002 coverage report identifies the actual hot spots.
  (If those differ from F-3's preview, follow the report.)

## Changes Required

### tests/test_tools_kernel.py
- **What:** Extend with cases for `cron_tick`, `output_layer.render_
  footer`, and any other kernel tool below 80 %.

### tests/test_tools_core.py
- **What:** Extend with cases for `prompt_log` non-blocking BG path,
  `turn_log.append` edge cases, plus any other core tool below 80 %.

### tests/test_tools_gaps.py  (optional new)
- **What:** Only if extending the existing files crosses the 800-
  line cap noted in CONTRIBUTING-style conventions — otherwise keep
  the cases co-located with their tool's existing test file.

### workspace/AXON-DOCS-*.md (per affected subsystem)
- **What:** Add Guarded-by rows where the new tests pin behaviour.

## Architecture Impact
None.

## Acceptance Criteria
- ✓ Coverage gate (PR-002) reports `tools/` ≥ 80 % overall and
  every previously-flagged file ≥ 80 %.
- ✓ ≥15 new cases land; all pass.
- ✓ Each added case has a Guarded-by reference in the relevant doc.

## Risks & Gotchas
- ⚠ Some hot spots are platform-conditional (cron). Skip on
  unsupported OS with a clear marker; do not silently exclude.
- ⚠ Non-blocking BG paths need careful tear-down to avoid leaked
  threads — use the existing harness fixtures.

## Tests Added
≈15.

## Doc Anchor
- One AXON-DOCS-* page per affected subsystem.

## Rollout / Rollback
- Rollout: merge after PR-002.
- Rollback: revert test additions.

## Out of Scope
- Rules tools (covered in PR-009/PR-011).
- Workflow harness (PR-005).
