# Phase: 1-foundations
schema-version: v4
status:         active
workflow-step:  build
branch:         main
current-pr:     (none)
created:        2026-06-03

## Working Context
First phase. The methodology study is already done and seeded in `01-study.md` (this dir) — refine it
with `code-dev study`, then `code-dev plan` to produce the PR breakdown. Phase goal: adopt the two free
laws (anti-masking + regression-lock-before-fix) as enforced artifacts, and design + prototype the
deterministic program harness on ONE flagship program, reproducing the 5 session mechanical bugs as
red→green regression tests (proof-of-thesis). See masterplan.md for the full phase graph + acceptance.
