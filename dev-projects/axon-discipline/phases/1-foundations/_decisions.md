# Decisions (ADRs) — 1-foundations

_No decisions recorded yet. The first ones to capture during `code-dev plan`:_
- ADR-001 (TBD): the harness's mechanical-op coverage — which AXON-LANG ops it interprets vs. treats as
  judgment/opaque (template expansion, EXTRACT/REPLACE, STORE/RETRIEVE, TOOL/EXEC routing, pure IF).
- ADR-002 (TBD): the tool-mock + judgment-stub contract — how a test supplies tool return values and
  pins judgment-point outputs (reuse / extend `tests/fixtures/programs/*/responses.jsonl`).
- ADR-003 (TBD): which flagship program is the phase-1 prototype target.
