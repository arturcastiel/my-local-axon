# PR-001 — Gate rule: code-change ⇒ on-workflow (the teeth)

- **Status:** spec
- **Phase:** 1-safety-contract
- **Complexity:** M
- **Depends on:** none
- **Why:** Criterion-zero — make off-workflow code work *impossible*, not merely discouraged. Reproduces and permanently locks the 2026-06-03 freelance incident. (01-study Part C, E1; ADR-005, ADR-008.)

## Goal
A crucible changeset rule that flags (WARN→BLOCK) a code change that has **no active phase + covering PR
spec** in the loaded code-dev project, while staying **silent** for legitimate non-project / non-code /
exempt changes (false positives bounded by construction).

## Acceptance criteria
1. code-file changeset · project loaded · **no active phase** → violation.
2. code-file changeset · active phase · **no open PR spec** → violation.
3. code-file changeset · active phase · ≥1 open PR spec (weakest-sound coverage) → **None**.
4. **no project loaded** → None (hotfix exemption — the biggest false-positive class).
5. only non-code / project-meta / generated files changed → None.
6. `L:code-change-requires-pr-phase-required = true` → severity flips **BLOCK** (default WARN).
7. **Reproduction test** — replay this session: ctx of "`tools/x.py` added · project loaded · no active
   phase / no PR spec" → violation; "active phase + open spec" → None. The live bug becomes a permanent lock.

## Changes Required

### tools/rules/r_code_change_requires_pr_phase.py  (new)
- `phase = "STATIC"`, `rule_id = "R_CODE_CHANGE_REQUIRES_PR_PHASE"`, severity via `_required(ctx)`.
- `check(ctx)`: read `changed_files` + `repo_root`; `_active_project()` from
  `workspace/memory/working/code-dev-project.md` → **None ⇒ silent**; classify code files (reuse the
  `r_new_needs_test` classifier: `tools/*.py` not test_/__init__/rules, `workspace/programs/*.md` not
  compiled/help/_*, `axon/*.md` not _*, `tests/*.py`); exempt project-meta (`_meta.md`/`_phases.json`/
  `0N-*.md`/`03-prs/`) + non-code/generated; `_active_phase()` from `_phases.json`; **weakest-sound**
  coverage (active phase + ≥1 open `03-prs/PR-*.md`); else `Violation`. Helpers: `_is_code_file`,
  `_is_project_meta`, `_list_pr_specs`, `_required` (the `L:`-flag pattern from `r_workflow_node_order`).
- Status="A"-grandfather is N/A here (we check the whole code changeset), but only A/M count (skip "D").

### tests/test_r_code_change_requires_pr_phase.py  (new)
- The 7 acceptance cases above, including the reproduction. Mirror `tests/test_rules/test_r_*.py` ctx shape.

### tools/crucible.json
- Add a control (kind `rule`, severity `WARN`, phase `static`) that runs the rule via the changeset path,
  OR register the rule in the existing `changeset-rules` control's rule list. Decide in implement (prefer
  the existing changeset-rules harness — `crucible.run_changeset` already loops the rules pack).

## Test plan
`pytest tests/test_r_code_change_requires_pr_phase.py` → green; full suite green; `crucible gate` →
parse `passed` SEPARATELY before any commit. doc-anchors + freshness unaffected (no program/doc change).

## Residual / notes
- Coverage is **weakest-sound first** (active phase + ≥1 open spec); file-level coverage (changed code ⊆
  union of PR-spec `### <file>` lists) is a later refinement — ship WARN so an imperfect match never
  hard-blocks early.
- Ships WARN; flip to BLOCK in PR-007 once the backlog is clean.
- The rule reads disk (ctx carries no W: scope) — the established `r_workflow_node_order` pattern.
