# PR list — AXON HR Team
Phase 2 · 8 PRs · bottom-up ACTIVE-with-tests (ADR-009)

| PR | Title | Size | Depends | New files | Modified |
|----|-------|------|---------|-----------|----------|
| PR-001 | hr-team-selector neuron | M | — | 2 | 1 |
| PR-002 | hr-team-convener neuron | M | PR-001 | 2 | 0 |
| PR-003 | hr-team-deliberator neuron | M | PR-001, PR-002 | 3 | 2 |
| PR-004 | hr-team router neuron | M | PR-001, PR-002, PR-003 | 2 | 0 |
| PR-005 | tools/hr_team.py (CLI + run_seats seam + audit-b | M | PR-004, PR-001..003 | 2 | 2 |
| PR-006 | menu [10] HR TEAM section + dispatch-phrases + d | S | PR-004 | 1 | 3 |
| PR-007 | FULL PORT of the hr-team asset packs (catalog +  | L | PR-004 | 11 | 3 |
| PR-008 | find-program: fold # dispatch-phrases into the s | S | — | 1 | 1 |

## Detail

### PR-001 — hr-team-selector neuron — Layer-1 roster/mode/protocol/tier selection + validation (ACTIVE + router-style test)
**Goal.** Author workspace/programs/hr-team-selector.md: the Layer-1 SELECTOR neuron that, given a task + invocation mode (M1/M2/M3) + optional filters/roster/mode/protocol/size, validates the request and emits the chosen council CONFIGURATION (roster + per-seat mode + protocol + tier) plus warnings/refusals — WITHOUT calling any seat. It is the entrypoint of the hr-team program family (ADR-002 3-layer split) and the first PR of the bottom-up build (ADR-009). Lands status:ACTIVE with a static router-style contract test (tests/test_hr_team_selector.py) in the SAME change (Core Rule 13 / R_NEW_NEEDS_TEST, ADR-008/D8). It encodes the HANDOFF §4.1 SELECTOR contract: slug-existence, budget, safety, circular-call, context-overflow, no-profession-match, catalog-incomplete checks; mandatory Challenger seat on every roster (all tiers, §16.3); M3 warn-not-fix (validate explicit roster, emit warnings/refusals, never silently repair); LOW_DIVERSITY warning on same model_variant; uniform 1/N seat weights; and advisory_only:true on its output object.

- Size: M · Depends: none
- New: workspace/programs/hr-team-selector.md, tests/test_hr_team_selector.py
- Modified: workspace/programs/REGISTRY.json (regenerated by maintenance, not hand-edited — selector picked up automatically)
- Full spec → 02-phases/phase-1-*.md

### PR-002 — hr-team-convener neuron — fragment assembly + parallel sealed Round 1 orchestration via run_seats seam
**Goal.** Author workspace/programs/hr-team-convener.md as an ACTIVE markdown neuron (CONVENER layer / M2 of the 3-layer architecture) that takes the SELECTOR's roster/protocol/mode/tier decision and instantiates the council: assembles per-seat messages in the FIXED fragment order (PERSONA→GUARDRAIL→SKILLS→[de-biasing] | MODE→PROTOCOL→FORMAT | TASK) split across system/developer/user roles per HANDOFF §4.2/§9.2, narrates the tier pick + announces-then-acts (30s objection window, cancel-only), runs parallel SEALED Round 1 with NO cross-seat leakage THROUGH the single run_seats(messages[])->responses[] seam (ADR-002), re-injects each seat's system prompt FRESH at the start of every round (Sydney/§13.5 mitigation), and assigns per-seat model-tier (Haiku 4.5/Sonnet 4.6/Opus 4.8) + reasoning effort for intra-harness diversity recording model_variant per seat (ADR-006). The convener is opinion-neutral and has NO write/tool/decision authority; it emits the per-round transcript for the DELIBERATOR and never aggregates or editorialises. Ships with static router-style tests + a fragment-order assertion test (Core Rule 13 / ADR-009).

- Size: M · Depends: PR-001 hr-team-selector (produces W:selector-result: seats[] with profession/mode/weight, protocol, tier, warnings, advisory_only) — convener consumes this object; bottom-up order means selector must exist first (ADR-009)
- New: workspace/programs/hr-team-convener.md, tests/test_hr_team_convener_router.py
- Modified: —
- Full spec → 02-phases/phase-2-*.md

### PR-003 — hr-team-deliberator neuron — schema-validate seats, BPC + WSV aggregation, dissent re-round, emit §4.3 advisory verdict
**Goal.** Author workspace/programs/hr-team-deliberator.md — Layer 3 of the hr-team council (the aggregation layer). It consumes the per-round/per-seat transcript produced by hr-team-convener (PR-002), schema-validates each seat's {reason,answer,confidence} JSON (one retry → seat-failure event), runs Balanced Position Calibration (randomized + reversed aggregation order, flag order sensitivity), applies Weighted Score Voting with 1/N (Brier-updatable) weights, classifies each dissent SUBSTANTIVE vs PREFERENTIAL, triggers a re-round ONLY on SUBSTANTIVE dissent, and EMITS the canonical HANDOFF §4.3 verdict object with advisory_only:true and verdict_distribution ALWAYS present, in opinion-neutral wording. Ships status:ACTIVE WITH its contract test in the same change (ADR-009 / Core Rule 13). No write/tool authority except participating in the per-call audit bundle.

- Size: M · Depends: PR-001 hr-team-selector (provides seat list + per-seat initial weights + protocol + advisory_only in W:selector-result), PR-002 hr-team-convener (provides the per-round/per-seat transcript in W:council-transcript with each seat's raw {reason,answer,confidence[,option_scores,dissent_class]} JSON, model_variant, round index, and emits seat_responded/protocol_completed events)
- New: workspace/programs/hr-team-deliberator.md, tests/test_hr_team_deliberator_contract.py, workspace/hr-team/handoff/fixtures/deliberator-verdict-01.json
- Modified: workspace/programs/REGISTRY.json (AUTO-GENERATED — regenerate via tools/programs_registry.py generate, do NOT hand-edit), workspace/DOC-INDEX.md (AUTO-GENERATED — regenerate via tools/doc_index.py)
- Full spec → 02-phases/phase-3-*.md

### PR-004 — hr-team router neuron — standalone + workflow-embeddable advisory-council dispatcher
**Goal.** Author workspace/programs/hr-team.md: the ACTIVE router neuron of the hr-team program family. It carries the synapse header (domain deliberation, family [council,advisory,decision-support], role router/mutator, invocation_source [program,workflow]), parses the M1/M2/M3 arg surface into working memory, owns the subcommand routing table, drives the mandatory pipeline EXEC(hr-team-selector) → EXEC(hr-team-convener) → EXEC(hr-team-deliberator), STOREs the canonical HANDOFF §4.3 verdict to W:hr-team-result, and enforces the advisory_only boundary on BOTH call paths (standalone axon.py + workflow-embed H4 guard). Lands status:ACTIVE with a static router regression test (Core Rule 13 / ADR-009), depending on its three layer neurons (PR-001/002/003) already existing.

- Size: M · Depends: PR-001 (hr-team-selector neuron — produces W:selector-result with seats/protocol/advisory_only), PR-002 (hr-team-convener neuron — consumes W:selector-result, calls the run_seats(messages[])->responses[] seam, produces W:council-transcript), PR-003 (hr-team-deliberator neuron — consumes W:council-transcript, aggregates to W:deliberator-verdict per HANDOFF §4.3)
- New: /home/arturcastiel/projects/new-axon/axon/workspace/programs/hr-team.md, /home/arturcastiel/projects/new-axon/axon/tests/test_hr_team_router.py
- Modified: —
- Full spec → 02-phases/phase-4-*.md

### PR-005 — tools/hr_team.py (CLI + run_seats seam + audit-bundle writer) + tools/REGISTRY.json entry
**Goal.** Ship the thin AXON tool layer for hr-team (ADR-002 D1): an argparse CLI that parses the full M1/M2/M3 invocation surface, emits a §4.3-shaped advisory verdict JSON with advisory_only:true + verdict_distribution, houses the ONE swappable seam run_seats(messages[])->responses[] (harness sub-agent fan-out backend now), and writes the per-call audit bundle to my-axon/hr-team/councils/{call-id}/ in 3 v1 persistence modes (ADR-008). Register it in tools/REGISTRY.json using the REAL AXON entry schema, and land the contract test in the same change (Core Rule 13 / R_NEW_NEEDS_TEST). This is the CLI entry + future-portability quarantine point: a provider-API/MCP backend can later replace the fan-out WITHOUT touching the selector/convener/deliberator neurons.

- Size: M · Depends: PR-004 (workspace/programs/hr-team.md router neuron) — provides the program-file reference to tools/hr_team.py that satisfies the liveness orphan gate (surface #1 program/import/dispatch); if PR-004 has not yet wired the reference at merge time, grandfather hr-team in tools/liveness-allow.txt as an interim (the test pins the registry entry)., PR-001..003 (hr-team-selector/-convener/-deliberator neurons) — define the SELECTOR/CONVENER/DELIBERATOR logic the seam feeds; the tool's run_seats seam is the execution substrate those neurons call. The tool does NOT re-implement council logic in Python (ADR-002 rejects option C).
- New: tools/hr_team.py, tests/test_hr_team_contract.py
- Modified: tools/REGISTRY.json, tools/liveness-allow.txt (CONDITIONAL — only if PR-004's program-file reference to tools/hr_team.py is not present at this PR's merge; remove the grandfather line once PR-006 wiring lands)
- Full spec → 02-phases/phase-5-*.md

### PR-006 — menu [10] HR TEAM section + dispatch-phrases + dispatch-index rebuild
**Goal.** Make the hr-team council discoverable through both AXON routing surfaces without any kernel edit: (1) add an ADDITIVE "HR TEAM" menu SECTION block at the next free number [10] in workspace/programs/menu.md, modeled exactly on the CODE DEVELOPMENT block (L263-276), with every existing section left byte-intact (Core Rule 12); (2) add a "# dispatch-phrases:" line to the hr-team.md router header (built by PR-004) carrying council vocabulary for TF-IDF smart-dispatch; (3) ensure that same council vocabulary ALSO lives in hr-team.md name/desc/PURPOSE because in-mode find-program does NOT read phrases (ADR-007; phrases-extension is the separate PR-008); (4) rebuild the smart-dispatch index from program source via tools/dispatch_index.py rebuild so the new phrases enter the match corpus. This is the discoverability/routing wiring layer of the ADR-009 bottom-up sequence (D2/ADR-003: harness section, NOT a kernel mode).

- Size: S · Depends: PR-004
- New: tests/test_hr_team_menu_section.py
- Modified: workspace/programs/menu.md, workspace/programs/hr-team.md, workspace/memory/longterm/dispatch-index.json
- Full spec → 02-phases/phase-6-*.md

### PR-007 — FULL PORT of the hr-team asset packs (catalog + prompts + optional handoff) into workspace/hr-team/
**Goal.** Land the hr-team profession catalog (151 rows + _REGISTRY.md + _CONFLICT-POLICY.md) and the 69-file prompt pack (personas/protocols/tiers/modes{families,presets}) as self-contained repo assets under workspace/hr-team/ (ADR-004), so the PR1-4 neurons and PR5 tool can READ real seat/persona/tier/protocol/mode data instead of placeholders. The port is mechanical/bulk COPY + a narrow, deterministic PATH-REWRITE + a mandatory H1-fix on the catalog rows so workspace/DOC-INDEX.md indexes each role by its real name (not "L0 IDENTITY"), with slug-uniqueness preserved, all 1077 slug-only cross-links left intact, and the doc-index regenerated so the freshness gate stays green.

- Size: L · Depends: PR-004 hr-team router neuron (the asset consumer; assets are inert data without a reader, so ADR-009 bottom-up places the port after the neurons that read it)
- New: workspace/hr-team/catalog/professions/_REGISTRY.md (ported; 1 path-ref rewritten), workspace/hr-team/catalog/professions/_CONFLICT-POLICY.md (ported verbatim), workspace/hr-team/catalog/professions/{ai-ml,business,design,education,energy,humanities,legal,medicine,ops,process,science,software}/{slug}.md (151 row files; H1-fixed), workspace/hr-team/prompts/personas/{concierge,convener,panel,stealth-auto,triumvirate}.md (5; convener.md has 1 path-ref rewritten), workspace/hr-team/prompts/protocols/{adversarial,consensus,debate,delphi,prediction-market,round-robin,weighted-vote}.md (7), workspace/hr-team/prompts/tiers/{micro,low,medium,high,xhigh,full}.md (6), workspace/hr-team/prompts/modes/families/F1-context..F6-voice/*.md (31 atomic modifiers), workspace/hr-team/prompts/modes/presets/*.md (20 presets), workspace/hr-team/handoff/* (OPTIONAL — 6 .md + manifest.json with output/ refs rewritten; checksums.sha256 regenerated or omitted), tests/test_hr_team_port_integrity.py (file-count + H1 + cross-link-resolver + slug-uniqueness + doc-index-freshness checks), tools/hr_team_port.py (OPTIONAL deterministic porter: copy + H1-fix + path-rewrite, re-runnable; or inline the transform in the test fixtures if a script is over-engineering for a one-shot)
- Modified: workspace/DOC-INDEX.md (regenerate via tools/doc_index.py export --canonical — now lists ~222 new workspace/hr-team/**/*.md entries; the freshness 'doc_index' gate drifts until this is regenerated), workspace/programs/REGISTRY.json (regenerate via tools/programs_registry.py generate — NO-OP for asset files since it globs workspace/programs/*.md requiring a '# PROGRAM:' header; run for commit honesty / to absorb any drift from sibling PRs), memory/longterm/dispatch-index.json (rebuild via tools/dispatch_index.py rebuild — also NO-OP for asset files, same programs/*.md scope; run to keep the maintenance-artifact set consistent)
- Full spec → 02-phases/phase-7-*.md

### PR-008 — find-program: fold # dispatch-phrases into the scoring corpus (repo-wide routing-vocabulary unification)
**Goal.** Extend the find-program neuron so its match corpus ALSO includes each program's `# dispatch-phrases:` line — the same opt-in routing vocabulary smart-dispatch already indexes (dispatch_index._PHRASES) and in-mode routing reads — so a query phrased in natural user vocabulary (e.g. "close out this task") surfaces the right program in find-program too, not only in smart-dispatch. This closes the latent footgun ADR-007 names: routing vocabulary currently must be duplicated into name/desc/PURPOSE for find-program to see it (ADR-007 rejected that local workaround explicitly). Ships as its OWN PR with its OWN test and review, fully decoupled from the axon-hr build (ADR-007 + D6: "SEPARATE small PR, NOT bundled with the hr-team build, keeps reviews decoupled").

- Size: S · Depends: none
- New: tests/test_find_program_phrases.py
- Modified: workspace/programs/find-program.md
- Full spec → 02-phases/phase-8-*.md

## Plan amendment — 2026-06-18 (open-items resolved, ADR-010/011)
PR-007 split → PR-007a (catalog/risk) · PR-007b (prompts) · PR-007c (handoff, optional).
PR-009 ADDED (closeout, fullest docs/wiki — ADR-011): wiki/hr-team.md + hr-team-catalog.md +
  hr-team-recipes.md + INDEX + getting-started blurb + AXON-DOCS/DOC-INDEX regen + CHANGELOG;
  satisfies test_wiki + test_freshness_wiki. Merged LAST.
Final PR order: PR-001 selector · 002 convener · 003 deliberator · 004 router · 005 tool/seam ·
  006 menu[10]+dispatch · 007a catalog · 007b prompts · (007c handoff) · 008 find-program(decoupled) ·
  009 docs/wiki(last). PR-008 orderable anytime (depends_on=[]).
