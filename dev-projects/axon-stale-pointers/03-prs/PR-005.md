# PR-005 — Repair stale records + docs/changelog
Project:     axon-stale-pointers
Created:     2026-07-09
Complexity:  S
Depends on:  PR-001
Status:      not-started
AXON score:  8.5/10
User score:  (pre-approved: "continue until the end")

## Summary
Bring the whole estate to zero sweep findings, honestly: mechanical repairs where
unambiguous, recorded --force retro-stamps where project logs prove delivery,
real artifacts where the phase genuinely ran (axon-obsidian's audit DID happen —
this morning, in session — so its 05-audit.md gets written, not forced).

## Repair rules (per finding class)
- meta-behind (7): advance _meta.phase to the first non-done manifest phase.
- split-brain (5): the unresolvable ids are real custom phases that were never
  registered — phase-model add each (deps = preceding ladder), preserving intent;
  where the id is vocabulary drift, amend _meta.phase to the canonical id instead.
- complete-claim-unbacked (6 + obsidian): where 04-log.md / 02-prs.md record
  delivery → phase-model done --force per pending phase (force is RECORDED — the
  deviation trail is the point) + a one-line retro-stamp note in the project's
  04-log.md. axon-obsidian: write 05-audit.md from this morning's verified audit
  (5 PRs vs repo vs remote), then done WITHOUT force.
- test-record-stale (1): resolved by PR-004 at the next full run — no manual edit.

## Changes Required
- my-axon/dev-projects/*: _meta.md / _phases.json / 04-log.md per rules above
  (my-axon layer — not in axon.git; changes ride the my-axon backup)
- CHANGELOG.md: entry for the sweep + gate fixes + completion discipline
- verify: self-care check → pointers ok (except test-record-stale until next gate)

## Acceptance Criteria
- [ ] sweep findings: 0 (modulo the PR-004-resolved freshness item)
- [ ] every --force retro-stamp has a rationale line in that project's 04-log.md
- [ ] axon-obsidian audit phase done via a REAL 05-audit.md, no force
- [ ] CHANGELOG.md updated

## Risks & Gotchas
- ⚠ retro-stamping must never invent completion — only where the project's own
  records prove delivery; anything ambiguous stays pending and listed
- ⚠ my-axon is the private layer: repairs there push via workspace-backup, not axon.git
