# PR-003 — Loud completion: `code-dev complete` + handoff escalation
Project:     axon-stale-pointers
Created:     2026-07-09
Complexity:  M
Depends on:  none
Status:      not-started
AXON score:  9/10
User score:  (pre-approved: "continue until the end")

## Summary
Make "complete" a gated claim: a `code-dev complete` closeout route that refuses to
write status: complete until the manifest is all-done, and escalation of best-effort
`outputs-missing` refusals from LOG(ERROR) to a rendered human-handoff at all five
phase-advance call sites.

## Changes Required
### workspace/programs/code-dev.md
- **What:** new route `IF cmd ≡ "complete"` — load manifest; IF every phase done →
  write _meta status: complete (phase stays at last ladder id) + stamp
  W:active-phase {project}:done + render "✓ COMPLETE (manifest-backed)"; ELSE →
  render the blocking phases with per-phase reason (missing outputs listed via
  phase-model) and DO NOT write. No force path here — force stays per-phase
  (`code-dev skip`), recorded.
- **Why:** the axon-obsidian class ("prose complete, manifest pending") becomes
  impossible to write through the workflow.

### 5 call sites (code-dev-study.md ×2, code-dev-plan.md, code-dev-pr-create.md,
### code-dev-journal-log.md, code-dev-safety-audit.md)
- **What:** after `IF adv.ok ≠ true AND reason_code ≡ "outputs-missing"` add a
  rendered escalation line + TOOL(human-handoff…) so the refusal reaches the USER,
  not just the log.
- **Why:** the silent-pending killer — the gate already fired; now it is heard.

### tests/test_code_dev_complete_route.py (new)
- Conformance greps (house pattern, cf. test_resweep_program_subcommands):
  complete route present in code-dev.md; refuses-write branch present; all five
  call sites carry the escalation marker; premature-done note: pr-phase glob
  contract documented at the route (deviation 2026-07-09).

## Acceptance Criteria
- [ ] `code-dev complete` route exists: manifest-gated write, loud refusal
- [ ] all 5 best-effort sites escalate outputs-missing visibly
- [ ] conformance tests green

## Risks & Gotchas
- ⚠ markdown programs are agent-interpreted — conformance tests pin the TEXT
  contract; behavior remains protocol-level until a host hook enforces it
- ⚠ do not touch the `done`/`skip` routes' semantics (force stays per-phase, recorded)
