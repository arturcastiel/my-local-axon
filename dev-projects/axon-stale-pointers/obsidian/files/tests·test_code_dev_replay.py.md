---
tags: [code, file]
path: tests/test_code_dev_replay.py
---

# tests/test_code_dev_replay.py

> 11 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `Matches must accumulate over phases, not overwrite to the last phase.`
- `Regression: `code-dev replay <PR-N>` reverse-lookup must not attribute another P`
- `Return the lines of the per-PR branch (from `IF pr` up to its `DONE`).      Scop`
- `The `| rounds` fallback (the foreign-round leak) must be gone.`
- `With accumulation `found` is a list; the header must not deref found.phase.`
- `_per_pr_block()`
- `test_code_dev_replay.py`
- `test_found_accumulates_across_phases()`
- `test_header_does_not_assume_single_phase()`
- `test_no_rounds_fallback_on_pr_filter()`
- `test_program_exists()`

## Depends on
- (none)
