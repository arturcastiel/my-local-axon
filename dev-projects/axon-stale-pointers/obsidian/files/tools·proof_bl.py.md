---
tags: [code, file]
path: tools/proof_bl.py
---

# tools/proof_bl.py

> 28 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `A CORRECT entropy-stable reference solver (global Rusanov / Lax-Friedrichs) — th`
- `Analytical derivative f'(S).`
- `Buckley-Leverett fractional flow.`
- `Closed-form S(x,t): rarefaction f'(S)=x/t behind the shock (S from 1 at the inle`
- `Grade the Rusanov reference solver for each seed (must PASS) — proves analytical`
- `Largest x where S exceeds half the shock height — the numerical front location.`
- `Return (goal_spec_without_analytical, M) — the agent-visible Buckley-Leverett ta`
- `Root of a continuous g on [a,b] with a sign change. Robust to either orientation`
- `Run the produced solver at each N, compare to the analytical profile in L1, and`
- `Shock speed = f'(S_f) (= f(S_f)/S_f by the tangent condition).`
- `Welge front saturation S_f: the tangent from the origin, f'(S_f) = f(S_f)/S_f, i`
- `_bisect()`
- `_f()`
- `_fp()`
- `_numerical_front()`
- `analytical_profile()`
- `build_parser()`
- `cmd_manufacture()`
- `cmd_selftest()`
- `front_speed()`
- `grade()`
- `main()`
- `manufacture_bl_1d()`
- `proof_bl.py`
- `reference_bl_solver_code()`
- `shock_saturation()`
- `t at which the shock front sits at x_front (keeps the front inside the domain).`
- `t_final_for_front()`

## Depends on
- (none)
