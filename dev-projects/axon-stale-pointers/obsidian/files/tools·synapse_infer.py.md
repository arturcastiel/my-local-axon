---
tags: [code, file]
path: tools/synapse_infer.py
---

# tools/synapse_infer.py

> 36 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `Any`
- `Build a neuron-contract record for a REGISTRY tool entry.`
- `Build a neuron-contract record for a single program file.`
- `Collapse a repeated ' AND '-joined precondition to first-seen-unique conjuncts.`
- `Derive domain from a program filename prefix.      Examples: code-dev-plan → cod`
- `Minimal indent-aware YAML subset parser (only top-level scalars + lists).      U`
- `Parse a `# synapse:` declared-override block (commented YAML-ish).      Strips l`
- `Path`
- `Pull every ASSERT(...)/GUARD(...) body with nested-paren awareness.`
- `Return the parenthesised body starting at index `start` (which must     point at`
- `Rewrite a program file's declared `precondition:` line with deduped conjuncts,`
- `Run regex scans against a program body, return raw evidence.`
- `Split a precondition on ' AND ' at paren-depth 0 AND outside any quoted string`
- `_build_inputs()`
- `_build_next_conditional()`
- `_build_outputs()`
- `_dedup_conjuncts()`
- `_domain_from_filename()`
- `_emit()`
- `_extract_asserts()`
- `_extract_balanced()`
- `_fallback_yaml()`
- `_infer_glossary()`
- `_infer_role()`
- `_infer_status()`
- `_iter_programs()`
- `_load_registry()`
- `_parse_declared_block()`
- `_read()`
- `_scan_program()`
- `_split_top_level_and()`
- `infer_program()`
- `infer_tool()`
- `main()`
- `scrub_preconditions()`
- `synapse_infer.py`

## Depends on
- [[tools·_axon_io.py]]
