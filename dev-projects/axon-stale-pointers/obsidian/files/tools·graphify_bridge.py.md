---
tags: [code, file]
path: tools/graphify_bridge.py
---

# tools/graphify_bridge.py

> 38 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `ArgumentParser`
- `Blast-radius of changing a FILE: the union of the affected sets over every node`
- `Build/update a code-only graph for a target repo. Degrades if graphify is absent`
- `Code-derived depends_on suggestions between PRs (advisory, INFERRED).      Input`
- `Detect graphify without importing it (never raises).`
- `Direct callers of a symbol (who depends on it, one hop) — an ergonomic query ove`
- `Fresh-guaranteed query dispatch (code-dev-graphify PR-02). Ensures freshness FIR`
- `Is the AEGIS `web` capability granted? (the P3 overlay's gate). False on any err`
- `Is the persisted graph fresh vs the target repo's CURRENT state? Reads the prove`
- `Node ids whose source_file matches `file_rel` (suffix-tolerant, deterministic).`
- `P3 LLM semantic overlay — INERT + AEGIS-gated + advisory-only.      Without a `w`
- `Path`
- `Reverse blast-radius over a graphify graph.json. Reads the typed `links` confide`
- `The freshness GUARANTEE (code-dev-graphify PR-02): before answering any query, i`
- `The staleness signal for a target repo (code-dev-graphify PR-01). Primary: git H`
- `_aegis_web_granted()`
- `_build_parser()`
- `_ensure_fresh()`
- `_graphify_version()`
- `_is_test_file()`
- `_load()`
- `_meta_path()`
- `_now_iso()`
- `_repo_signal()`
- `_write_provenance()`
- `affected()`
- `available()`
- `build()`
- `callers()`
- `check()`
- `file_nodes()`
- `graphify_bridge.py`
- `impact()`
- `main()`
- `pr_edges()`
- `query()`
- `semantic()`
- `staleness()`

## Depends on
- (none)
