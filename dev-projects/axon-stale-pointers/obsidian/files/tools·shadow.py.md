---
tags: [code, file]
path: tools/shadow.py
---

# tools/shadow.py

> 28 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `Append content to a named section in a shadow file.`
- `ArgumentParser`
- `Check whether a shadow exists and whether its hash matches the current file.`
- `Create a new shadow findings file skeleton for a source file.`
- `JSON view of a shadow file's header meta — the single header source     (the .md`
- `List all shadow files under a shadow directory.`
- `List files in shadow whose hash no longer matches the source.`
- `Map source file path to shadow file path.     /abs/codebase/src/payment/gateway.`
- `PR-shadow coverage report for a code-dev project.      Walks `<project>/phases/{`
- `Parse the YAML-ish header lines at the top of a findings file.`
- `Return git object hash for a file, or fall back to sha256.`
- `Stats: total, stale (hash mismatch), fresh (hash match).`
- `_git_hash()`
- `_read_shadow_meta()`
- `_section_heading()`
- `_shadow_path_for()`
- `build_parser()`
- `cmd_append()`
- `cmd_check()`
- `cmd_coverage()`
- `cmd_hash()`
- `cmd_header()`
- `cmd_init()`
- `cmd_list()`
- `cmd_stale()`
- `cmd_stats()`
- `main()`
- `shadow.py`

## Depends on
- (none)
