---
tags: [code, file]
path: tests/test_axon_path_unified.py
---

# tests/test_axon_path_unified.py

> 17 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `#11 mandated a single helper so the gates cannot drift. _axon_io, shell, and the`
- `A LEGIT relocation (boot/CI/federation) — a dir that DOES carry the sentinel — i`
- `An agent doing `AXON_ROOT=/tmp <tool>` must NOT move the kernel anchor. A dir wi`
- `F-D8-001 vec1: a symlink pointing into axon/ must not slip past (realpath follow`
- `The regression guard. From a non-root cwd, a relative `axon/X` MUST still classi`
- `Wave G / G4 — arch-audit #11: one cwd-independent axon/ path classifier.  These`
- `shell + r9 pass a workspace_root; a tmp-workspace axon/ must still be caught`
- `test_absolute_kernel_path_is_inside()`
- `test_all_three_sites_delegate_to_canonical()`
- `test_axon_path_unified.py`
- `test_axon_root_env_relocation_is_rejected()`
- `test_empty_target_is_false()`
- `test_governed_axon_root_env_is_honored()`
- `test_non_axon_relative_paths_are_outside()`
- `test_relative_axon_path_is_cwd_independent()`
- `test_symlink_bypass_is_caught()`
- `test_workspace_anchored_classification()`

## Depends on
- [[tools·_axon_paths.py]]
