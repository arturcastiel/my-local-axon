---
tags: [code, file]
path: tools/git_dag_reconciler.py
---

# tools/git_dag_reconciler.py

> 16 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `ADDITIVE-only capture (PR-007): for each CODE_FIRST_DRIFT finding (a branch with`
- `Derive a node id from a branch name: prefer an embedded PR-<id>, else the last s`
- `Path`
- `Per-node audit log (council Q3 gate 5): one JSONL line per added node.`
- `Read-only git<->DAG reconciliation. Returns {ok, findings, ...}; ok is False on`
- `_audit_apply()`
- `_branch_to_node_id()`
- `_commit_in_git()`
- `_emit_advisory()`
- `_git()`
- `_now_iso()`
- `_today()`
- `apply_additive()`
- `git_dag_reconciler.py`
- `main()`
- `reconcile()`

## Depends on
- [[tools·_axon_io.py]]
