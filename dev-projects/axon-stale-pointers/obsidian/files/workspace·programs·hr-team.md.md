---
tags: [code, file]
path: workspace/programs/hr-team.md
---

# workspace/programs/hr-team.md

> 72 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `(require_receipt ON) only when a named EXTERNAL auditor of verdicts exists + a dispatcher holds a`
- `--domain run was mislabeled M1-FULL. Store the narrowing object when any flag is set.`
- `1) SEAL the deliberator's unsealed aggregation through the authorized path.`
- `2) The REAL surface gate — verify_surfaceable (kind discriminator + advisory_only, mechanically,`
- `Advisory boundary guard — H4 L1163 promoted here so BOTH standalone and workflow-embed paths enforce it`
- `C2 fix (axon-bugfix01): persist the audit bundle through the now-registered helper.`
- `C3 fix (axon-bugfix01): the selector detects M2-FILTERED solely via W:hr-team-filter —`
- `Empty-task gate (HANDOFF §13 H1 L1061 — SELECTOR also catches this, but router guards first)`
- `GAP-001: mint the council call_id ONCE (was never minted → verdicts/receipts collided on`
- `GAP-001: the live runtime now routes through the AUTHORIZED Python enforcement (was a bare`
- `IDENTITY LOCK`
- `LOAD CONTEXT`
- `M1/M2/M3 surface — router marshals into W: keys; SELECTOR infers mode from which keys are set`
- `Marshal --context into W:hr-team-context here. SELECTOR + CONVENER read the loaded form below.`
- `OUTPUT`
- `PR-005: the program router is the SINGLE invocation entry (the CLI is a fixture, PR-004).`
- `PR-007: load+sanitize+hash the context ONCE via the deterministic helper (the convener is`
- `PR-008: resolve the F1..F6 mode (named preset OR a+b+c 6-tuple) to a canonical tuple +`
- `PR-009: deterministic keyword/domain roster pre-pass over the catalog (the selector is`
- `PROGRAM: hr-team`
- `ROUTE (explicit dispatch — no interpolated dead-routes)`
- `SEAM NOTE (ADR-002): the CONVENER (hr-team-convener) executes seats through ONE seam:`
- `SECTION:HELP`
- `SECTION:RUN`
- `TOOL-free, so the router does the load and stores the result for the convener to read).`
- `TOOL-free, so the router runs it). Unknown domain → fail-closed in the selector; weak → warn.`
- `The seam is implemented in tools/hr_team.py; the router orders the layers but`
- `Three-layer sealed EXEC pipeline (HANDOFF §13 H1 L1060-1064 — ORDER IS MANDATORY)`
- `Trailing FAIL guard for unknown subcommands`
- `W:hr-team-*     — M1/M2/M3 filter+roster options (see LOAD CONTEXT)`
- `W:task          — the advisory question to deliberate`
- `[--budget B] [--persistence M]`
- `[--roster a,b] [--mode M] [--protocol P] [--size T] [--priority P]`
- ``hr-team provenance-audit`.`
- `call_id:"unknown"). Convener + deliberator read W:hr-team-call-id.`
- `contract-version: neuron-contract v1.1`
- `desc:    Advisory professional council — selects seats, runs protocol, returns dissent-preserving verdict`
- `dispatch-phrases: convene the council · ask the panel · run it past the HR team · multi-persona review · get a second opinion from a council · deliberate this decision · advisory panel verdict`
- `domain: deliberation`
- `every --persistence full-audit run produced zero persisted, checksummed audit trail.`
- `family-fragment paths (the convener is TOOL-free, so the router resolves and stores it).`
- `family: [council, advisory, decision-support]`
- `glossary: AXON-GLOSSARY v2`
- `hr-team.md`
- `in Python). require_receipt stays OFF — in single-trust-domain use (the convener is the relying`
- `inputs-count: 3`
- `inputs:  W:hr-team-cmd   — subcommand: full | help | ∅`
- `invocation_source: [program, workflow]`
- `key the convening agent cannot read (council slate 2026-06-22). No blanket per-output banner: a`
- `messages[] shape (§4.2): per-seat [{system: PERSONA+GUARDRAIL+SKILLS},`
- `never calls the seam directly — that is the CONVENER's job.`
- `next-suggests: [hr-team-selector, hr-team-convener, hr-team-deliberator]`
- `outputs-count: 1`
- `outputs: W:hr-team-result — HANDOFF §4.3 verdict object (advisory_only:true, mandatory`
- `party) the fan-out receipt is a RUN-INTEGRITY LOG, not a security control; it earns fail-closed`
- `per round per §4.2 rule 5 / §13.5 Sydney mitigation).`
- `precondition: "L:cognition-frame ≡ \"AXON-OS\""`
- `responses[] = per-seat {reason, answer, confidence} JSON (INVARIANT)`
- `role: mutator`
- `run_seats(messages[]) -> responses[]`
- `status: ACTIVE`
- `string compare that bypassed seal_advisory_verdict + verify_surfaceable — gap-find finding).`
- `synapse:`
- `tests:   tests/test_hr_team_router.py`
- `these flags were loaded as locals and never stored, so M2 was unreachable and every`
- `usage:   hr-team [full] [--task "..."] [--domain X] [--family F] [--roles a,b] [--context C] [--persona P]`
- `v1 backend = harness sub-agent fan-out (parallel sealed Round 1; FRESH system re-injection`
- `verdict_distribution must be present (ADR-005 MANDATORY on every verdict, even unanimous)`
- `verdict_distribution, human_review_required:true); optional my-axon audit bundle`
- `warning that fires on 100% of councils is zero-signal — provenance is checkable on demand via`
- `write_audit_bundle was implemented + test-covered but unreachable from any program —`
- `{developer: MODE+PROTOCOL+FORMAT}, {user: TASK}]`

## Depends on
- (none)
