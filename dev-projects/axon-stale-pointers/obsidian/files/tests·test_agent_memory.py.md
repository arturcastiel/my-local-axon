---
tags: [code, file]
path: tests/test_agent_memory.py
---

# tests/test_agent_memory.py

> 22 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `AM1 — agent-memory machinery (axon-memory cluster).  Pins the tier routing, the`
- `Bindings carry `kind:value` tokens; the parser must keep the inner colon.`
- `Orphan filtering runs before TF-IDF, so it's verifiable without sklearn:     an`
- `The privacy guardrail: a default write must NOT land in the tracked workspace.`
- `test_agent_memory.py`
- `test_bindings_roundtrip_preserves_colons()`
- `test_capture_deterministic_writes_inline()`
- `test_capture_gate_rejects_pathlike()`
- `test_capture_judgment_stages_to_digest()`
- `test_digest_drop_removes()`
- `test_digest_keep_promotes_and_clears()`
- `test_gc_dry_run_does_not_move()`
- `test_gc_quarantines_when_enabled()`
- `test_gc_reports_orphans_default_off()`
- `test_list_entries()`
- `test_private_default_lands_in_myaxon()`
- `test_recall_orphan_excluded_from_results()`
- `test_recall_ranks_by_relevance()`
- `test_recall_skips_orphaned_entries_count()`
- `test_scoped_tiers_require_scope()`
- `test_tier_routing()`
- `test_write_parse_roundtrip()`

## Depends on
- (none)
