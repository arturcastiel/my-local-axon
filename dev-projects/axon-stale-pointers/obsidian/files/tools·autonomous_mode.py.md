---
tags: [code, file]
path: tools/autonomous_mode.py
---

# tools/autonomous_mode.py

> 28 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `Bind the active grant to a project's AUTONOMY.md: records project_dir + the doc'`
- `Fail-closed: while an unattended run is active, grant_on/bind_doctrine/renew are`
- `Is `op` on `repo` authorised by the active grant? Gated by operation RISK:`
- `Re-stamp granted_ts, extending the grant by another TTL window. HUMAN-ONLY: requ`
- `SelfMintRefused`
- `True iff an UNATTENDED autonomous run is in progress — the grant is active AND w`
- `True iff the grant is past its TTL (axon-next PR-003). granted_ts + ttl_hours vs`
- `Validate a grant's doctrine binding (axon-next PR-008): it must name a project d`
- `_audit()`
- `_audit_path()`
- `_doctrine_binding_ok()`
- `_expired()`
- `_grant_path()`
- `_now()`
- `_out()`
- `_refuse_self_mint()`
- `_resolve_myaxon()`
- `_save_grant()`
- `authorized()`
- `autonomous_mode.py`
- `bind_doctrine()`
- `doctrine-fix C3: raised when an active unattended run tries to (re)grant/rebind/`
- `grant_off()`
- `grant_on()`
- `load_grant()`
- `main()`
- `renew()`
- `run_active()`

## Depends on
- [[_unknown_]]
