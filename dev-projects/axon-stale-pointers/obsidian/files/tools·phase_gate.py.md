---
tags: [code, file]
path: tools/phase_gate.py
---

# tools/phase_gate.py

> 17 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `A file is a stub when it carries the scaffold marker, or when it is     just a m`
- `Evaluate the artifact contract for `phase` within `project_dir`.`
- `Parse `_phase-contract.md`. Bullet lines name required artifacts:         - 02-p`
- `Path`
- `Return (contract, source). Per-phase override beats the default.`
- `Show the resolved contract for a phase (debug / authoring aid).`
- `_default_workspace()`
- `_is_enforce()`
- `_is_stub()`
- `_parse_contract_file()`
- `_read_longterm()`
- `check()`
- `cmd_check()`
- `cmd_contract()`
- `main()`
- `phase_gate.py`
- `resolve_contract()`

## Depends on
- [[tools·_axon_paths.py]]
