---
tags: [code, file]
path: tools/doctrine_run.py
---

# tools/doctrine_run.py

> 20 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `Complete the current node (receipted), then resolve + advance to the next.     H`
- `Path`
- `Validate the routine against the project + arm the bound run. Fail-closed: an`
- `Write the current node's declared op-class + scope to a well-known key the shell`
- `_clear_current_node()`
- `_current_node_path()`
- `_load_state()`
- `_node()`
- `_receipt()`
- `_record_current()`
- `_save_state()`
- `_state_path()`
- `_yaml()`
- `axon/state/doctrine-current-node.json — the anchor the shell gate reads. Under a`
- `doctrine-fix M9: teardown — remove the current-node record at run end so the S7b`
- `doctrine_run.py`
- `main()`
- `start()`
- `status()`
- `step()`

## Depends on
- [[tools·_axon_io.py]]
