---
tags: [code, file]
path: workspace/preferences/tools/README.md
---

# workspace/preferences/tools/README.md

> 13 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `Available settings per tool:`
- `Controls how verbose output is handled before it hits the active context window.`
- `Each file: [toolname].md`
- `If no file exists for a tool, raw output is used (no filtering).`
- `Inspired by RTK's declarative filter pipeline.`
- `Per-tool output shaping — consulted by AXON before incorporating tool results into context.`
- `README.md`
- `TOOL OUTPUT PREFERENCES`
- `max_lines: N          — truncate output to N lines (0 = unlimited)`
- `on_empty: "message"   — output this if tool returns nothing`
- `strip_patterns: [...]  — regex patterns to strip from output lines`
- `summary_mode: true    — return only the summary/key fields, not raw output`
- `tee_threshold: N      — auto-TEE if output exceeds N lines`

## Depends on
- (none)
