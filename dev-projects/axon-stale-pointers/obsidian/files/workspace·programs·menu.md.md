---
tags: [code, file]
path: workspace/programs/menu.md
---

# workspace/programs/menu.md

> 62 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `(C5 repair: this dict header and the chat entry were lost in a migration,`
- `(Goal A lossless mandate). The RETRIEVE() lines are memory reads (free), unchanged.`
- `.text — render either shape, and take the TRUE open count from snap.todos_open (the`
- `.total is the golden-path check (the old ok-check killed this line on every boot)`
- `13 real jobs existed)`
- `Active context indicators`
- `Active mode badge`
- `COLLAPSED — every signal nominal: a 3-line rollup (was ~8 lines). Type 'status' for the full board.`
- `Compute the warning signals FIRST so a fully-nominal OS collapses to a rollup line`
- `Data-gathering + snapshot fallbacks are preserved verbatim; only the RENDER collapses.`
- `FULL — at least one non-nominal signal: the per-signal board (unchanged detail).`
- `Header`
- `Inference gaps (non-blocking)`
- `LOAD CONTEXT`
- `Last session`
- `Loaded project context`
- `Mode hints — used by the mode menu + the active-mode badge.`
- `Mode menu`
- `ONE-CALL STATE (axon-plus pr-6): TOOL(axon-state, menu-snapshot) carries every`
- `OS STATE panel`
- `OS state`
- `OUTPUT → PYTHON_FAST · doc`
- `Only renders when there's something worth showing.`
- `Open reminders — boot-surfaced EVERY session until `todo done` (owner directive 2026-05-27).`
- `PR-112 — orchestrator suggestion footer (gated by L:suggestions-enabled, default true)`
- `PROGRAM: menu`
- `Plans`
- `Pointer coherence (axon-stale-pointers PR-002): PR-001's sweep via the snapshot.`
- `Pulls together signals from PR-009 / PR-010 / PR-012 / PR-015 / PR-017.`
- `Queue / Cron — from the tools (bugfix02: W:ws-queue and L:cron-jobs had no writer`
- `Resumable program (interrupted or paused this session)`
- `Snapshot workflows carry {total, reference, user} with no ok key — presence of`
- `Snapshot-failure → ∅ (probe degraded) counts as nominal — the sweep is advisory.`
- `Snapshot-path reminders are capped STRINGS; the tool-fallback rows are objects with`
- `TIP OF SESSION`
- `The persistent to-do list IS the reminder store; the menu renders open items each boot.`
- `Workspace backup status — read from memory/local/ (machine-specific, gitignored)`
- `anywhere, so the queue warning could never fire and the Cron line was dead while`
- `audit total normalized to a SCALAR on both paths (bugfix02: the snapshot field is a`
- `contract-version: neuron-contract v1.1`
- `desc:    AXON home screen — OS state dashboard, mode switcher, active context`
- `domain: meta`
- `family: [meta]`
- `glossary: AXON-GLOSSARY v2`
- `inferred-by: synapse-infer (PR-108 bulk migration)`
- `inputs-count: 30`
- `instead of the full per-signal list (PR-003 axon-hr-ui — kill per-turn nominal noise).`
- `invocation_source: [program]`
- `lines are removed rather than rendered as permanent dead state.`
- `menu.md`
- `number while the fallback returned an object, so .total was undefined on the golden`
- `old COUNT over the capped preview misreported every boot: 4 shown as the total of 8).`
- `orphaning the body onto the resumable block and killing modes [1]–[5].)`
- `outputs-count: 0`
- `path). W:boot-cron-tick / W:boot-last-snapshot had no writer anywhere — their panel`
- `precondition: "true"`
- `probed field below in ~300 tokens (was ~10 separate envelopes). Per-field fallback`
- `role: reader`
- `status: ACTIVE`
- `synapse:`
- `to the original probes when a snapshot field is null — content parity is test-pinned`
- `── SELF-IMPROVEMENT PANEL (PR-018) ─────────────────────────────────`

## Depends on
- (none)
