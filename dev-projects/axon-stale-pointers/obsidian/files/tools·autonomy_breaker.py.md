---
tags: [code, file]
path: tools/autonomy_breaker.py
---

# tools/autonomy_breaker.py

> 14 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `A stable id for 'the same change'. With `anchor` (e.g. the active phase id) the`
- `Is the breaker tripped for `cid`? -> {tripped: bool, reason: str|None}.`
- `Record a gate outcome ('red' | 'green') for change-id `cid`. Returns the new sta`
- `The change-id the GATE uses — anchored to the active phase so an evolving retry`
- `_load()`
- `_save()`
- `_state_path()`
- `anchored_change_id()`
- `autonomy_breaker.py`
- `change_id()`
- `check()`
- `main()`
- `record()`
- `reset()`

## Depends on
- [[tools·rules·r_code_change_requires_pr_phase.py]]
