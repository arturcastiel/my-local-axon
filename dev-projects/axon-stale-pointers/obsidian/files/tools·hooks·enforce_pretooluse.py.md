---
tags: [code, file]
path: tools/hooks/enforce_pretooluse.py
---

# tools/hooks/enforce_pretooluse.py

> 29 symbol(s) · 3 outbound file dependency(ies)

## Symbols
- `A block-reason if `content` hits a TOKENIZED `_dont-do` prohibition governing `t`
- `Path`
- `R9 + the S1 risk-tier extended to the shell tool. The Claude Code Bash tool does`
- `Return True when this repo is AXON-governed.      Checks the tracked sentinel (.`
- `The ACTIVE code-dev project's `_dont-do` files when `tgt` is inside that project`
- `The file a write-tool call targets, or None if this isn't a write tool.`
- `The nearest `_dont-do*.md` governing the target path — walk up from the file to`
- `The new text a write-tool call would put on disk (Write content / Edit new_strin`
- `The shell command a Bash tool call would run, or None.`
- `True if `child` is at or under `parent` (both resolved).`
- `Write-tool arm of the kernel floor (PR-001): an UNATTENDED autonomous run may ne`
- `_active_project_dont_dos()`
- `_axon_active()`
- `_default_ws()`
- `_governing_dont_do()`
- `_is_within()`
- `_meta_field()`
- `_read_stdin_json()`
- `_tool_name()`
- `anti_self_renew_block()`
- `axon-next PR-003: an unattended agent may NEVER (re)grant or renew its own     a`
- `bash_axon_write_block()`
- `bash_command()`
- `dont_do_violation()`
- `enforce_pretooluse.py`
- `kernel_floor_block()`
- `main()`
- `target_path()`
- `write_content()`

## Depends on
- [[tools·_axon_paths.py]]
- [[tools·_longterm.py]]
- [[tools·rules·r_dont_do.py]]
