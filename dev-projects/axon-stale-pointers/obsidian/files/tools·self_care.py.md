---
tags: [code, file]
path: tools/self_care.py
---

# tools/self_care.py

> 16 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `One frontmatter-style field from {project_dir}/_meta.md (lowercased match).`
- `Opt-in remediation: reconcile auto-generable docs (freshness refresh) + re-probe`
- `Pointer-coherence sweep (axon-stale-pointers PR-001) — read-only cross-check of`
- `Run `axon.py <args>` and return (exit_code, stdout). Read-only callers only.`
- `Verify the Claude-Code persistence artifacts that keep the host BECOMING AXON ev`
- `_axon()`
- `_loads()`
- `_meta_field()`
- `_print_report()`
- `_read_first()`
- `heal()`
- `main()`
- `persistence_check()`
- `pointer_check()`
- `run_check()`
- `self_care.py`

## Depends on
- [[tools·_axon_paths.py]]
