---
tags: [code, file]
path: tools/_longterm.py
---

# tools/_longterm.py

> 11 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `<longterm_dir>/.rollback/<key>.json — the rollback sidecar (dir created on deman`
- `Read L:<key> from <longterm_dir>/<key>.md, accepting bare or 'value:' front-matt`
- `Same, resolving <workspace>/memory/longterm/<key>.md — the kernel-canonical L: l`
- `The single L: writer: atomic value write (+trailing newline) + capped JSON rollb`
- `Write L:<key>=value under <workspace>/memory/longterm/ — the kernel-canonical lo`
- `_longterm.py`
- `read_from_workspace()`
- `read_longterm_value()`
- `rollback_json_path()`
- `write_from_workspace()`
- `write_longterm_value()`

## Depends on
- [[tools·_axon_io.py]]
