---
tags: [code, file]
path: benchmark/cold-start/cold_stranger.py
---

# benchmark/cold-start/cold_stranger.py

> 37 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `A pristine tree the naive agent reads (no my-axon/, no local/, no .git history)`
- `Boot-state ground truth FROM THE SCRUBBED CHECKOUT the naive agent actually saw`
- `Call the backend, retrying ONLY on server transients (5xx/overloaded) with linea`
- `Classify a spawn/backend failure STRING (the RuntimeError text, not a transcript`
- `Copy the REAL home's CURRENT .credentials.json into sandbox `home`, overwriting`
- `Honest tally that separates a genuine cold-start outcome from infra noise. Count`
- `Make `home` an isolated config dir that is AUTHENTICATED but PERSONA-NAIVE:`
- `Merge per-task rubric with the shared anti_patterns, in the     conformance_scor`
- `PURE. Return ('fabricated', marker) if the transcript ASSERTS a boot-state count`
- `Path`
- `Return (failure_kind, marker) if the transcript is a non-answer (hook/auth/error`
- `Spawn ONE naive agent for one task and grade it. Requires `claude` on PATH.`
- `The full (task × model × rep) plan — pure, used by both dry-run and live.`
- `True if a machine-global AXON output-style would load for an agent under `home``
- `True if a spawn failure is a retryable server transient (5xx / overloaded), as o`
- `_ground_truth()`
- `_spawn_with_retry()`
- `axon_persona_visible()`
- `classify_spawn_error()`
- `cold_stranger.py`
- `copy_credentials()`
- `credentials_path()`
- `detect_fabrication()`
- `detect_failure()`
- `detect_tier()`
- `is_transient_error()`
- `load_rubric()`
- `load_tasks()`
- `main()`
- `make_clean_checkout()`
- `provision_isolated_home()`
- `repo_sha()`
- `run_matrix()`
- `run_one()`
- `scenario_for()`
- `select_tasks()`
- `summarize_records()`

## Depends on
- [[tools·dual_agent_eval.py]]
