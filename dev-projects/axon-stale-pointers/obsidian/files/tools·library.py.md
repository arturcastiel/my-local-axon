---
tags: [code, file]
path: tools/library.py
---

# tools/library.py

> 35 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `BibTeX entry. If `fetch` (doi->str) given and DOI present, try it first.`
- `Frequency-ranked content terms, stopword-filtered, lowercased.`
- `Gate a list of {claim, confidence[, support]} by certainty:        confidence >=`
- `JSON payload: --input FILE when given (the TOOL()-callable path — programs     h`
- `Noise filter / corrective-retrieval (CRAG) gate over retrieved chunks {id,text,s`
- `Parse a shadow note into {title, authors, doi, year, venue, body, key_terms}.`
- `Rank candidate articles by overlap with library terms + a DOI bonus     (DOI+aut`
- `Turn gap claims into search queries (the --gaps mode of search).`
- `_bibkey()`
- `_read()`
- `_stdin_json()`
- `_term_set()`
- `build_parser()`
- `cite()`
- `cmd_cite()`
- `cmd_gap_queries()`
- `cmd_gate()`
- `cmd_intersect()`
- `cmd_parse()`
- `cmd_partition()`
- `cmd_rank()`
- `cmd_terms()`
- `extract_terms()`
- `format_apa()`
- `format_bibtex()`
- `format_mla()`
- `gap_queries()`
- `intersect()`
- `library.py`
- `main()`
- `parse_shadow()`
- `partition_chunks()`
- `partition_claims()`
- `rank_candidates()`
- `union / intersection(present in >= threshold of articles) / unique-to-one.`

## Depends on
- (none)
