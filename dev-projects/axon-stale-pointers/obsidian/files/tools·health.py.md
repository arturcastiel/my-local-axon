---
tags: [code, file]
path: tools/health.py
---

# tools/health.py

> 38 symbol(s) · 2 outbound file dependency(ies)

## Symbols
- `Default probe: file exists + --help exits 0 within 5s.`
- `Emit→listen roundtrip with a sentinel event + hook-add/list/remove smoke (PR-006`
- `Format a tiny fixture program and assert the .cmp.md output is well-formed.`
- `Match a known query against the dispatch index. Treat empty-index as SKIPPED.`
- `Offline smoke ONLY. A live query here made every health sweep network-dependent:`
- `PR-007 unified compile dispatcher. Smoke: each subcommand listed correctly.`
- `Per-probe timeout with environment headroom (flaky-gate root fix 2026-07-08):`
- `Persist the health score + date — the SINGLE writer of     L:health-score / L:he`
- `Record→read roundtrip on a sentinel program name.`
- `Record→report roundtrip on a sentinel inference gap.`
- `Run section 1a (fast structural pass) and assert valid output.`
- `Verify notify.py loads and its required stdlib/optional imports are available.`
- `_t()`
- `health.py`
- `main()`
- `parse_json_output()`
- `persist_health()`
- `probe_axon_audit()`
- `probe_calculator()`
- `probe_clock()`
- `probe_compile()`
- `probe_compile_write()`
- `probe_diff()`
- `probe_dispatch()`
- `probe_document_parser()`
- `probe_drift()`
- `probe_events()`
- `probe_igap()`
- `probe_kv_store()`
- `probe_notify()`
- `probe_smoke()`
- `probe_tokenizer()`
- `probe_translate()`
- `probe_usage()`
- `probe_validator()`
- `probe_verify()`
- `probe_web_search()`
- `run()`

## Depends on
- [[tools·_axon_paths.py]]
- [[tools·_longterm.py]]
