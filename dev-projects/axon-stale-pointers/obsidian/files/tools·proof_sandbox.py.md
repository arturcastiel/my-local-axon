---
tags: [code, file]
path: tools/proof_sandbox.py
---

# tools/proof_sandbox.py

> 10 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `Run `code` (a Python program) sandboxed. Returns a structured result dict.`
- `_limits()`
- `_net_isolation_prefix()`
- ``['unshare','-rn']` if unprivileged net isolation works here, else [] (detected`
- `build_parser()`
- `cmd_run()`
- `main()`
- `preexec_fn: cap address space, CPU time, and core dumps in the child.`
- `proof_sandbox.py`
- `run_code()`

## Depends on
- (none)
