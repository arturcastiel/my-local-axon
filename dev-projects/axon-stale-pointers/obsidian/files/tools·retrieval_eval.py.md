---
tags: [code, file]
path: tools/retrieval_eval.py
---

# tools/retrieval_eval.py

> 36 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `Answer-token support against retrieved context.      This is not a semantic fait`
- `ArgumentParser`
- `Build a RetrievalCase from an in-memory payload (fixture- or trace-shaped).`
- `Deterministic, model-free structural chunker. Splits on markdown headings, then`
- `Evidence-quality verdict (CRAG-style) over a scored RetrievalCase — the retrieva`
- `Fraction of known relevant chunk IDs retrieved.`
- `Fraction of retrieved chunks that are relevant.`
- `Path`
- `RetrievalCase`
- `RetrievedChunk`
- `Return explicit per-chunk latency when present, else measured evaluation latency`
- `Score a (live or fixture) case and attach the evidence-quality verdict.`
- `Score an in-memory trace/case payload (same shape as a fixture) via score_case.`
- `Token IoU between retrieved relevant context and gold evidence spans.`
- `_append_log()`
- `_case_from_payload()`
- `_case_paths()`
- `_emit_chunk()`
- `_load_case()`
- `_tokens()`
- `aggregate()`
- `build_parser()`
- `chunk_document()`
- `context_precision()`
- `context_recall()`
- `evaluate()`
- `faithfulness_proxy()`
- `latency_ms()`
- `main()`
- `render_text()`
- `retrieval_eval.py`
- `score_case()`
- `score_live()`
- `score_trace()`
- `token_iou()`
- `verdict()`

## Depends on
- (none)
