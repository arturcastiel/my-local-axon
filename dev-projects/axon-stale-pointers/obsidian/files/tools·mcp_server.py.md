---
tags: [code, file]
path: tools/mcp_server.py
---

# tools/mcp_server.py

> 17 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `Gate an exposed MCP call to read-only operation. Returns (ok, reason).`
- `Map a REGISTRY entry to an MCP tool descriptor.      AXON tools take a CLI argv,`
- `Read newline-delimited JSON-RPC from stdin, write responses to stdout.`
- `Route a JSON-RPC message → a response dict, or None for notifications.`
- `Run an allowlisted tool as a subprocess; return an MCP tools/call result.`
- `_is_readonly_call()`
- `_mcp_tool()`
- `dispatch()`
- `exposed_tools()`
- `handle_call()`
- `handle_initialize()`
- `handle_list()`
- `load_registry()`
- `main()`
- `mcp_server.py`
- `serve()`
- `{name: entry} for ACTIVE tools that are on the safety allowlist.`

## Depends on
- (none)
