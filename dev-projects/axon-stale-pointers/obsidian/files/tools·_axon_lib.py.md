---
tags: [code, file]
path: tools/_axon_lib.py
---

# tools/_axon_lib.py

> 18 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `AXON shared library — in-process functions for hot tool calls.  PR-019: lifts th`
- `Append an audit row in-process. Same effect as `auto-audit record`.`
- `Best-effort token count. Falls back to char/4 when tiktoken absent.`
- `Emit an event in-process. Same effect as `events emit`.`
- `Load tools/<script_name> as a module (handles hyphens in filenames).`
- `Read an L: kv-store value. Returns None if absent.`
- `Record a usage entry in-process. Same effect as `usage record`.`
- `Return the drift gate decision dict in-process.      Same shape as `python3 tool`
- `Write an L: kv-store value atomically.`
- `_axon_lib.py`
- `_load_module()`
- `audit_record()`
- `drift_gate()`
- `events_emit()`
- `kv_get()`
- `kv_set()`
- `tokenize()`
- `usage_record()`

## Depends on
- [[tools·_axon_io.py]]
