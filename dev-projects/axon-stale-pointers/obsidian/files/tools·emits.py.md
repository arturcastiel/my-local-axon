---
tags: [code, file]
path: tools/emits.py
---

# tools/emits.py

> 13 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `Find the program that declares ``# phase: {phase}`` and return its ``# emits:``.`
- `Parse ``# emits:`` into a list of root-relative path tokens (globs allowed).`
- `Resolve a program name to {ok, program, phase, emits}.`
- `Return the value of a ``# field:`` header line, or None.`
- `_read()`
- `emits.py`
- `emits_for_phase()`
- `main()`
- `parse_emits()`
- `parse_field()`
- `parse_phase()`
- `program_path()`
- `resolve()`

## Depends on
- (none)
