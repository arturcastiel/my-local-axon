---
tags: [code, file]
path: tools/program_tool_conformance.py
---

# tools/program_tool_conformance.py

> 31 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `.__init__()`
- `.sub()`
- `.top()`
- `Accessor reads against tool outputs: inline `TOOL(a, b, …).key` plus the     var`
- `All TOOL(...) call-sites in `text` → [{tool, sub, flags, file, line}]. Line-scop`
- `ArgumentParser`
- `Cached live introspection of axon.py tool surfaces.`
- `Grandfathered violations (axon-bugfix02 PR-001 ratchet). Identity is     (file,`
- `Path`
- `Remove {…} spans (innermost-first, so nested braces resolve) — template contents`
- `Split a TOOL(...) arg tail on top-level commas (quote-aware, templates already`
- `Subcommand/positional-action set from an argparse usage line; None when absent.`
- `_Prober`
- `_build_parser()`
- `_help_text()`
- `_load_baseline()`
- `_load_manifest()`
- `_print_table()`
- `_scoped_files()`
- `_split_args()`
- `_strip_templates()`
- `_unquote()`
- `_violation_id()`
- ``argv + ['--help']` → (rc, combined output). --help makes argparse print + exit`
- `declared_flags()`
- `declared_subs()`
- `extract_accessor_reads()`
- `extract_call_sites()`
- `main()`
- `program_tool_conformance.py`
- `run_check()`

## Depends on
- (none)
