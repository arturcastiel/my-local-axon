---
tags: [code, file]
path: tools/programs_registry.py
---

# tools/programs_registry.py

> 26 symbol(s) · 2 outbound file dependency(ies)

## Symbols
- `A menu-worthy program: ACTIVE and not a stub/alias/autogen placeholder.`
- `Drift GATE — exit 1 if any program .md is missing from / orphaned in the     reg`
- `Explain one menu category — its purpose line + programs grouped by submenu.`
- `Finer submenu than raw `area`: phase-split code-dev, fold the 'other' bucket.`
- `Map a free-text intent to the most relevant menu category (a routing hint).`
- `Map a program to a top-level menu category (name patterns win over area).`
- `Modernized program-library menu from the registry — a philosophy header,     the`
- `Pull the value after `# marker:` in the first 30 lines.`
- `Pull tool names from `Tools:` header or grep for TOOL(name, ...).`
- `_classify_area()`
- `_classify_status()`
- `_extract()`
- `_extract_tools()`
- `_is_listable()`
- `_menu_category()`
- `_registry_path()`
- `_submenu()`
- `cmd_category()`
- `cmd_check()`
- `cmd_generate()`
- `cmd_intent()`
- `cmd_menu()`
- `cmd_query()`
- `cmd_validate()`
- `main()`
- `programs_registry.py`

## Depends on
- [[tools·_axon_io.py]]
- [[tools·_axon_paths.py]]
