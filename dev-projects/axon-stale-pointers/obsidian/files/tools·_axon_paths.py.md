---
tags: [code, file]
path: tools/_axon_paths.py
---

# tools/_axon_paths.py

> 20 symbol(s) · 0 outbound file dependency(ies)

## Symbols
- `AXON path resolver — single source of truth for repo-anchored paths.  Resolution`
- `Canonical R9 path classifier — True iff `target` resolves to / under the axon/ k`
- `Default --myaxon argument: absolute path to my-axon/.`
- `Default --workspace argument: absolute path to workspace/.`
- `Join parts under MYAXON_ROOT and return a string path.`
- `Join parts under WORKSPACE and return a string path.`
- `Path`
- `Read the W:myaxon-path pointer (workspace/memory/working/myaxon-path.md), parsin`
- `Resolve the kernel root — anchored tamper-resistantly to the tracked `.axon-gove`
- `Return 'env' if AXON_ROOT came from environment, 'file' otherwise.`
- `_axon_paths.py`
- `_resolve_axon_root()`
- `_resolve_myaxon_root()`
- `default_myaxon()`
- `default_workspace()`
- `is_axon_path()`
- `read_myaxon_pointer()`
- `root_source()`
- `under_myaxon()`
- `under_workspace()`

## Depends on
- (none)
