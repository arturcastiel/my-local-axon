---
tags: [code, file]
path: tools/boot.py
---

# tools/boot.py

> 27 symbol(s) · 2 outbound file dependency(ies)

## Symbols
- `Check for .axon/preferences/ in cwd (project-local config).      Three-tier reso`
- `Fallback inference-mode from workspace/preferences/agent.md — the declared     p`
- `First-boot autowire (PR-011): seed the 5 default self-improvement cron     entri`
- `Merge parent workspace tools/programs into child (child wins on conflict).`
- `PR-6.1 — Boot-time wire-up for session.recover().      Locates the my-axon dev-p`
- `Parse 'Inherits: <path>' from WORKSPACE.md.`
- `Return first line of last session summary if stored.`
- `Return list of overdue cron jobs.      Resolves cron.py via AXON_ROOT — works wh`
- `Return summary of the W: snapshot file (if any), without restoring.      Returns`
- `Run a scheduler tick — fires the first overdue job when L:cron-auto ≡ true.`
- `auto_recover_sessions()`
- `boot.py`
- `check_cron()`
- `count_active_queue()`
- `list_tools()`
- `list_working_keys()`
- `load_last_session_snapshot()`
- `load_last_session_summary()`
- `main()`
- `merge_federation()`
- `parse_workspace_paths()`
- `read_inherits()`
- `read_longterm()`
- `read_pref_inference()`
- `read_project_config()`
- `seed_cron_if_first_boot()`
- `tick_cron()`

## Depends on
- [[tools·_axon_paths.py]]
- [[tools·_longterm.py]]
