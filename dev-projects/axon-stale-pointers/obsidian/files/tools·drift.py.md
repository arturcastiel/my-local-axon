---
tags: [code, file]
path: tools/drift.py
---

# tools/drift.py

> 27 symbol(s) · 1 outbound file dependency(ies)

## Symbols
- `Extract tool-call sequence from a program file by static scan.     Catches both`
- `Fail-closed gate payload — same shape/severity as diverged.`
- `Levenshtein on lists. O(len(a) * len(b)) time, O(min(len)) space.`
- `Map 'tools/foo.py' filename stem to canonical tool name from registry.`
- `Map a drift score to {state, decision, modifier}.`
- `Normalized edit distance over the prefix the trace has reached.`
- `Pure helper — returns the gate dict; also used by _axon_lib.drift_gate.      Fai`
- `Return the gate decision for the current drift score.      Fail-closed (PR-AUTO-`
- `_evaluate_gate()`
- `_unknown_gate()`
- `classify()`
- `cmd_check()`
- `cmd_gate()`
- `cmd_init()`
- `cmd_record()`
- `cmd_reset()`
- `compute_score()`
- `drift.py`
- `edit_distance()`
- `extract_expected()`
- `gate_decision()`
- `load_trace()`
- `main()`
- `now_iso()`
- `save_trace()`
- `script_to_tool_name()`
- `trace_path()`

## Depends on
- [[tools·_axon_paths.py]]
