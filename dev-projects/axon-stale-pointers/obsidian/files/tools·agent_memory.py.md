---
tags: [code, file]
path: tools/agent_memory.py
---

# tools/agent_memory.py

> 43 symbol(s) · 3 outbound file dependency(ies)

## Symbols
- `An entry is orphaned when ANY checkable binding's referent is gone.`
- `Cosine TF-IDF of query vs each corpus doc. Mirrors dispatch.py's     primitive —`
- `Every entry across general + each program + each project tier.`
- `Green mode — reconcile memory to disk. Deterministic orphans (a binding's     re`
- `Hybrid capture. Deterministic sources (explicit/fail/decision) are written     i`
- `Id of a near-duplicate existing entry to supersede, else None. Uses     recall;`
- `Is a `kind:value` binding's referent still present on disk? Unknown     kinds (e`
- `Light mechanical floor on the derivability gate. The deep 'is this     re-deriva`
- `Metadata filter: equality on scalar meta fields, subset-match on bindings.`
- `Promote a staged judgment candidate into a real entry (user-approved).`
- `Rank memory entries against a free-text query (TF-IDF), skipping orphaned     en`
- `Read a boolean L: longterm flag (memory/longterm/<key>.md).`
- `Resolve the directory a tier's entries live in. PRIVATE -> my-axon.`
- `Resolve the my-axon root: explicit arg > W:myaxon-path > repo sibling.`
- `_all_entries()`
- `_binding_alive()`
- `_dedup_target()`
- `_digest_path()`
- `_digest_rewrite()`
- `_matches_filters()`
- `_now()`
- `_out()`
- `_read_gate()`
- `_slug()`
- `_tfidf_rank()`
- `_today()`
- `agent_memory.py`
- `capture()`
- `digest_drop()`
- `digest_keep()`
- `digest_list()`
- `entry_alive()`
- `entry_path()`
- `gc()`
- `is_capturable()`
- `list_entries()`
- `main()`
- `parse_entry()`
- `recall()`
- `render_entry()`
- `resolve_myaxon()`
- `tier_dir()`
- `write_entry()`

## Depends on
- [[_unknown_]]
- [[tools·_axon_io.py]]
- [[tools·_axon_paths.py]]
