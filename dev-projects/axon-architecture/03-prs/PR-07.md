# PR-07 · Declarative rule manifest + parity lock (Theme B) · AUTO

## Closes
- **F38** — 4 hand-maintained, disjoint runner lists (registry._collect_rules=19, crucible run_changeset=6,
  lint_summary.RULES_TO_SCAN=9, neuron_audit._LINT_RULES=9) with no parity check; adding a rule means
  editing up to 4 places and forgetting one silently drops it from a gate.
- partial **F03** — locks the 8 identity/override/cognition rules into their runners so they can't
  silently orphan (promoting them from lint/audit-WARN into a BLOCK gate is a separate behavior change).

## Ground truth (measured)
30 rule files (incl. r3_arithmetic, r7_no_symbolic_output, r9_axon_write). ALL 30 are in ≥1 runner today
(verify 19, crucible 6, lint=audit 9) — so no orphan rule file now; the risk F38 raises is FUTURE drift.

## Change
- `tools/rules/manifest.py` — `RULE_MANIFEST` declares every rule-file stem → its runner(s); helpers
  `rules_for_runner` / `runners_for_rule`. Behavior-preserving mirror of the live lists.
- `tests/test_rules_manifest.py` — parity lock: manifest keys == rule files; no rule in zero runners;
  manifest's per-runner set == the actual `verify`/`lint`/`audit` lists; crucible set ⊆ run_changeset.

## Why a lock, not a rewire (scope)
Rewiring the 4 runners to READ the manifest is the riskier follow-up (could break rule loading). This PR
makes the manifest authoritative + the parity test mechanical NOW (it runs in crucible + pytest), so a
silently-dropped rule turns the gate red — closing F38's core without behavior risk.

## Gate
`crucible gate` green → merge. AUTO.
