# PR-09 · Triage orphan backlog + flip the orphan gate to BLOCK (Theme B) · AUTO

## Closes
- **F36 / F06 / F37** — 6 ACTIVE-but-unwired tools + the anti-orphan gate stuck at WARN/grandfathered.

## Change
- Reclassify the 6 orphans ACTIVE → OPTIONAL in REGISTRY.json: a2a, axiom, axon-eval, axon-managed,
  memory-sync, skill-adapter. Each is built + unit-tested but invoked by nothing; OPTIONAL is the honest
  status (fake-wiring a tool nobody calls is worse). The owner can promote+wire any back to ACTIVE.
- Empty `tools/liveness-allow.txt` (backlog cleared).
- Flip the `liveness` crucible control **WARN → BLOCK**: with 0 orphans, a NEW ACTIVE tool invoked by
  nothing now fails the gate. The anti-orphaning loop is closed end-to-end.

## Result
ACTIVE 134 / OPTIONAL 12; `liveness check` → 0 orphans (green now, red on any future un-wired ACTIVE tool).

## Note
Reclassify-not-wire is the conservative, honest call for tools with no current caller. If the gate trips
a stale ACTIVE-count or status-assert test, that's a real doc/test drift to fix-forward (the guarded
merge blocks on it).

## Gate
`crucible gate` green (liveness now BLOCK, 0 orphans) → merge. AUTO.
