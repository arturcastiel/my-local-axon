# PR-05 · Liveness resolver + orphan gate (Theme B) · AUTO  (combines plan PR-5 + PR-6)

## Closes
- **F07** — no single source of truth for "is this tool invoked?"; liveness split across 6 disjoint surfaces.
- **F06 / F26 / F37** — the only wiring checks are diff-only/WARN (r_no_orphan_tools) or existence-only
  (registry_drift); neither does a full-tree invocation census.

## Change
- `tools/liveness.py` — unions all 6 surfaces (program TOOL()/path, tool imports, axon.py dispatch,
  crucible.json control cmds, .pre-commit-config, mcp SAFE_TOOLS) → per-tool reached/by/orphan.
  `report` (JSON) + `check` (exit 1 on any non-allowlisted orphan). Excludes tools/rules/ + tools/hooks/.
- `tools/liveness-allow.txt` — the SHRINKING grandfather list (today's 6 true orphans: a2a, axiom,
  axon-eval, axon-managed, memory-sync, skill-adapter). PR-9 empties it.
- Registered ACTIVE; wired as a **WARN crucible control** (`liveness`) — runs every gate, surfaces
  orphans, never blocks yet. Flips to BLOCK once the allowlist is empty.

## Live result
Resolver reports **133/139 reached, 6 orphans** (all grandfathered) — so `check` is green now; a NEW
un-grandfathered orphan turns it red.

## Tests (tests/test_liveness.py)
- reached via each surface (program TOOL, crucible cmd, mcp SAFE_TOOLS); orphan when none.
- the LIVE tree has no un-grandfathered orphan (`check` green); rules/ + hooks/ excluded.

## Gate
`crucible gate` green (the new control is WARN) → merge. AUTO.
