# PR-10 · One canonical L: reader + fix the dev-mode write-gate split-brain (Theme C) · AUTO
(combines plan PR-11 dev-mode-owner + PR-12 L:-reader — tightly coupled)

## Closes
- **F08 / F46** — `_axon_io` read dev-mode from `my-axon/` (a path the toggle never writes → write-gate
  silently always-OFF) while enforce/verify read `workspace/`; the two gates could disagree (a write-gate
  simultaneously ON and OFF) the moment dev-mode is enabled.
- **F18 / F39** — divergent `value:`/whitespace/case parsing across readers (BF-021 fixed only one).

## Change
- `tools/_longterm.py` — the one canonical reader: `read_longterm_value(dir,key,default)` +
  `read_from_workspace(ws,key,default)` (accept bare or `value:` front-matter; strip; case-insensitive
  sentinel).
- `_axon_io._DEVMODE_KEY` → **workspace** longterm (matches enforce/verify); `_dev_mode_active` reads via
  the canonical reader.
- `enforce.read_longterm`, `verify._read_longterm_value`, `boot.read_longterm` → delegate to the canonical.

## Verified
`_axon_io._DEVMODE_KEY` now resolves to `…/workspace/memory/longterm/dev-mode.md` (was my-axon/);
dev_mode_active=False (reads the real `false`) — no current behavior change, latent split-brain closed.

## Scope
The 4 dev-mode/halt-mode readers are unified here (the safety-critical set). The lower-stakes L: readers
(context, phase_gate, shell, agent_memory) delegating too is a mechanical follow-up.

## Tests (tests/test_longterm.py)
- canonical parses bare / `value:` / whitespace / case / missing-default.
- enforce + verify + boot + _axon_io all read dev-mode identically (no split-brain).
- `_axon_io._DEVMODE_KEY` is under workspace/, never my-axon/.

## Gate
`crucible gate` green → merge. AUTO.
