# PR-08 · Behavioral coverage: kill the silent false-green (Theme B) · AUTO

## Closes (the achievable part of)
- **F05** — `test_behavior.py` skipped every structural check when fixtures were empty → green while
  covering nothing.
- **F28** — "every ACTIVE tool invoked" is already delivered by the liveness gate (PR-5); rule-runner
  parity by PR-7.

## Architectural limit (honest)
AXON programs are interpreted by the HOST LLM, not Python — so true **end-to-end program execution is
not pytest-testable** (there is no Python model-call seam in dispatch; the `_mock_model` harness was
aspirational scaffolding). Behavioral coverage is therefore: structural fixtures + invocation-liveness
(PR-5) + rule-runner parity (PR-7) + per-tool unit tests — not in-process program runs. Claiming
otherwise would be the over-statement the audit targets.

## Change
- Seed a real curated structural case `tests/fixtures/programs/study/golden-smoke/{expected,output}/`
  (overlapping H2 sections) so the structural-overlap check is actually EXERCISED, not skipped.
- `test_behavioral_coverage_present` — fails if NO target has an `expected/` tree (converts the silent
  skip-everything into a red), with the architectural-limit note inline.

## Gate
`crucible gate` green → merge. AUTO.
