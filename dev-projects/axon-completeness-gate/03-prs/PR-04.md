# PR-04 — reconnect emits->seed_outputs + recompile
status: DONE (merged a6ec042)
wave: A
findings: liveness
depends: PR-03

## Goal
reconnect emits->seed_outputs + recompile

## Change
seed-outputs --from-emits resolves each phase program's # emits as SSOT (phase_model imports emits -> live). Recompile stale .cmp.md.

## Acceptance / Test
tests/test_phase_model_outputs.py (from-emits)

## Constraints
- no KERNEL-SLIM edits; no gate bypass; ACTIVE-with-tests; crucible-green before merge.
