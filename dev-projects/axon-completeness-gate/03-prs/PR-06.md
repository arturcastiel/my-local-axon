# PR-06 — workflow_run node outputs schema + verify
status: DONE (merged c141d6f)
wave: A
findings: L4
depends: PR-01

## Goal
workflow_run node outputs schema + verify

## Change
optional per-synapse outputs:; verify_node_outputs(); record_step downgrade; advance refuse. Backward-compat.

## Acceptance / Test
tests/test_workflow_node_outputs.py

## Constraints
- no KERNEL-SLIM edits; no gate bypass; ACTIVE-with-tests; crucible-green before merge.
