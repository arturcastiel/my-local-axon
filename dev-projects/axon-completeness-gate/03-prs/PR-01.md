# PR-01 — phase_model mode-aware completeness gate
status: DONE (merged a6ec042)
wave: A
findings: #2,#8
depends: none

## Goal
phase_model mode-aware completeness gate

## Change
done() reads manifest `outputs` only (SSOT); REQUIRED_OUTPUTS=seed+drift ref. seed_outputs()+CLI.

## Acceptance / Test
tests/test_phase_model_outputs.py

## Constraints
- no KERNEL-SLIM edits; no gate bypass; ACTIVE-with-tests; crucible-green before merge.
