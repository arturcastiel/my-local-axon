# PR-08 — R9: compile_write traversal sanitize + _axon_io
status: DONE (merged 4b0fb8f)
wave: B
findings: #3
depends: none

## Goal
R9: compile_write traversal sanitize + _axon_io

## Change
sanitize --name; assert under out_dir; route via _axon_io.atomic_write.

## Acceptance / Test
tests/test_compile_write_traversal.py

## Constraints
- no KERNEL-SLIM edits; no gate bypass; ACTIVE-with-tests; crucible-green before merge.
