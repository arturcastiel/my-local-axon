# PR-11 — completeness keystone (the meta-gate)

Status: merged
Merged: → main (squash) · crucible green 29 controls · zero warnings · self-hosting
Branch: general-bugfix/pr-11-keystone → main
Depends-on: PR-0a/0b, PR-7, PR-8 (all merged)
Phase: 3-prs
Covers: the WARN-graveyard/orphan-guard prevention hole; landed EARLY so it guards PR-9/PR-10

## Goal
The prevention spine fails open in two quiet ways: a WARN control someone meant to
promote and forgot (graveyard), and a control whose tool was deleted (guards nothing,
"passes" forever). The keystone makes both impossible.

## Change
- **All 11 WARN controls explicitly dispositioned**: 4 `promotes_on` (registry-drift,
  coverage-gate, budget-lint, freshness — each with its earning condition) and 7
  `advisory` (coherence-lint, harness-conformance, audit-axon-lang, lint-paths,
  neuron-audit, metric-integrity, lint-summary-rules — each with why it can never be
  deterministic). The "already-wired" critic correction applied: no blanket promotion —
  promotion is earned per-control, the keystone enforces EXPLICITNESS.
- **New `tools/keystone.py`** (crucible **BLOCK**, the 29th control, self-hosting):
  (1) every non-BLOCK control carries `advisory` or `promotes_on`, else violation;
  (2) every control's cmd references an existing script (orphan-guard check).
- Spine census at wire time: 17 BLOCK · 7 advisory · 4 promotes_on · 0 violations.
- Tests (4): live fail-closed invariant · graveyard negative · orphan negative · CLI gate.

## Guarded-by
- Itself (self-hosting) + `R_NEW_NEEDS_TEST`. Full gate green, zero warnings (29 controls).
