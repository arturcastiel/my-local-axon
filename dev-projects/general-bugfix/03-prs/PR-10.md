# PR-10 — doc/UI honesty (R6)

Status: merged
Merged: → main (squash) · crucible green 29 controls · zero warnings
Branch: general-bugfix/pr-10-doc-honesty → main
Depends-on: PR-0d (merged)
Phase: 3-prs
Covers: §C doc-drift (the resume briefing + architecture map misleading every agent that reads them)

## Change (each item verified against live values first)
- CONTEXT.md version pin **3.7.0 → 3.8.0** with a current parenthetical.
- CONTEXT.md test-surface claim "~3,900 across 214 files" → live **4,138 across 270**
  (dated, with the live-count command named).
- CONTEXT.md structure map gains the **graphify-obsidian self-introspection subsystem**
  row (was absent from the resume briefing).
- AXON-DOCS-ARCHITECTURE.md tool counts ×2: **84 (77+7) → 160 (142+18)**, with the
  registry named as the live source of truth.
- AXON-DOCS-ARCHITECTURE.md **six/seven-subsystems self-contradiction** resolved
  (header → seven; the doc itself contains the seventh — the composition path).
- **Pins added** (F58-class, `test_onboarding_counts.py`): the ARCHITECTURE tool count
  is locked to the live registry (it can never silently drift again) + the subsystem
  count must stay self-consistent.

## Verified-no-bug (study items already healed by earlier waves)
- "Misleading `tests:` headers" — 0 phantom references found (proper resolver scan).
- "Advertised-but-unimplemented commands/modes/flags" — the known instances (menu
  modes, compile rows, mode-router claims) were fixed in Waves 1–2; the conformance
  lint (BLOCK) now covers the flag class mechanically (the r6 fold the spec asked for).
- lifecycle-tour/help auto-generation: deferred to reduce-surface (route-manifest is
  its natural source).

## Guarded-by
- The two new onboarding pins + the existing CONTEXT pin.
