# PR-142: Audit-warn cleanup: close remaining axon-audit warns

**Wave:** 3 — Hardening
**Status:** SHIPPED
**Depends on:** PR-119..120

## Goal

After PRs 119–120 land, axon-audit will still emit some warnings (typed but lower-severity items). Close out the remaining ones so `verdict: OK` is achievable.

## Touch

- Sweep remaining `WARN` entries — likely a mix of misc unresolved refs, file-path drift, registry hygiene.
- Patch the corresponding source files.

## Tests (mandatory per axon-tests PR-020)

- `tests/test_integration.py::test_section_1a_healthy` tightens its assertion from `WARN <= 5` to `WARN == 0`.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-GOVERNANCE.md § Audit invariants`. Guarded-by row added.

## Notes / risks

Data-driven; can't size precisely until upstream lands. May split into 142a/b. Optional — project can ship without this PR if user wants to defer.
