# PR-141: Pre-push hook adds tools/test.py --all

**Wave:** 3 — Hardening
**Status:** SHIPPED
**Depends on:** PR-121

## Goal

Catch corpus regressions locally, before push. `tools/test.py --all` should run in ≤ 2 s on a clean corpus.

## Touch

- `scripts/install-hooks.sh` — append a third step to the generated pre-push hook: `python3 tools/test.py --all || { echo "pre-push: program lint failed" >&2; exit 1; }`. Idempotent installer must regenerate.

## Tests (mandatory per axon-tests PR-020)

- Extend `tests/test_install_hooks.py` with a case asserting the pre-push script references `tools/test.py --all`.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-TESTING.md § Pre-push` with the new step. Guarded-by row added.

## Notes / risks

Order matters: smoke → test.py --all → secret scan. Fastest fail first.
