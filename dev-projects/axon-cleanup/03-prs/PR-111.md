# PR-111: Fix authoring-guide.md DONE placeholder

**Wave:** 1 — Corpus sweep
**Status:** SHIPPED
**Depends on:** PR-101..102

## Goal

`authoring-guide.md` has literal `DONE({program-name})` placeholder text — template never specialised.

## Touch

- `workspace/programs/authoring-guide.md` — replace `DONE({program-name})` with `DONE(authoring-guide)`. Also verify no other `{placeholder}` strings remain.

## Tests (mandatory per axon-tests PR-020)

- Parametrised case `test_program_structure[workspace::authoring-guide]` clears.

## Doc co-output (mandatory)

- Doc: this is the authoring guide itself — if it documents the DONE() contract, double-check the example still reads sensibly.

## Notes / risks

Smallest PR in the wave. Could be merged with PR-110 but keeping separate for traceable history.
