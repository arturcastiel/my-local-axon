# PR-119: Resolve 50 Unresolved EXEC references

**Wave:** 1 — Corpus sweep
**Status:** SHIPPED
**Depends on:** PR-101..118

## Goal

axon-audit reports ~50 `Unresolved EXEC('workspace/programs/<name>.md')` warnings across router/composite programs (code-dev-flow, code-dev-knowledge, code-dev-state, etc.). Each callee either (a) was renamed, (b) was deleted, or (c) was planned but never authored.

## Touch

- For each unresolved EXEC: classify (a/b/c). If (a) fix the caller's EXEC string. If (b) remove the EXEC. If (c) author a thin stub program that documents the planned behaviour and emits `DONE()` — better than a silent dangling reference.
- Touches ~12 caller files + N new stub files.

## Tests (mandatory per axon-tests PR-020)

- axon-audit `internal_refs` WARN count drops by ~50.
- Extend `tests/test_integration.py::test_section_1a_healthy` to assert `unresolved-exec` count <= 5 (allowing for residual deferred items).

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-PLAN.md § Composite programs` to document the resolution. Guarded-by row added.

## Notes / risks

Highest-risk PR in the wave because it touches the call graph. Land after PR-118 so the structural sweep is done first. Consider splitting if the file count exceeds 15.
