# PR-112: Add OUTPUT + DONE + priority to code-dev-audit family

**Wave:** 1 — Corpus sweep
**Status:** SHIPPED
**Depends on:** PR-101..102

## Goal

`code-dev-audit.md` is missing priority flag, DONE() call, OUTPUT section, and banner. Likely a stub that never got finished.

## Touch

- `workspace/programs/code-dev-audit.md` — author full OUTPUT + DONE(code-dev-audit) + `!NORM` priority + ▶ banner.
- Same audit for sibling files: `code-dev-rules-audit.md`, `code-dev-safety-audit.md`, `code-dev-safety-audit-structure.md`. Apply the missing sections.

## Tests (mandatory per axon-tests PR-020)

- Per-file parametrised cases in `test_programs_md.py` clear.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-GOVERNANCE.md § Audit programs` or equivalent. Guarded-by rows added.

## Notes / risks

Verify the OUTPUT narrative is semantically correct, not just structurally valid. These programs need to *do* something meaningful.
