# PR-103: Fix test_plan_dag — read JSON from CompletedProcess

**Wave:** 0 — CI unblock
**Status:** SHIPPED
**Depends on:** PR-101

## Goal

`test_real_axon_master_dag` does `result["key"]` on a `subprocess.CompletedProcess`. Pure test bug.

## Touch

- `tests/test_plan_dag.py` — change to `data = json.loads(result.stdout); data["key"]`. Three-line patch.

## Tests (mandatory per axon-tests PR-020)

- Same test guards itself once fixed.

## Doc co-output (mandatory)

- No doc surface — test-only fix.

## Notes / risks

Confirm `tools/plan_dag.py` (or whichever) actually emits JSON on stdout.
