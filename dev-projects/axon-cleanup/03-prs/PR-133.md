# PR-133: Guard test: tests/test_requirements_intent.py

**Wave:** 2 — Deps cleanup
**Status:** SHIPPED
**Depends on:** PR-131..132

## Goal

Prevent future re-introduction of unused dependencies. Test asserts every package declared in `pyproject.toml`'s `dependencies` and `optional-dependencies` either appears in at least one `import` statement somewhere in the repo OR is in an explicit whitelist with a justification.

## Touch

- New `tests/test_requirements_intent.py` (~50 LOC). Reads `pyproject.toml`, grep imports across `tools/`, `tests/`, `axon.py`, `scripts/`, fails on orphans.

## Tests (mandatory per axon-tests PR-020)

- Whitelist mechanism: a top-of-file dict `ALLOWED_NO_IMPORT = {"pytest-cov": "plugin loaded via pytest config", ...}`.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-TESTING.md § Dependencies` with Guarded-by row.

## Notes / risks

Edge case: `pytest-cov` is loaded via pytest config not import — whitelist explicitly.
