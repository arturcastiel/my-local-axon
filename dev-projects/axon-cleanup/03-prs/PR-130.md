# PR-130: Confirm chromadb has no live caller; archive semantic tools

**Wave:** 2 — Deps cleanup
**Status:** SHIPPED
**Depends on:** PR-101

## Goal

User decision Q1: drop chromadb entirely. Before pulling the package, confirm no active program or tool calls it.

## Touch

- `grep -r "chromadb\|sentence_transformers\|huggingface_hub\|transformers\b" tools/ workspace/programs/ axon/ axon.py` — must return no production hits.
- If hits exist: archive those files under `tools/_archive/` (kernel doesn't allow `axon/` writes; archive under `tools/` is fine). Log a deviation describing what was archived.
- If clean: noop PR — just records the grep result as evidence.

## Tests (mandatory per axon-tests PR-020)

- Extend `tests/test_requirements_intent.py` (new in PR-133) to forbid future re-introduction.

## Doc co-output (mandatory)

- Document the decision in `workspace/AXON-DOCS-GOVERNANCE.md § Removed subsystems` with Guarded-by row.

## Notes / risks

Runs first in Wave 2. If grep surfaces something unexpected, **stop and ask user** before archiving.
