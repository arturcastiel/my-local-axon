# PR-104: Fix test_call_graph — restore or rename cycles key

**Wave:** 0 — CI unblock
**Status:** SHIPPED
**Depends on:** PR-101

## Goal

`test_cycle_detected` raises `KeyError: 'cycles'`. Either `tools/call_graph.py` dropped the key (production bug — restore) or it renamed it (test bug — update test).

## Touch

- Inspect `tools/call_graph.py`. Two paths:
  - If renamed → update the test to read the new key.
  - If dropped → restore the key in the tool output.

## Tests (mandatory per axon-tests PR-020)

- Existing test guards once the patch lands.

## Doc co-output (mandatory)

- If the tool output schema is documented anywhere in AXON-DOCS, update + Guarded-by row.

## Notes / risks

Lean toward restoring the key — the test name encodes a real behaviour we want to keep.
