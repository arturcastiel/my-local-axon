# PR-131: Rewrite deps as pyproject.toml [project] + extras

**Wave:** 2 — Deps cleanup
**Status:** SHIPPED
**Depends on:** PR-130

## Goal

User decision Q2: pyproject.toml extras. Replace freeze-style `requirements.txt` with `[project]` + `[project.optional-dependencies]` table.

## Touch

- `pyproject.toml`:
  - Add `[project]` with `name = "axon"`, `version` (pull from `VERSION` file), `dependencies = [...]` (curated runtime list).
  - Add `[project.optional-dependencies] dev = [...], tests = [...]`.
  - Existing `[tool.pytest.*]` / `[tool.coverage.*]` stay.
- `requirements.txt`: replace with `-e .[dev]` shim, or delete and update installation docs.
- `SETUP.md`, `CONTRIBUTING.md`: update install commands.
- `.github/workflows/ci.yml`: change pip install to `pip install -e .[dev]`.

## Tests (mandatory per axon-tests PR-020)

- Existing `tests/test_smoke.py::test_pyproject_has_pytest_config` still passes.
- Add 1–2 cases to `tests/test_coverage_config.py` asserting `[project] dependencies` exists.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-TESTING.md § Local setup`. Guarded-by row added.

## Notes / risks

Biggest PR in Wave 2. Pre-flight by running `pip install -e .[dev]` locally before pushing.
