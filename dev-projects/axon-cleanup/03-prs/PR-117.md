# PR-117: Add OUTPUT to library-dev-* family

**Wave:** 1 — Corpus sweep
**Status:** SHIPPED
**Depends on:** PR-101..102

## Goal

`library-dev-{cite,explain,ingest,intersect,new,report,search,status}.md` + `library-dev.md` — author OUTPUT for each.

## Touch

- 9 files in `workspace/programs/`.

## Tests (mandatory per axon-tests PR-020)

- Parametrised cases clear.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-STUDY.md § library-dev` (library-dev is research/lit programme). Guarded-by rows added.

## Notes / risks

If library-dev is going to be deprecated, log a deviation and just stub the OUTPUT blocks with `done(library-dev-*)` plus `# planned: full spec in axon-library-dev project`.
