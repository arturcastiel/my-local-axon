# PR-110: Add OUTPUT + ▶ banner to auto-* programs

**Wave:** 1 — Corpus sweep
**Status:** SHIPPED
**Depends on:** PR-101..102

## Goal

`auto-actions.md` is missing the `▶` banner inside OUTPUT; `auto-improve.md` is missing the entire `## OUTPUT` section. Both must conform to the structural linter.

## Touch

- `workspace/programs/auto-actions.md` — add `▶` banner block inside OUTPUT.
- `workspace/programs/auto-improve.md` — add `## OUTPUT` section with `▶` banner and exit narrative.

## Tests (mandatory per axon-tests PR-020)

- `tests/test_programs_md.py` parametrised cases for these two programs go green automatically.

## Doc co-output (mandatory)

- Update `workspace/AXON-DOCS-WORKFLOWS.md § auto-*` if these programs are documented there. Guarded-by row added.

## Notes / risks

Use the canonical OUTPUT shape from a known-passing program (e.g. `code-dev.md`) as the template.
