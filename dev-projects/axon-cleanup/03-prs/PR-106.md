# PR-106: Refresh test_rename_safety identity snapshot OR fix tool

**Wave:** 0 — CI unblock
**Status:** SHIPPED
**Depends on:** PR-101

## Goal

`test_diff_identity_is_clean` expects rename diff is empty for an identity rename. axon-3.6.1 edits to `axon/programs/identity.md` likely made the snapshot stale.

## Touch

- Inspect the diff. If genuinely benign (whitespace, comments) → refresh the snapshot. If meaningful → trace why rename-safety produced non-clean output for a no-op rename and patch `tools/rename_safety.py`.

## Tests (mandatory per axon-tests PR-020)

- Existing test guards once snapshot is fresh.

## Doc co-output (mandatory)

- No new doc surface unless the tool changed behaviour.

## Notes / risks

**Human review of the diff required** before snapshot bump. Don't auto-accept.
