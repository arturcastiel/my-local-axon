# PR-121: Regenerate generated/compiled/programs/ snapshot

**Wave:** 1 — Corpus sweep
**Status:** SHIPPED
**Depends on:** PR-110..120

## Goal

After the corpus sweep, the committed compiled snapshot is stale. `test_compiled_regression.py` still asserts the linter on the snapshot — regenerate it once the source corpus is clean.

## Touch

- Run `python3 tools/compile_optimizer.py` (or the canonical entry point) to regenerate `generated/compiled/programs/`.
- Commit the regenerated artefacts.

## Tests (mandatory per axon-tests PR-020)

- `test_compiled_regression.py` parametrised cases all clear.

## Doc co-output (mandatory)

- No doc surface (snapshot is mechanical).

## Notes / risks

Runs last in Wave 1 — must come after every source program is fixed. Wave 3 PR-140 will make this part of CI.
