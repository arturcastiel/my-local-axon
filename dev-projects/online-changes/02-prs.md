# 02-prs.md — numbered PR list (dependency-ordered; no PR depends on a later one)

| PR | Phase | Title | Effort | Depends on |
|----|-------|-------|--------|-----------|
| PR-0 | 0 | Land the aoxn-rag retrieval-eval foundation on main (merge + 2 bookkeeping fixes) | S | None — this is the root precondition. Every later RAG-reach PR (Phase 1 MCP SAFE_TOOLS exposure of retrieval-eval/rag-maturity-audit in tools/mcp_server.py with the --append-log/--root flag-deny; Phase 2 list-tools/menu discoverability; Phase 3 __all__ public-surface; Phase 4 standalone packaging) requires tools/retrieval_eval.py + tools/rag_maturity_audit.py to exist on main, which only this PR provides. |
| PR-1 | 0 | Guarded MCP exposure of retrieval-eval + rag-maturity-audit (read-only; closes the --append-log/--root write hole) | S | None. Stands alone on main. REGISTRY.json already has both tools ACTIVE (verified), and the _is_readonly_call/exposed_tools/handle_call machinery already exists. |
| PR-2 | 0 | Discoverability: registry-fallback in list-tools + menu SELF-OBSERVE band for the two RAG tools | S | None. Independent of the Phase-1 MCP-exposure PR (study confirms Phases 1, 2, 3-guard, 4-import-guard are mutually independent). It does NOT depend on the two RAG tools being MCP-exposed; list-tools/menu render from the local registry regardless. Land in parallel. |
| PR-3 | 1 | retrieval_trace.py keystone — live sparse retrieval → RetrievalCase adapter (capture/replay/dump-case) + invoke _append_log | M | none — keystone, first PR of phase 1. retrieval_eval.py (_load_case, score_case, _append_log, --append-log) and the four sparse retrievers already exist on main. All later phase-1/2/3 rows (latency p50, freshness checker, rerank, noise-gate, CRAG verdict, dense B1 index) depend on THIS adapter for their eval-lock; nothing this PR needs depends on them. |
| PR-4 | 1 | Wave-1 observability quick-wins: latency p50, retrieval-index freshness, metadata filters, should_retrieve | M | None — this is a standalone Wave-1 quick-wins PR. It does NOT depend on the keystone retrieval_trace.py (PR for E8/D4) and does not block it; the p50/perf_counter stamps it adds are forward-compatible with the trace adapter (which will read the same `latency_ms` field). Recommend landing AFTER any in-flight retrieval_trace PR only to avoid touching the same dispatch.match result-dict lines twice, but no hard ordering. If the leverage-plan's `--append-log` wiring lands separately, this PR is orthogonal to it. |
| PR-5 | 1 | Deterministic structural chunker as a retrieval_eval subcommand + per-chunk metadata (kill the raw-text[0..8000] truncation) | M | None — this is the A-dimension keystone and the first PR. It does NOT depend on the retrieval_trace adapter (the synthesis's separate Wave-1 keystone). It precedes any dense work (B1): the dump-case fixtures B1 must be graded against are exactly the chunk corpora this PR produces. The metadata_filters quick-win (B5) and retrieval_trace can build on the {source,section,doc_type} fields this lands, but neither blocks this PR. |
| PR-6 | 2 | Second-stage reranker over already-retrieved candidates, locked by a context_precision before/after delta gate | M | None hard-required to land the tool + gate (it operates on any `retrieval_eval`-shaped case, and the real `agentic_anchor.json` fixture already exists). Reuses the wave-1 `RetrievalCase`/`context_precision` contract already shipped in `tools/retrieval_eval.py`. Wave-1's `tools/retrieval_trace.py` capture adapter is the eventual SOURCE of live cases to rerank, so reranking LIVE dispatch/recall traffic (vs the static fixture) depends on that adapter landing first — but that is consumer-wiring follow-up, not a blocker for this PR's gate. |
| PR-7 | 2 | Chunk noise-gate (accept/ambiguous/reject) + token-budget context packing | S | None hard. Operates entirely on score fields and budgets that already exist (RetrievedChunk.score, resolve_limit, _count_tokens) and the already-shipped fixture agentic_anchor.json. Independent of the keystone retrieval_trace.py emitter and of any dense index (B1). If the live-trace emitter PR lands later, partition_chunks/pack become its recordable buckets, but this PR is gradeable today via the fixture. |
| PR-8 | 2 | CRAG verdict (accept|ambiguous|reject) + AEGIS-gated bounded web fallback | M | None hard-required to land: score_live consumes a RetrievalCase JSON (the existing _load_case fixture shape, retrieval_eval.py:48), so it does NOT block on the unbuilt Wave-1 keystone tools/retrieval_trace.py. SOFT dependency: live (non-fixture) driving of retrieval-reflect over real dispatch/recall traffic needs that trace adapter to emit RetrievalCases; until it exists the program runs on dump-case/fixture inputs. If the Wave-1 trace adapter and Wave-2 noise-gate (tools/retrieval_quality.py) land first, the verdict can share their bucket thresholds — but this PR defines them locally to stay independently shippable. |
| PR-9 | 3 | Dense embedding index — DEFER-gated on a measured Wave-1 recall gap; ship only the net-negative-surface sparse() consolidation now | M | HARD: Wave-1's tools/retrieval_trace.py (the live RetrievalCase emitter) and the retrieval_eval --append-log wiring (retrieval_eval.py:206 _append_log, currently built-but-uninvoked) MUST land first — they produce the my-axon/log/retrieval-evals/ traces the precondition probe measures. Without them the recall gap cannot be measured and Part B cannot be honestly gated (only Part A could ship). Recommended: also after Wave-2's noise-gate/rerank so the dense gain is measured on top of the cheaper sparse-side precision wins (avoids crediting dense with gains a reranker already delivers). Part A (sparse() consolidation) has NO dependency and can ship immediately. |
| PR-10 | 3 | RRF hybrid fusion (sparse + dense) — tools/retrieval_fusion.py + retrieval_index query --hybrid | M | PR-9 (tools/retrieval_index.py with the dense index, the `query` subcommand, `dump-case`, and the shared sparse()/dense() functions + RetrievedChunk dict shape). PR-10 imports sparse()/dense() and extends query; it cannot land or be graded before PR-9's dense list and dump-case fixtures exist. Transitively assumes PR-9's chain back to the retrieval_trace/dump-case adapter (the Wave-1 keystone) that produces real fixtures. |
| PR-11 | 3 | Query planner: expand (HyDE/step-back) + decompose + route, fused into the orchestrator's OBSERVE query | M | PR-9 (tools/retrieval_index.py with `query --hybrid`, `dump-case`, shared `sparse()`, dense fallback) and PR-10 (tools/retrieval_fusion.py `rrf(sparse,dense,k=60)`) MUST land first — confirmed NOT yet present (no retrieval_index.py / retrieval_fusion.py in tools/). The planner's subquery retrieval and fusion are built directly on their `query --hybrid` + `rrf` APIs. Transitively depends on the Wave-1 RetrievalCase/dump-case machinery the leverage.json keystone established. No dependency on the agent-memory/library metadata_filters PRs. |
| PR-12 | 4 | In-process surface (__all__) + standalone portability for retrieval_eval, no package shim | S | None. Independent of the phase-1 MCP-exposure and phase-2 discoverability PRs in /tmp/avail.json — it touches neither tools/mcp_server.py nor any program/menu file. Can land standalone. |

Full per-PR specs in `03-prs/PR-NN.md`.