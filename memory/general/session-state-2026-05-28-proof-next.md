---
id: session-state-2026-05-28-proof-next
tier: general
scope-ref: 
bindings: 
source: session log 2026-05-28 (updated after B2.5)
date: 2026-05-28
confidence: high
privacy: private
supersedes: 
---
SESSION STATE (2026-05-28). 18 PRs merged to TNO main (MR !3..!20), tags v3.8.0 + 5 dev checkpoints (safety / wedge-complete / meta-fixes / proof-machinery / proof-harness-mms). SHIPPED: (safety) dont-do-enforce x3, dag-consistency 1-gate, compiled-mirror shrink+prefer-compiled:false, commit-trailer --range/--stdin backstop; (release) v3.8.0; (axon-million WEDGE feature-complete) axiom coherence+enforcement-gaps+portability+report; (boot reminder) menu surfaces open todos until done; (META-FIXES from concerns review) project-refresh (tracking drift) + metric-integrity/R_METRIC_GROUNDED (hollow self-metrics); (PROOF MACHINERY) benchmark/METHODOLOGY.md (owner: "I like the methodology"), proof_sandbox (fail-closed), proof_mms (MMS oracle: sympy forcing + convergence-order grader), and **B2.5** = the objective grader wired into dual_agent_eval.

PROOF STATE — the benchmark is now RUNNABLE END-TO-END + OBJECTIVE. Use `python3 tools/dual_agent_eval.py run-mms` (NOT the old self-graded prose `run`/run.sh): it manufactures an MMS PDE goal (forcing/BCs/grid, NEVER u*), runs the AXON arm + bare arm, extracts the operator's produced solver, and grades it for convergence at the expected order via proof_mms.grade — a win is an ORACLE PASS, never a model say-so. Paired AXON-vs-bare → Wilson-CI verdict (conservative: ties=non-wins). Leakage HARDENED: family excludes eigenfunctions (f∝u* would leak u*'s shape via forcing) + fail-closed guard; METHODOLOGY §6.A = two-layer defense (order-check also rejects a hardcoded-exact answer).

PROOF TARGET COMPLETE (tag v3.8.0-dev-proof-target): B4 preflight + B5 prereg both MERGED. `dual_agent_eval.py preflight --n K` = PRICE-INDEPENDENT conclusiveness gate (best-case Wilson CI + N_min + CI at assumed win-rate) + caveated $ est; `prereg --seeds 0..K --model <m>` writes a LOCKED record (fixed bar CI-lower>0.5, git commit + sha256 fingerprint of methodology+oracle+harness, embedded power projection) to commit BEFORE running; protocol in benchmark/PRE-REGISTRATION.md. FLOW: preflight → prereg (commit it) → run-mms. BREADTH SHIPPED (tag v3.8.0-dev-proof-spectrum): a 2nd MMS field — 1D advection-diffusion/transport (reservoir-adjacent), u_t+c·u_x=α·u_xx+f, validated order-2 CN+central reference — plus operator dispatch (goal id = "operator:seed", e.g. heat:0/advdiff:1; grader operator-agnostic; run-mms/preflight/prereg take --goals). Families expanded to 6 each → 12 mixed goals, ALL reference solvers order ~2 (selftest sweep); preflight(12) conclusive-capable at win-rate 0.85 (CI lower 0.552). The MMS unlock realized: breadth across fields with ONE automatic oracle, no extra experts. REMAINING for the live NUMBER: B3 full-AXON-over-MCP arm (today the AXON arm is an AXON-discipline PROMPT via make_operator, not full AXON+tools — todo 43b4bf4b, owner-steered fidelity) · OPTIONAL more breadth (Buckley-Leverett analytical oracle = methodology oracle-type 2, or a 3rd MMS field) · THEN HUMAN: cheap pilot → confirm effect → scale → headline (Opus) → THE NUMBER.

PAUSED 2026-05-28 pending an internal TNO discussion on HOW to run it. TWO backends now merged (MR !25): (A) `--backend anthropic` = pay-per-token API key (separate console account, no cap; bare-model base — the locked design); (B) `--backend claude-cli` = the Code CLI headless (`claude -p`) on a Pro/Max SUBSCRIPTION (no API key — backend strips ANTHROPIC_API_KEY to force subscription; throttled not over-billed; base is a full agent-with-tools, a real-world H1 variant). Colleague-facing explainer (kept OUT of the repo, owner's request): /home/arturcastiel/projects/new-axon/AXON-benchmark-guide.md (what/why/2 options/config/commands/limitations). Pre-reg for an API-Haiku pilot already committed (MR !24); a subscription run needs a fresh prereg with the CLI model (e.g. sonnet) + the agent-base note. Loadable resume anchor = todo cbe1b46d (boot menu surfaces it). AFTER the discussion: pick option+model → prereg → run-mms → bring the verdict back to interpret. While ON HOLD (proposed + built), the Buckley-Leverett ANALYTICAL oracle is DONE + merged (tag v3.8.0-dev-proof-bl-oracle, MR !26): tools/proof_bl.py — fractional flow + Welge shock saturation (S_f=0.577 @ M=2, textbook) + closed-form rarefaction+shock profile + Rusanov reference + L1/front grader; validated across M={1.5,2,3,5}; wrong/non-conservative/rarefaction-missing solvers FAIL; sandbox output cap raised 8KB→1MB (finer shock grids); registered in tools/REGISTRY.json; 8 tests. This is the methodology's oracle-TYPE #2 (analytical), reservoir-native. BL is now WIRED into run-mms (MR !29, done e2ae00a9): _resolve_goal routes 'bl:N' → proof_bl.grade (L1+front), else → proof_mms.grade (order-fit); render_mms_goal handles both spec shapes. So ONE run can span 3 oracle types across 3 fields: `run-mms --goals heat:0,advdiff:0,bl:0`. Optional remaining breadth (todo 3a33424a, do AFTER the pilot): a 3rd MMS field + property oracles.

LANE RUN 2026-05-28 (owner sequence 2→3→4→1, 10 MRs !20–!29): L2 safety (lint_paths sees new+excludes-gitignored files MR !27, pre-commit hook re-enabled; verified+closed 116bf0af brand-gate + ccc8e2b0 handoff-rule already built). L3 wedge (axiom covers Windsurf/Cline/Copilot-per-path/Junie + dir-glob kind fix MR !28; feeders deferred). L4 compiled-mirror KILL decision-locked + scoped+queued (todo 20166489: decouple dispatch-index from compile first — found compile_suggest populates it; partial attempt reverted). L1 BL wired (above). STILL HUMAN: proof API-key/budget + X1 Stop-hook (signature prereq + ~/.claude, supervised). Relates to [[proof-pillar-is-the-bottleneck]] [[autonomous-loop-wired-resume-pointer]].
